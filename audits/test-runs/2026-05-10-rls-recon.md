# Anon REST surface audit — recon

**Generated:** 2026-05-10T00:01:16.423Z
**Source script:** `audits/scripts/anon-surface-audit.ts` (re-runnable)
**Auth:** publishable apikey only — no `Authorization` header
**Endpoint pattern:** `https://xpcjanqrcgzjmwketxtt.supabase.co/rest/v1/<surface>?select=*&limit=5`
**Filtered-probe fixture:** QA row 1 proof `f47c88c1-98a9-4719-94c6-f4e3cea8a175` (version `f88a97dd-f5d8-4251-bd09-7a41392d2cb0`)

Recon-only. No fixes proposed.

## Summary

| Surface | Type | Anon SELECT * status | Rows leaked | Priority |
|---|---|---|---|---|
| `proofs` | table | 401 | — | clean |
| `proof_versions` | table | 401 | — | clean |
| `proof_version_images` | table | 401 | — | clean |
| `proof_round_variants` | table | 200 | 0 (filtered) | clean |
| `proof_name_approvals` | table | 200 | 0 (filtered) | clean |
| `proof_pins` | table | 200 | 0 (filtered) | clean |
| `proof_events` | table | 200 | 0 (filtered) | clean |
| `proof_version_views` | table | 200 | 0 (filtered) | clean |
| `contacts` | table | 200 | 0 (filtered) | clean |
| `companies` | table | 200 | 0 (filtered) | clean |
| `materials` | table | 401 | — | clean |
| `material_variants` | table | 401 | — | clean |
| `material_options` | table | 401 | — | clean |
| `material_option_surcharges` | table | 401 | — | clean |
| `price_tiers` | table | 401 | — | clean |
| `add_ons` | table | 200 | 0 (filtered) | clean |
| `add_on_prices` | table | 200 | 0 (filtered) | clean |
| `letterpress_core_colours` | table | 401 | — | clean |
| `site_settings` | table | 401 | — | clean |
| `reply_templates` | table | 200 | 0 (filtered) | clean |
| `audit_log` | table | 200 | 0 (filtered) | clean |
| `profiles` | table | 200 | 0 (filtered) | clean |
| `public_proofs` | view | 401 | — | clean |
| `public_proof_versions` | view | 401 | — | clean |
| `public_proof_version_images` | view | 401 | — | clean |
| `public_dashboard_projects` | view | 401 | — | clean |
| `public_site_settings` | view | 401 | — | clean |
| `public_material_options` | view | 401 | — | clean |
| `public_material_option_surcharges` | view | 401 | — | clean |
| `public_material_variants` | view | 401 | — | clean |
| `public_price_tiers` | view | 401 | — | clean |
| `dashboard_latest_events` | view | 401 | — | clean |
| `material_price_tier_counts` | view | 401 | — | clean |

**Counts:** P1 = 0, P2 = 0, clean = 33.

## RPC probe — `public_get_customer_proof` (migration 000162)

**Status:** HTTP 200 — proof object present: true

| Field | Count |
|---|---|
| versions | 1 |
| material_variants | 3 |
| price_tiers | 120 |

The RPC is the only customer-side read of the proof graph after migration 000162. Non-zero counts here are the positive signal that legitimate customer URLs still resolve.

## P1 — leaks PII or proof-graph data

_(none)_

## P2 — exposed but lower-stakes (catalogue / settings)

_(none)_

## clean — locked down

### `proofs` (table)

**Verdict:** clean — HTTP 401 — permission denied

**Unfiltered SELECT \*:** HTTP 401, — row(s) returned

Error body:

```json
{
  "code": "42501",
  "details": null,
  "hint": null,
  "message": "permission denied for table proofs"
}
```

**Filtered probe (`id=eq.f47c88c1-98a9-4719-94c6-f4e3cea8a175`):** HTTP 401, — row(s)

### `proof_versions` (table)

**Verdict:** clean — HTTP 401 — permission denied

**Unfiltered SELECT \*:** HTTP 401, — row(s) returned

Error body:

```json
{
  "code": "42501",
  "details": null,
  "hint": null,
  "message": "permission denied for table proof_versions"
}
```

**Filtered probe (`proof_id=eq.f47c88c1-98a9-4719-94c6-f4e3cea8a175`):** HTTP 401, — row(s)

### `proof_version_images` (table)

**Verdict:** clean — HTTP 401 — permission denied

**Unfiltered SELECT \*:** HTTP 401, — row(s) returned

Error body:

```json
{
  "code": "42501",
  "details": null,
  "hint": null,
  "message": "permission denied for table proof_version_images"
}
```

**Filtered probe (`proof_version_id=eq.f88a97dd-f5d8-4251-bd09-7a41392d2cb0`):** HTTP 401, — row(s)

### `proof_round_variants` (table)

**Verdict:** clean — 200 OK, 0 rows — RLS filtered all rows

**Unfiltered SELECT \*:** HTTP 200, 0 row(s) returned

Body: `[]`

### `proof_name_approvals` (table)

**Verdict:** clean — 200 OK, 0 rows — RLS filtered all rows

**Unfiltered SELECT \*:** HTTP 200, 0 row(s) returned

Body: `[]`

**Filtered probe (`proof_version_id=eq.f88a97dd-f5d8-4251-bd09-7a41392d2cb0`):** HTTP 200, 0 row(s)

> Empty body — either the QA fixture row is missing, or the filtered customer-page read also returns nothing. Compare with the unfiltered result above to disambiguate.

### `proof_pins` (table)

**Verdict:** clean — 200 OK, 0 rows — RLS filtered all rows

**Unfiltered SELECT \*:** HTTP 200, 0 row(s) returned

Body: `[]`

**Filtered probe (`proof_id=eq.f47c88c1-98a9-4719-94c6-f4e3cea8a175`):** HTTP 200, 0 row(s)

> Empty body — either the QA fixture row is missing, or the filtered customer-page read also returns nothing. Compare with the unfiltered result above to disambiguate.

### `proof_events` (table)

**Verdict:** clean — 200 OK, 0 rows — RLS filtered all rows

**Unfiltered SELECT \*:** HTTP 200, 0 row(s) returned

Body: `[]`

**Filtered probe (`proof_id=eq.f47c88c1-98a9-4719-94c6-f4e3cea8a175`):** HTTP 400, — row(s)

### `proof_version_views` (table)

**Verdict:** clean — 200 OK, 0 rows — RLS filtered all rows

**Unfiltered SELECT \*:** HTTP 200, 0 row(s) returned

Body: `[]`

**Filtered probe (`proof_version_id=eq.f88a97dd-f5d8-4251-bd09-7a41392d2cb0`):** HTTP 200, 0 row(s)

> Empty body — either the QA fixture row is missing, or the filtered customer-page read also returns nothing. Compare with the unfiltered result above to disambiguate.

### `contacts` (table)

**Verdict:** clean — 200 OK, 0 rows — RLS filtered all rows

**Unfiltered SELECT \*:** HTTP 200, 0 row(s) returned

Body: `[]`

### `companies` (table)

**Verdict:** clean — 200 OK, 0 rows — RLS filtered all rows

**Unfiltered SELECT \*:** HTTP 200, 0 row(s) returned

Body: `[]`

### `materials` (table)

**Verdict:** clean — HTTP 401 — permission denied

**Unfiltered SELECT \*:** HTTP 401, — row(s) returned

Error body:

```json
{
  "code": "42501",
  "details": null,
  "hint": null,
  "message": "permission denied for table materials"
}
```

### `material_variants` (table)

**Verdict:** clean — HTTP 401 — permission denied

**Unfiltered SELECT \*:** HTTP 401, — row(s) returned

Error body:

```json
{
  "code": "42501",
  "details": null,
  "hint": null,
  "message": "permission denied for table material_variants"
}
```

### `material_options` (table)

**Verdict:** clean — HTTP 401 — permission denied

**Unfiltered SELECT \*:** HTTP 401, — row(s) returned

Error body:

```json
{
  "code": "42501",
  "details": null,
  "hint": null,
  "message": "permission denied for table material_options"
}
```

### `material_option_surcharges` (table)

**Verdict:** clean — HTTP 401 — permission denied

**Unfiltered SELECT \*:** HTTP 401, — row(s) returned

Error body:

```json
{
  "code": "42501",
  "details": null,
  "hint": null,
  "message": "permission denied for table material_option_surcharges"
}
```

### `price_tiers` (table)

**Verdict:** clean — HTTP 401 — permission denied

**Unfiltered SELECT \*:** HTTP 401, — row(s) returned

Error body:

```json
{
  "code": "42501",
  "details": null,
  "hint": null,
  "message": "permission denied for table price_tiers"
}
```

### `add_ons` (table)

**Verdict:** clean — 200 OK, 0 rows — RLS filtered all rows

**Unfiltered SELECT \*:** HTTP 200, 0 row(s) returned

Body: `[]`

### `add_on_prices` (table)

**Verdict:** clean — 200 OK, 0 rows — RLS filtered all rows

**Unfiltered SELECT \*:** HTTP 200, 0 row(s) returned

Body: `[]`

### `letterpress_core_colours` (table)

**Verdict:** clean — HTTP 401 — permission denied

**Unfiltered SELECT \*:** HTTP 401, — row(s) returned

Error body:

```json
{
  "code": "42501",
  "details": null,
  "hint": null,
  "message": "permission denied for table letterpress_core_colours"
}
```

### `site_settings` (table)

**Verdict:** clean — HTTP 401 — permission denied

**Unfiltered SELECT \*:** HTTP 401, — row(s) returned

Error body:

```json
{
  "code": "42501",
  "details": null,
  "hint": null,
  "message": "permission denied for table site_settings"
}
```

### `reply_templates` (table)

**Verdict:** clean — 200 OK, 0 rows — RLS filtered all rows

**Unfiltered SELECT \*:** HTTP 200, 0 row(s) returned

Body: `[]`

### `audit_log` (table)

**Verdict:** clean — 200 OK, 0 rows — RLS filtered all rows

**Unfiltered SELECT \*:** HTTP 200, 0 row(s) returned

Body: `[]`

### `profiles` (table)

**Verdict:** clean — 200 OK, 0 rows — RLS filtered all rows

**Unfiltered SELECT \*:** HTTP 200, 0 row(s) returned

Body: `[]`

### `public_proofs` (view)

**Verdict:** clean — HTTP 401 — permission denied

**Unfiltered SELECT \*:** HTTP 401, — row(s) returned

Error body:

```json
{
  "code": "42501",
  "details": null,
  "hint": null,
  "message": "permission denied for view public_proofs"
}
```

**Filtered probe (`id=eq.f47c88c1-98a9-4719-94c6-f4e3cea8a175`):** HTTP 401, — row(s)

### `public_proof_versions` (view)

**Verdict:** clean — HTTP 401 — permission denied

**Unfiltered SELECT \*:** HTTP 401, — row(s) returned

Error body:

```json
{
  "code": "42501",
  "details": null,
  "hint": null,
  "message": "permission denied for view public_proof_versions"
}
```

**Filtered probe (`proof_id=eq.f47c88c1-98a9-4719-94c6-f4e3cea8a175`):** HTTP 401, — row(s)

### `public_proof_version_images` (view)

**Verdict:** clean — HTTP 401 — permission denied

**Unfiltered SELECT \*:** HTTP 401, — row(s) returned

Error body:

```json
{
  "code": "42501",
  "details": null,
  "hint": null,
  "message": "permission denied for view public_proof_version_images"
}
```

**Filtered probe (`proof_version_id=eq.f88a97dd-f5d8-4251-bd09-7a41392d2cb0`):** HTTP 401, — row(s)

### `public_dashboard_projects` (view)

**Verdict:** clean — HTTP 401 — permission denied

**Unfiltered SELECT \*:** HTTP 401, — row(s) returned

Error body:

```json
{
  "code": "42501",
  "details": null,
  "hint": null,
  "message": "permission denied for view public_dashboard_projects"
}
```

**Filtered probe (`proof_id=eq.f47c88c1-98a9-4719-94c6-f4e3cea8a175`):** HTTP 401, — row(s)

### `public_site_settings` (view)

**Verdict:** clean — HTTP 401 — permission denied

**Unfiltered SELECT \*:** HTTP 401, — row(s) returned

Error body:

```json
{
  "code": "42501",
  "details": null,
  "hint": null,
  "message": "permission denied for view public_site_settings"
}
```

### `public_material_options` (view)

**Verdict:** clean — HTTP 401 — permission denied

**Unfiltered SELECT \*:** HTTP 401, — row(s) returned

Error body:

```json
{
  "code": "42501",
  "details": null,
  "hint": null,
  "message": "permission denied for view public_material_options"
}
```

### `public_material_option_surcharges` (view)

**Verdict:** clean — HTTP 401 — permission denied

**Unfiltered SELECT \*:** HTTP 401, — row(s) returned

Error body:

```json
{
  "code": "42501",
  "details": null,
  "hint": null,
  "message": "permission denied for view public_material_option_surcharges"
}
```

### `public_material_variants` (view)

**Verdict:** clean — HTTP 401 — permission denied

**Unfiltered SELECT \*:** HTTP 401, — row(s) returned

Error body:

```json
{
  "code": "42501",
  "details": null,
  "hint": null,
  "message": "permission denied for view public_material_variants"
}
```

### `public_price_tiers` (view)

**Verdict:** clean — HTTP 401 — permission denied

**Unfiltered SELECT \*:** HTTP 401, — row(s) returned

Error body:

```json
{
  "code": "42501",
  "details": null,
  "hint": null,
  "message": "permission denied for view public_price_tiers"
}
```

### `dashboard_latest_events` (view)

**Verdict:** clean — HTTP 401 — permission denied

**Unfiltered SELECT \*:** HTTP 401, — row(s) returned

Error body:

```json
{
  "code": "42501",
  "details": null,
  "hint": null,
  "message": "permission denied for view dashboard_latest_events"
}
```

**Filtered probe (`proof_id=eq.f47c88c1-98a9-4719-94c6-f4e3cea8a175`):** HTTP 401, — row(s)

### `material_price_tier_counts` (view)

**Verdict:** clean — HTTP 401 — permission denied

**Unfiltered SELECT \*:** HTTP 401, — row(s) returned

Error body:

```json
{
  "code": "42501",
  "details": null,
  "hint": null,
  "message": "permission denied for view material_price_tier_counts"
}
```

## Observed patterns

- 9 `public_*` view(s) probed → 0 leak, 9 clean.
- 22 raw table(s) probed → 0 leak, 22 clean.
- Status-code distribution across all probed surfaces:
  - HTTP 200: 12 surface(s) — `proof_round_variants`, `proof_name_approvals`, `proof_pins`, `proof_events`, `proof_version_views`, `contacts`, `companies`, `add_ons`, `add_on_prices`, `reply_templates`, `audit_log`, `profiles`
  - HTTP 401: 21 surface(s) — `proofs`, `proof_versions`, `proof_version_images`, `materials`, `material_variants`, `material_options`, `material_option_surcharges`, `price_tiers`, `letterpress_core_colours`, `site_settings`, `public_proofs`, `public_proof_versions`, `public_proof_version_images`, `public_dashboard_projects`, `public_site_settings`, `public_material_options`, `public_material_option_surcharges`, `public_material_variants`, `public_price_tiers`, `dashboard_latest_events`, `material_price_tier_counts`
- Filtered probes attempted on 12 surface(s); 3 returned empty (fixture missing, RLS filter, or table genuinely has no matching rows).

Recon only — no remediation in this report.
