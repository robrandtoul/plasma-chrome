# CLAUDE.md

Proof viewer web app for Plasma Design. This file gives Claude Code the context needed to work in this repo without re-deriving decisions that are already made.

Global business rules (VAT, Help Scout, Xero, British English) live in Rob's global Claude instructions and apply here. This file covers the repo-specific bits.

## Split-name tooling surcharges (authoritative)

This repo is the single source of truth for split-name tooling surcharges. When a card order is split between two or more names, charge the listed price for the total quantity, then add the per-extra-name tooling surcharge below for each name beyond the first. The figures live on the `materials` table (`split_name_surcharge_gbp` / `_eur` / `_usd`), seeded in `seed.sql` and reconciled by migration `000146`; the table below mirrors that live state. GBP figures are VAT-inclusive; EUR and USD are VAT-free (those currencies are used only outside the UK).

| Material family | GBP | EUR | USD |
| --- | --- | --- | --- |
| Metal (steel, gold, copper, gun metal, matte black, matte white, mini steel, titanium) | £39 | €39 | $49 |
| Carbon fibre, Carbon fibre CNC | £39 | €39 | $49 |
| Standard paper | £39 | €39 | $49 |
| Translucent / tinted / satin plastic | £25 | €39 | $39 |
| Letterpress, Letterpress with gilding | £25 | €39 | $39 |
| Acrylic (Perspex) | £25 | €39 | $39 |
| Full colour plastic | £15 | €25 | $25 |
| Wood | — (no split-name surcharge) | — | — |

Notes: acrylic, standard paper, carbon fibre and carbon fibre CNC were enabled in migration `000146` (they were null at seed); wood is deliberately null. Letterpress is two distinct material codes (`paper_letterpress`, `paper_letterpress_gilded`) since `000098`, both carrying the same surcharge. If any value here diverges from the live `materials` table, the database wins and this table should be corrected. The global Claude instructions point here rather than re-listing these figures, so there is only one place to keep current.

## Stack

- Supabase (Postgres + auth + storage) for the backend
- React 19 + Vite frontend, TypeScript, Tailwind v4, React Router v7
- Frontend deployed to Netlify
- Custom subdomain served via Hostinger DNS
- Migrations live under `supabase/migrations/`, numbered `NNNNNN_description.sql`
- Seed data in `supabase/seed.sql`
- Rob is a non-coder — step-by-step explanations, short commits, no surprises

## Database state

> ⚠️ **Drift warning.** The migration summary in this section is hand-curated and has repeatedly lagged reality — the QR codes feature in PR #55 (000168/000169), W20-005's 000170, and the snooze-attribution + profile-self-edit follow-ups (000167/000171) all shipped without a doc update before this refresh. Treat this section as orienting context only — for what is actually in source and on the live DB, run `pnpm db:status` (lists the linked project's migration history) and `ls supabase/migrations/0001*` (shows everything in source). Picking a new migration number from this doc has caused multiple collisions; don't.

Migrations 000001–000251 have been applied (note two files share the `000217` prefix — `000217_ai_drafts_cache_usage` and `000217_needs_attention_approved_earlier_version` — a real collision that landed on live, so don't treat "highest number" as "next free number"; `ls supabase/migrations/0002*` and `pnpm db:status`). Migration 000005 originally installed a single flat `pricing_tables` table that didn't match reality; it was replaced by `000009_rebuild_pricing.sql`, which installs the current five-table pricing model. The `finishes` concept introduced in 000020 was later generalised to `material_options` in 000025. A second batch (000128–000150) shipped the per-recipient approval flow, letterpress core/front/back colours, variant rounds with the per-direction-pricing sub-mode, and several pricing reconciles. 000151 closed the previously-pending follow-up by adding the missing REVOKE on the 000148 view. A third batch (000152–000155) shipped the redesigned designer dashboard: tile-counts + needs-attention scaffolding, a designer-profile cleanup, the configurable Needs-attention rules engine, and the `proof_pins` table for Pinned and Team sections. A fourth batch (000156–000158) reconciled live `profiles.helpscout_user_id` values into source, seeded the three customer-confirmation reply templates the proof-action edge function resolves by code, and added the `reopen_proof` RPC that flips status and clears stale per-recipient approvals atomically. The follow-up batch 000159–000162 tightened `proof_pins` audit attribution, redefined `business_days_between` as inclusive at end, added the `awaiting_customer` column to `public_dashboard_projects` for tile/list parity, and closed the anon enumeration leak by routing customer reads through the SECURITY DEFINER `public_get_customer_proof` RPC plus `REVOKE FROM anon` on every `public_*` view and underlying table (see memory:proof_viewer_anon_rpc_pattern.md). A fifth batch (000163–000164) shipped the snooze feature: the `proof_attention_snoozes` table and the updated rules engine + dashboard view that exclude snoozed (proof_id, rule_code) pairs from needs-attention results. Two further migrations (000165–000166) shipped self-service profile editing: an owner-update RLS policy with role-immutability `WITH CHECK`, plus `profiles.avatar_url`, the public `avatars` storage bucket, and `designer_avatar_url` on `public_dashboard_projects`. A follow-up sweep (000167–000171) tightened `proof_attention_snoozes` audit attribution (analogous to 000159 on `proof_pins`), shipped QR-code upload metadata on `proof_version_images` plus QR-confirmation gating on per-recipient approvals, dropped the dead `awaiting_customer` column from `public_dashboard_projects` and `dashboard_tile_counts()` after the dashboard moved that tile to client-side computation (superseding 000161), and hardened the 000165 self-edit policy to also lock `helpscout_user_id` and `deactivated_at` against owner mutation. A final batch (000172–000175) shipped the personalisation add-on for membership-style cards (per-card rate + minimum charge seeded per currency; `has_personalisation` on `proof_versions`; gated off variant rounds via a DB CHECK; exposed through `public_get_customer_proof`), patched the anon leak on `public_proof_version_images` that 000168 introduced, and added admin-managed lead-time windows (`lead_time_min_days`, `lead_time_max_days`, `lead_time_updated_at`) to `materials` for the quote compiler. 000176 then restored explicit per-table CRUD grants to `authenticated` ahead of the 30 October 2026 Supabase enforcement that removes the implicit schema-wide grant — 20 frontend-written tables get full CRUD, 6 service-role tables get SELECT only, plus `ALTER DEFAULT PRIVILEGES` so future tables inherit CRUD automatically; deliberately no grants to `anon` (the customer-page reads stay routed through the 000162 SECURITY DEFINER RPC). 000177 exposed `materials.code` as `material_code` on `public_proof_versions` and `public_get_customer_proof` so the customer page can gate metal-specific contextual panels without a second lookup. The next batch (000178–000179) shipped the Quote compiler's shipping card: 000178 added `material_variants.weight_grams`, the FedEx box-weight + adjustment settings, and a service-role-only `fedex_rate_cache` table (the canonical example of the "future SELECT-only tables need an explicit REVOKE" footgun, since 000176's `ALTER DEFAULT PRIVILEGES` would have granted authenticated CRUD); 000179 added the two GBP flat domestic-UK rate settings (mainland + Northern Ireland) so DPD pricing is admin-editable without a redeploy. 000180 exposed `public_get_lead_times()` as a SECURITY DEFINER anon RPC for the marketing-site `/turnaround-times` page, scoped to the six lead-time columns added in 000175. Security-advisor cleanup followed: 000181 flipped `security_invoker = on` on all 11 public-schema views via `ALTER VIEW ... SET` (option-only change, so grants stay intact — the dangerous anon exposure was already closed in 000162), and 000182 pinned `search_path = public` on the 27 plain SECURITY INVOKER functions that didn't have it (looped over `pg_proc`, skipping extension-owned functions) and revoked EXECUTE from anon + authenticated on 11 internal-only SECURITY DEFINER trigger/plumbing functions that nothing calls directly. 000183 filled the only USD price-tier gap surfaced by the 2026-05-15 CommonNinja vs Supabase parity audit: 12 `paper_standard` USD rows (2000/3000/5000/10000 × standard/uv_spot/foiling) that GBP and EUR already carried, totals taken from the live US price list, `on conflict do nothing` for idempotency. 000184 added `public_get_price_list(p_currency)` as the anon read path for the new marketing-site price-list script — a SECURITY DEFINER RPC returning published-materials → variants → tiers + per-material option_surcharges for one currency, with no customer/proof/contact/audit data; same house pattern as 000180, raw `price_tiers` stays anon-revoked. The most recent run (000210–000227) is three concurrent workstreams: the **proof-type wizard / Set-collection** shape model (000210–000212), **follow-up automation** (the nudge ledger + sender, 000214/000220–000224), and the **AI draft pipeline** going from backtest to live integration (000216/000217/000219/000225–000227), plus a clutch of needs-attention rule additions (000213/000217/000218/000221). All of these target the merged stock-control project's `proofs` schema and were applied by Rob via the dashboard SQL editor, never via CLI push — see the merged-project workflow below. A further batch (000228–000251) shipped the **Ordering & checkout** system — pay page, embedded Stripe Elements, Xero invoicing, US tariff & customs handling (000249), unpaid-order reminders (000238) — plus the **approved_no_order** needs-attention rule (000250). All merged to `main` via PR #317 on 2026-06-22; see the Ordering & checkout section. The Stripe-clearing-account migration was renumbered `000242`→`000251` at that merge to clear a collision with main's `000242_latest_event_uses_latest_view_of_day`.

Migration summary (post-000009):

- **000010/000011** — app tables (proofs, contacts, companies, proof_versions, proof_version_images), multi-variant proofs
- **000012–000014** — featured quantities on materials, multi-image versions
- **000015** — companies + contacts schema
- **000016** — global + per-material disclaimers (site_settings table, materials.disclaimer)
- **000017** — proof status: `in_progress` | `approved`, `approved_at`
- **000018** — dormant status + `last_activity_at` + `bump_proof_activity` trigger (only flips dormant→in_progress, other statuses pass through)
- **000019** — pg_cron daily job `mark_dormant_proofs()`, originally a 30-day inactivity threshold. **Superseded by 000080**, which tore down this job + function and replaced them with `auto_dormant_stale_proofs()` at a **90-day** threshold (plus audit on both the forward flip and the activity-driven wake). **Then 000209** moved that threshold out of the function body into the admin-editable `site_settings.dormancy_threshold_days` column (default 90). Don't trust "30 days" anywhere for dormancy — the live cutoff is whatever that column says.
- **000020** — `finishes` + `finish_surcharges` tables (later renamed), proof_versions.finishes, proof_version_images.finish, seeds Steel/Gold with Natural/Brushed/Mirror
- **000021** — `proof_version_images.original_filename`
- **000022** — `abandoned` status + `abandoned_at`, view updates
- **000023** — `proof_versions.custom_quote` flag
- **000024** — `materials.requires_ink_names` flag (plastic_translucent/tinted/satin + paper_letterpress)
- **000025** — rename finishes→material_options, finish_surcharges→material_option_surcharges, columns renamed to match; adds `materials.option_label`; seeds five wood species (Black Walnut base, American Cherry, Finnish Birch, Canadian Maple, Bamboo — no surcharges)
- **000026** — carbon fibre "Cutting" option (Without cutting = base, Optional CNC cutting = 39-tier × 3-currency surcharge). Later superseded by 000098, which promoted CNC cutting to its own material `carbon_fibre_cnc` rather than a per-material option dimension; the `Cutting` option no longer exists on live.
- **000098** — split CNC cutting and gilded letterpress into distinct materials. Inserts `carbon_fibre_cnc` and `paper_letterpress_gilded` as standalone materials with their own variants, price tiers, and split-name surcharges; tears down 000026's per-material `Cutting` dimension on `carbon_fibre` (nulls `option_label`, deletes the two `material_options` rows + 117 cascade-dropped surcharges). Rationale: optional finishing that materially changes the product is modelled as a separate material rather than an option, so the artwork the customer approves matches the product they order.
- **000126** — `maybe_finalize_proof_status()` trigger on `proof_name_approvals`. After a direct customer approval (state='approved', carried_from_version_id IS NULL), if the proof's current version has every required slot approved (names[] + '__shared__' iff the version has any associated_name=null images), the proof flips to `status='approved'` + `approved_at=now()`. Skips carry-forward writes; never overrides `abandoned` or already-`approved`. Designer's "Mark as approved" button is now belt-and-braces, not the only path.
- **000127** — `dashboard_latest_events` view extended via UNION ALL with non-bot rows from `proof_version_views`, deduped to first view per (proof_version_id, day). Synthesises `event_type='view'` rows with `actor_name = contact.full_name` (fallback 'Customer') so the sidebar reads as a unified customer-activity timeline. The `proof_events.event_type` CHECK constraint is unchanged — 'view' only ever appears in the view's output, never as a stored row. Dashboard cap bumped from 10 → 20 to absorb the extra volume.
- **000128–000137** — per-recipient approval flow polish, audit-actor backfills, key-features model upgrade (string[] → KeyFeature[]), letterpress core-colours catalogue + per-version picker (000133), Colorplan card-type metadata.
- **000133/000135** — letterpress edge construction. 000133 adds the `letterpress_core_colours` admin-managed palette + per-version `core_colour_id`; 000135 adds the `front_colour_id` / `back_colour_id` siblings so the customer page can render a layered Colorplan cross-section. All three pull via left joins so non-letterpress / gilded versions return null across the board.
- **000138/000139/000140/000141/000143** — variant rounds. 000138 introduces `proof_round_variants` (write-once `code`, `display_name`, `sort_order`) and `proof_version_images.round_variant_id`; 000139 surfaces the new shape on the public_* views; 000140 widens RLS for authenticated CRUD on `proof_round_variants`; 000141 hardens `maybe_finalize_proof_status` against variant-round state; 000143 backfills `side='front'` on legacy variant images.
- **000142** — `proof_versions.is_mixed_materials` boolean for variant rounds where each direction is a distinct material; loosens `material_id` / `currency` NOT NULL with a trigger asserting the equivalent constraints for non-variant-round rows.
- **000144** — rename `is_mixed_materials` → `is_per_direction_pricing` (column + view + trigger). Per-direction pricing also fires when directions differ in thickness / tier within one material family, so the by-materials framing was misleading. Customer page hides pricing card, Specification, and About-material when this is true.
- **000145** — backfill the qty 25 row of the metal Mirror/Brushed surcharge schedule across all three currencies. The 39-tier schedule shipped in 000020 was missing the qty 25 row that the rest of the metal grids have always had.
- **000146** — drift reconcile. Reverts a single `metal_gun_metal | 800um | GBP | 750` tier back to the seed canonical (1799.00) and captures four split-name surcharge enablements (acrylic, carbon_fibre, carbon_fibre_cnc, paper_standard) that were applied directly to live and never made it into source. Wood remains null. Update Rob's global `~/.claude/CLAUDE.md` pricing rules to match if not already done.
- **000147** — drop the orphan `add_on_prices` rows for `metal_finish_upgrade`. The admin Mirror/Brushed editor used to read/write `add_on_prices`; it now points at `material_option_surcharges` (b4d7ecd / 3935b72). The catalogue `add_ons.metal_finish_upgrade` row stays as the route key.
- **000148** — `material_price_tier_counts` aggregate view. Replaces the admin pricing index's "fetch every price_tiers row and roll up client-side" path that silently truncated to supabase-js's 1000-row cap. NOTE: shipped without the explicit `revoke from anon, public` defence pattern; 000151 adds it.
- **000149/000150** — strip the auto-appended sign-off from seeded reply templates. 000149 used a strict equality check that matched zero live rows (apostrophe drift); 000150 ships the loose-LIKE follow-up that actually rewrites the live bodies. Both are kept in source for fresh-replay correctness.
- **000152** — Dashboard Phase 1 schema. Adds `public_dashboard_projects` view (one-row-per-proof shape used by the redesigned dashboard), `dashboard_tile_counts()` SQL function for the four stat-tile counts in a single round-trip, and the first cut of `proofs_needing_attention()` returning `uuid[]` from two hardcoded rules (current version untouched for 3 working days; `last_activity_at` in the pre-dormant warning band — at the time a hardcoded 25–30 days; the band now tracks `site_settings.dormancy_threshold_days` minus the rule's own threshold, see 000209). Also adds `designer_colour` / `designer_initials` columns on `profiles` with a backfill that pins Rob to `'blue'` and distributes other designers across `'teal'` / `'coral'` / `'purple'` via a stable hash of the profile id.
- **000153** — designer profile cleanup. Re-pins Rob's `'blue'` from the `rob.randtoul@gmail.com` account to `rob@plasmadesign.co.uk` (000152's email-based pin landed on the wrong account), sets Chris Jackson's `full_name` + `'teal'` colour, and moves Jack Johnson off `'teal'` to `'purple'` so two designers don't share a hue. Deletes the gmail-Rob and `test-designer@example.invalid` auth users; the 392 audit_log rows referencing gmail-Rob's `actor_id` survive intact via the existing `ON DELETE SET NULL` FK and the denormalised `actor_email` / `actor_label` columns that were written for exactly this case.
- **000154** — Needs-attention rules engine (Phase 2a). Adds `site_settings.needs_attention_rules` JSONB (six rules: `request_changes_no_version`, `helpscout_follow_up_tag`, `sent_never_viewed`, `viewed_not_actioned`, `approaching_dormant`, `stuck_in_progress`) and a `business_days_between(date, date)` helper. Replaces `proofs_needing_attention()` with a `(proof_id, rule_code, rule_meta)`-returning version that reads thresholds + priorities from settings and emits the highest-priority rule per proof. Extends `public_dashboard_projects` with `rule_code` / `rule_meta` columns so dashboard reason chips can render without a second query. Also adds `proofs.helpscout_tags text[]` as a placeholder for Phase 2b — the `helpscout_follow_up_tag` rule reads from it, but the array stays empty until the HS → DB tag sync ships.
- **000155** — `proof_pins` table backing the dashboard's Pinned (mine) and Team sections. Single table with `scope IN ('mine', 'team')`; partial unique indexes per scope (one mine-pin per proof per user, one team-pin per proof). RLS lets every authenticated designer read all pins, write/remove their own mine-pins, and write/remove team-pins. Deliberately not joined into `public_dashboard_projects` — pin state is fetched as a small standalone query and merged client-side, so pin churn doesn't force a full dashboard refetch.
- **000156** — capture live `profiles.helpscout_user_id` values into source so a fresh-replay no longer falls back to `HELPSCOUT_DEFAULT_USER_ID`. Uses `IS DISTINCT FROM <expected>` (the integer-correct equivalent of the loose-LIKE pattern from 000150) so re-running on a DB that already matches is a no-op. Pairs with the proof-action edge function's confirmation-reply work in 000157.
- **000157** — seed three `reply_templates` rows that the proof-action edge function resolves by code: `proof_approval_confirmation`, `proof_change_request_confirmation`, `proof_variant_selection_confirmation`. Help Scout never emails customer-thread messages back to the customer, so the edge function layers a staff reply on top of the existing customer-thread post and Help Scout emails THAT out. Default bodies mirror `src/lib/replyTemplates.ts` `DEFAULT_BODIES` so the admin editor's "Reset to default" button works. `ON CONFLICT (id) DO NOTHING` for safe replay. **June 2026 follow-up (no migration):** the proof-action edge function now *hides* that staff reply in Help Scout once it's sent — `hideThread()` in `_shared/helpscout.ts` does a `PATCH /conversations/{id}/threads/{threadId}` with `op replace /hidden true` (valid only for non-draft customer/reply threads). The customer-thread post stays expanded; the staff reply collapses so it doesn't clutter the designer's view. The hide is deferred to a background task (`deferHideThread` via `EdgeRuntime.waitUntil`, `HIDE_DELAY_MS` = 5s) so it adds no latency to the customer's request and runs *after* Help Scout has emailed the reply — hiding never unsends the email (HS sends it at reply-creation time). Best-effort: a hide miss is cosmetic only and logged, never failing the request.
- **000158** — `reopen_proof(p_proof_id uuid)` RPC. Atomic UPDATE of `proofs.status` to `in_progress` (also clearing `approved_at` / `abandoned_at`) plus DELETE of every `proof_name_approvals` row across the proof's versions. Fixes the bug where `handleReopen`'s client-side UPDATE left v1's `state='approved'` rows in place, so the v2 carry-forward block then INSERTed them onto v2 with `carried_from_version_id=v1` and the customer page treated v2 as pre-approved. Returns `integer` (count of cleared approvals) so the audit row carries a structured signal of how many approvals were wiped. SECURITY INVOKER — caller's RLS context is sufficient.
- **000163** — `proof_attention_snoozes` table. Columns: `id`, `proof_id` (FK proofs), `rule_code text`, `snoozed_by` (FK profiles — not auth.users, so initials/colour are joinable), `snoozed_at`, `snoozed_until`, `note text`. Unique index on `(proof_id, rule_code)` for upsert pattern. RLS: all authenticated users can read and manage snoozes.
- **000164** — wire snoozes into the rules engine and dashboard view. `proofs_needing_attention()` gains a `WHERE NOT EXISTS` guard that excludes any `(proof_id, rule_code)` pair with an active snooze — `dashboard_tile_counts()` picks this up automatically. `public_dashboard_projects` is dropped and recreated to add six snooze columns via a lateral join on `proof_attention_snoozes`: `snooze_rule_code`, `snoozed_until`, `snooze_note`, `snoozed_by_name`, `snoozed_by_initials`, `snoozed_by_colour` (longest-remaining active snooze per proof).
- **000159** — drop and recreate the `proof_pins` insert policies with a `WITH CHECK` that asserts `pinned_by = auth.uid()`, closing the audit-attribution spoofing gap from 000155 (PV-2026W19-011). DELETE stays open so any designer can clear a colleague's team pin.
- **000160** — redefine `business_days_between(start_date, end_date)` so the count is inclusive at end (`>= threshold_days` now fires on the human-counted day rather than the day after). Needs-attention thresholds previously fired one business day later than the configured value implied (PV-2026W19-012).
- **000161** — add `awaiting_customer boolean` to `public_dashboard_projects` mirroring `dashboard_tile_counts.awaiting_customer` so the tile count and the click-through list use the same predicate (PV-2026W19-015). View dropped + recreated. **Superseded by 000170**, which drops the column after the dashboard moved that tile to client-side computation.
- **000162** — anon enumeration fix. Adds the `public_get_customer_proof(p_id uuid)` SECURITY DEFINER RPC (returns the full customer-page payload for one proof) and `REVOKE FROM anon, public` on every `public_*` view and underlying table that the customer page used to read directly. Anon reads now flow exclusively through the RPC, which scopes the response to one proof id and skips the contact/company enumeration risk. See memory:proof_viewer_anon_rpc_pattern.md.
- **000165** — restore owner-update on `profiles` (revoked in 000036). Policy ships with a `WITH CHECK` that asserts the incoming `role` matches the currently-stored value via a self-subquery, so a direct API call trying to flip role from `'designer'` to `'admin'` fails the check at the database layer. Designers can now edit `full_name`, `designer_initials`, and `designer_colour` from the self-service profile modal. **Extended by 000171** to also lock `helpscout_user_id` and `deactivated_at` via the same `is not distinct from` self-subquery pattern.
- **000166** — self-service profile avatar upload. Three changes: (1) `profiles.avatar_url text` nullable; (2) public `avatars` storage bucket with a 2 MB cap and JPEG/PNG/WebP mime allow-list, paths keyed on `{user_id}/avatar`, RLS gates insert/update/delete on the first path segment matching `auth.uid()`; (3) `public_dashboard_projects` dropped and recreated to expose `pr.avatar_url` as `designer_avatar_url` immediately after `designer_colour`.
- **000167** — tighten `proof_attention_snoozes` RLS to match the 000159 pattern on `proof_pins`. Splits the open `for all` policy from 000163 into separate INSERT / UPDATE / DELETE policies; INSERT and UPDATE now `WITH CHECK (snoozed_by = auth.uid() or snoozed_by is null)` so a designer can't spoof another's attribution via a direct REST upsert. SELECT and DELETE stay open (every designer can see and clear any snooze). The frontend's `handleSnooze` already writes `snoozed_by = auth.uid()`, so no client change needed. IS NULL branch is defensive against future service-role backfills (column is FK `ON DELETE SET NULL`).
- **000168** — QR-code fields on `proof_version_images`: `is_qr_code boolean default false`, `qr_decoded_data text`, `qr_kind text` (one of `vcard | url | wifi | mecard | email | phone | sms | text`). Adds a CHECK constraint that QR fields are populated iff `is_qr_code = true` and `qr_kind` is in the eight-value enum, plus a partial index on `proof_version_id` where `is_qr_code = true` for the customer-page QR panel filter. `public_proof_version_images` dropped + recreated to expose the three new columns; anon stays revoked per 000162. No backfill — every existing row trivially satisfies the CHECK at the column defaults.
- **000169** — QR-confirmation gating on per-recipient approvals. Adds `proof_name_approvals.qr_confirmed_at timestamptz` (nullable) and rewrites `_finalize_proof_if_complete(p_proof_id)` so that, when the current version has any QR row, a slot only counts as approved if either the slot's coordinates resolve to zero QRs or `qr_confirmed_at is not null` on the approval row. Slot coordinates for the QR predicate: named slot = QRs with `associated_name = <name>` OR null (a shared QR is verified by every named recipient); `__shared__` (names-empty path) = every QR on the version. Variant-round versions bail at the top guard from 000141 so this rule never fires for them. The `v_has_shared` "Shared artwork section exists" check is narrowed to `is_qr_code = false` so a shared QR row doesn't on its own imply a Shared artwork slot. Carry-forward correctness handled in `NewVersionPage.tsx` (copies `qr_confirmed_at` when image-set identity is unchanged).
- **000170** — drop `awaiting_customer` from `public_dashboard_projects` (added in 000161) and from `dashboard_tile_counts()` (added in 000152, re-emitted in 000154). The dashboard moved both the tile count and its click-through filter to client-side computation against the projects array in PV-2026W20-014, so the SQL paths were dead and risked re-introducing the count/filter drift the original migrations existed to prevent. View drop+recreate; function drop+recreate (return shape change). Header comment in the file mislabels the migration as 000169 — cosmetic, the filename and final state are correct.
- **000171** — extend the 000165 self-edit policy to also lock `helpscout_user_id` (would otherwise let a designer impersonate another's Help Scout authorial identity on staff replies) and `deactivated_at` (would let a deactivated designer un-deactivate themselves while their session token was still valid). Both columns join `role` in the WITH CHECK using `is not distinct from (select … from profiles where id = auth.uid())` — `is not distinct from` rather than `=` because both columns are nullable and SQL's `null = null` is NULL, which would fail the check on legitimately-null rows.
- **000172** — personalisation add-on. Six changes: `materials.supports_personalisation boolean` seeded true on all `metal_*` codes plus `plastic_full_colour`, `wood`, and `acrylic`; new `personalisation_pricing` table (per currency: GBP £0.20/card + £50 min, USD $0.25/$50, EUR €0.25/€50) with admin-update + anon-revoked RLS; `proof_versions.has_personalisation boolean not null default false`; `public_proof_versions` and `public_get_customer_proof` updated to expose the flag and a `personalisation_pricing` map scoped to currencies referenced by the proof's versions. Pricing is live (not snapshotted).
- **000173** — DB-level CHECK on `proof_versions`: `NOT (is_variant_round AND has_personalisation)`. Added NOT VALID then validated separately. Form-side gates in NewVersionPage / EditVersionPage already enforce this; the constraint is the backstop.
- **000174** — remediate anon leak on `public_proof_version_images`. Migration 000168 dropped and recreated the view for QR columns but lost the `REVOKE FROM anon` that 000162 had installed. An audit (PV-2026W20-027 / PV-2026W20-041) confirmed the live leak — anon SELECT was returning vCard payloads. Same pattern as 000148/000151; fixed with a single `REVOKE SELECT ON public_proof_version_images FROM anon, public`.
- **000175** — admin-managed lead times on `materials`. Three nullable columns: `lead_time_min_days int`, `lead_time_max_days int`, `lead_time_updated_at timestamptz`. CHECK: either both min/max are NULL or both are set with positive integers and max ≥ min. Designer-only surface (quote compiler); not exposed on customer-facing views or the anon RPC. `lead_time_updated_at` is explicitly nullable (no `default now()`) so unset rows don't claim false freshness.
- **000177** — expose `m.code as material_code` on both `public_proof_versions` and `public_get_customer_proof`. Rebuilds the view (drop+recreate, grants re-stated) and updates the RPC (`create or replace`). Customer page uses this to gate material-specific contextual panels: `material_code?.startsWith('metal_')` shows the metal thickness guide; letterpress construction continues to gate on colour-data presence. Null when `material_id` is null (per-direction-pricing variant rounds).
- **000176** — explicit per-table grants ahead of the Supabase 30 October 2026 enforcement that removes the schema-wide implicit `GRANT ALL ... TO anon, authenticated, service_role` plus the matching `ALTER DEFAULT PRIVILEGES`. After that date, REST writes from the authenticated session would start returning `permission denied`. This migration restates the matrix explicitly: full CRUD on 20 frontend-written tables (companies, contacts, proofs, proof_versions, proof_version_images, proof_name_approvals, proof_round_variants, proof_pins, proof_attention_snoozes, profiles, materials, material_variants, material_option_surcharges, price_tiers, add_on_prices, letterpress_core_colours, personalisation_pricing, reply_templates, site_settings, settings); SELECT only on 6 tables whose writes flow via service-role edge functions or SECURITY DEFINER RPCs (material_options, add_ons, audit_log, proof_version_views, proof_events, app_settings); `usage, select` on all sequences; and `ALTER DEFAULT PRIVILEGES` so future tables inherit CRUD automatically. Deliberately NOT granting `anon` — the customer-page anon-RPC pattern from 000162 stays. Grant matrix derived empirically by grepping `src/` + `supabase/functions/` for `.insert|.upsert|.update|.delete` per table; admin-only catalogue writes (e.g. `price_tiers`, `material_variants`) flow through authenticated sessions gated by RLS, so they're CRUD here.
- **000178** — international FedEx shipping in the Quote compiler (designer-only). Three pieces: `material_variants.weight_grams int not null default 10 CHECK (> 0)` for the per-card weight that drives parcel weight calculation (per-variant rather than per-material because thickness materially changes the figure on the metal family); `settings.fedex_box_weight_grams int default 250` (empty-box tare, placeholder until Rob weighs one) and `settings.fedex_intl_adjust_percent numeric(5,2) default 0 CHECK (between -100 and 100)` for an admin-tweakable global percentage applied to FedEx totals at render time so changes take effect without invalidating the cache; and `fedex_rate_cache` keyed on `(dest_country, dest_postcode, weight_grams, currency)` for server-side rate caching with TTL enforced in the edge function (12h). `fedex_rate_cache` is the canonical example of the 000176 "future SELECT-only tables need an explicit REVOKE" footgun: the migration `REVOKE ALL ... FROM anon, authenticated` and enables RLS with zero policies so the rate cache stays service-role-only. Customer-facing surfaces (public_* views, anon RPCs) are deliberately untouched — shipping must not leak to `/p/:id`.
- **000179** — flat DPD domestic-UK shipping rates as two new `settings` columns: `domestic_uk_mainland_rate_gbp numeric(8,2) default 12.90` and `domestic_uk_ni_rate_gbp numeric(8,2) default 18.90`, both VAT-inclusive GBP per the GBP convention. The compiler detects Northern Ireland from a `BT`-prefix postcode under `destCountry='GB'` and picks the matching rate; EUR/USD displays convert via the same Frankfurter/ECB path FedEx already uses. Admin-editable from `/admin/settings`, so DPD price changes don't need a redeploy.
- **000180** — `public_get_lead_times()` SECURITY DEFINER RPC, EXECUTE granted to anon. Returns a jsonb array of `(code, display_name, category, lead_time_min_days, lead_time_max_days, lead_time_updated_at)` for every active/published/non-archived material that has a lead-time pair set; materials with no lead time are omitted entirely so the marketing-site `/turnaround-times` page only shows figures a human signed off on via the admin Lead times tab. Same anon-RPC house pattern as 000162: no view, no fresh anon grant; the 000175 lead-time columns stay reachable through this tight surface only, with the rest of `materials` (pricing, disclaimers, split-name surcharges) unexposed.
- **000181** — security-advisor cleanup, part 1. Sets `security_invoker = on` on all 11 public-schema views (the eight `public_*` customer-shape views + `public_dashboard_projects`, `material_price_tier_counts`, `dashboard_latest_events`) so they run with the querying user's RLS context instead of the owner's. Done via `ALTER VIEW ... SET (security_invoker = on)` — option-only change, no drop/recreate, grants stay intact. Genuinely dangerous exposure (anon enumerating customer data) was already closed in 000162; for the designer role the owner-vs-invoker switch is a no-op because every underlying SELECT policy is `to authenticated using (true)`. The win is the advisor-clean declaration: 11 CRITICAL findings cleared in one pass without re-stating any grant.
- **000182** — security-advisor cleanup, part 2. Two safely-mechanical fixes for the WARN-level findings: (1) pins `search_path = public` on the 27 plain SECURITY INVOKER functions that lacked it (looped over `pg_proc` filtered on `prokind='f'` and absence of any `search_path=%` element in `proconfig`, idempotently skipping extension-owned functions to avoid `must be owner` failures) — closes the latent injection vector where a caller could inject a lookalike object via their search_path; (2) revokes EXECUTE from `public, anon, authenticated` on 11 internal-only SECURITY DEFINER trigger/plumbing functions that nothing in `src/` or `supabase/functions/` calls — triggers still fire because trigger invocation ignores EXECUTE, but the functions are no longer part of the callable API surface. The other 21 SECURITY DEFINER functions (the deliberate anon + designer/admin API) are left callable.
- **000183** — fill the USD Standard Paper price-tier gap surfaced by the 2026-05-15 CommonNinja vs Supabase parity audit. Inserts 12 `price_tiers` rows for `paper_standard` USD: quantities 2000 / 3000 / 5000 / 10000 across the three finish variants (`standard`, `uv_spot`, `foiling`). GBP and EUR already carried those quantities; only USD was short. Totals taken from the live US price list (treated as source of truth where it and the DB diverge, same stance as 000111); unit_price computed to 4 dp to match seed convention. `on conflict (material_variant_id, currency, quantity) do nothing` for idempotency. After this, all 1,248 cells across the three CommonNinja currency pages reconcile exactly against Supabase, which is the precondition for 000184's read path.
- **000184** — `public_get_price_list(p_currency text)` SECURITY DEFINER RPC, EXECUTE granted to anon. Single-round-trip read path for the new marketing-site price-list script (see "Price-list rendering script" below): takes one currency and returns a jsonb object `{ currency, materials: [{ code, display_name, variants: [{ id, code, display_name, variant_type, ink_count, sort_order, tiers: [{ quantity, total_price }] }], option_surcharges: [{ option_code, quantity, surcharge }] }] }`. Filters to `is_active = true AND is_published = true AND archived_at IS NULL` on materials so unpublished rows (Titanium today) stay hidden, plus `is_active = true` on variants and `currency = p_currency` on tiers and option surcharges. `carbon_fibre_cnc` and `paper_letterpress_gilded` come through as ordinary materials; the script computes the CNC and gilding surcharge columns as differences against the base materials. Pricing data only — no customer, proof, contact, profile, audit, or settings columns. Same house pattern as 000180; raw `price_tiers` and the catalogue views stay anon-revoked.
- **000192** — hosted-vCard QR codes. Adds `proof_version_images.qr_vcard_slug text` and widens the qr-consistency CHECK to allow `qr_kind = 'hosted_vcard'`, with the slug populated iff that's the kind. The Plasma vCard product prints a QR pointing at a short URL (`https://qcrd.uk/<slug>`); the old upload-and-decode path can't show the customer the actual contact details because there aren't any in the QR payload. The new kind lets the proof viewer generate the QR itself (so the QR shown for approval is provably the one that prints) and fetch the live contact fields from the vCard app's anon RPCs for the customer-side verification panel. Rebuilds `public_proof_version_images` to expose the new column and restates the anon revoke + the authenticated grant + `security_invoker = on` (per the 000168/000174/000181 drop+recreate footgun chain). `public_get_customer_proof` is intentionally untouched — image rows flow through the `customer-proof-images` edge function which `select *`s the view, not through the RPC. The 000169 `qr_confirmed_at` predicate already gates any `is_qr_code = true` row, so hosted-vCard QRs inherit the existing approval flow without a function rewrite. See "Plasma vCard QR integration" below.
- **000185–000191** — the PV-2026W21 bug-audit follow-ups. 000185 added the defensive `REVOKE INSERT, UPDATE, DELETE` on `personalisation_pricing` from anon. 000186 widened the snooze lateral in `public_dashboard_projects` to a 24-hour grace window so a recently-expired snooze still surfaces `snoozed_until` (this powers the `recentlyAwakened()` Today-bucket boost; frontend predicates meaning "currently snoozed" must therefore check the timestamp is in the future, not just non-null). 000187 dropped the dead `dashboard_tile_counts()` function after every tile count moved client-side. 000188 added `designer_override_approve` to the needs-attention rule event-type predicates. 000189 added the `proof_pins (scope, user_id)` CHECK constraint. 000190 relaxed the `proof_pins.pinned_by` insert `WITH CHECK` to `(= auth.uid() OR IS NULL)`, matching the 000167 snooze pattern. 000191 added the missing `WITH CHECK` to the `avatars` storage UPDATE policy.
- **000193–000196** — 000193 added `image/svg+xml` to the `proof-images` storage bucket's allowed mime types so the generated hosted-vCard QR SVGs upload. 000194 added `proof_name_approvals.qr_snapshot jsonb` — Phase 4 of the vCard QR brief — capturing the hosted-vCard contact fields the customer approved, written slug-keyed by the proof-action edge function at approval time. 000195 added the `public_get_letterpress_colours()` SECURITY DEFINER anon RPC for the separate Stock Control app, same house pattern as 000180/000184. 000196 added `proof_events.side` (`front` | `back`, nullable) recording which card side the customer had open in the detail view when they submitted a change request. ⚠️ Note: migration 000186 dropped and recreated `public_dashboard_projects` but did not restate `security_invoker = on`; migration 000197 (bug-audit PV-2026W22-110) re-pins it.
- **000197–000206** — the reskin/scaling/editable-copy batch, all pushed to production 2026-05-29 alongside the reskin going live on `main`. Two pre-existing-but-unpushed migrations led: **000197** re-pinned `security_invoker = on` on `public_dashboard_projects` (000186 had dropped+recreated it without restating the option); **000198** added `latest_non_view_event_at` / `latest_non_view_event_type` to `public_dashboard_projects` so the Changes-requested tile ignores synthetic `view` events (PV-2026W22-087). Editable customer copy (same anon `public_settings()` house pattern, nullable columns seeded with shipped defaults, frontend falls back if null): **000199** metal Thickness card notes (`settings.metal_thickness_notes` jsonb) + the "About this proof" note (`settings.about_proof_copy`); **000200** the QR-panel copy (`settings.qr_panel_intro_copy` / `qr_panel_vcard_copy`); **000206** the login-page copy (`settings.login_headline` / `login_description` / `login_stats` jsonb / `login_form_heading` / `login_form_subcopy`). Scaling RPCs (all SECURITY INVOKER, authenticated-only, no anon grant): **000201** `admin_search_customers(p_search, p_sort, p_limit, p_offset)` → one paginated/searchable/sorted page of companies with contact + project counts + last-activity + global overview + the "No company" orphan summary, for the rebuilt list+detail Admin → Customers; **000202** `dashboard_tile_counts()` → the six stat-tile counts computed across ALL proofs so they're correct regardless of the loaded list (replaces the client-side counts; supersedes the 000187 drop — predicates kept in exact lockstep with the dashboard's click-through filters, each excluding currently-snoozed proofs); **000203** `dashboard_list()` → the dashboard "working set" (active `in_progress`/`dormant` + approved ≤14d + abandoned ≤30d + anything pinned mine/team) instead of loading every proof capped at 2000; **000204** added `or rule_code is not null` to that set so every needs-attention proof is present regardless of status (the live data has needs-attention proofs outside the active statuses the old docs assumed); **000205** added a `p_search text default ''` param (empty → working set, non-empty → all proofs incl. the archive, matched on company/contact/email/HS-id/proof-id) and dropped the zero-arg overload. Customers add/edit/delete + dashboard pin/snooze write paths shipped but were not click-tested end-to-end against live data at cutover.
- **000207–000209** — the Needs-attention resolve/nudge batch (PR #222) plus the dormancy-cutoff fix. **000207** seeds four per-rule "send a reminder" `reply_templates` rows (`nudge_sent_never_viewed`, `nudge_viewed_not_actioned`, plus the other two chase rules) for the one-click nudge in the dashboard/detail resolve popover; same house pattern as 000157 (`ON CONFLICT (id) DO NOTHING`, bodies mirror `DEFAULT_BODIES` in `src/lib/replyTemplates.ts`, no sign-off, `designer_picked` variable set). **000208** ships inbound Help Scout reply-activity sync (see `docs/helpscout-webhook-spec.md`): two timestamp columns on `proofs` (`helpscout_last_reply_at`, `helpscout_last_customer_reply_at`) stamped by the `helpscout-webhook` edge function; a `helpscout_reply_grace_days` scalar key (default 3) added idempotently inside the `needs_attention_rules` JSONB; a `proofs_needing_attention()` rewrite (body verbatim from 000188 plus a grace guard suppressing the four customer-chase rules — not `request_changes_no_version`, not `helpscout_follow_up_tag` — when a reply happened within the grace window); and a `public_dashboard_projects` create-or-replace (000198 definition + the two timestamps, grants + `security_invoker` re-stated). Self-clearing: the flag returns when the window lapses if the customer still hasn't acted. ⚠️ The grace key lives alongside the six rule objects in `needs_attention_rules`; the admin Needs-attention "Reset to defaults" path must merge defaults over the saved object rather than replace it, or the scalar key is dropped on the next save (PV-2026W22-239). **000209** made the dormancy cutoff admin-editable and fixed a 60-day drift: it added `site_settings.dormancy_threshold_days` (default 90), pointed `auto_dormant_stale_proofs()` at it instead of the literal `'90 days'`, and rewrote the `approaching_dormant` rule in `proofs_needing_attention()` so its warning band counts back from that cutoff instead of a hardcoded `30` — the rule had been firing ~60 days before proofs actually went dormant. Both functions `CREATE OR REPLACE`, preserving the 000182 grants/search_path. Admin surface: a "Dormant cutoff" control on Admin → Needs-attention, saved + audited alongside the rules. Default 90 means apply-time behaviour is unchanged.
- **000210–000212** — the proof-type wizard's Set (collection) shape gets real storage (Phase 2 of `docs/proof-type-wizard-phase2-spec.md`). **000210** adds `proof_versions.shape` (nullable: `recipients` | `set_single` | `set_collection` | `selection`), the `proof_layouts` table (one row per titled layout in a collection — deliberately mirrors `proof_round_variants`: write-once `code`, `display_name`, `sort_order`), and `proof_version_images.layout_id` (the receive-all equivalent of `round_variant_id` for pick-one and `associated_name` for recipients); rebuilds `public_proof_version_images` (re-stating the anon revoke + authenticated grant + `security_invoker`) and `public_get_customer_proof` to expose `shape` + the layouts list. Additive and inert on prod. **000211** backfills `shape` from the pre-wizard flags (`is_variant_round`→selection, membership+no-names→set_single, business→recipients), idempotent via `shape is null` guards; legacy membership-with-names tier proofs are deliberately left null (no honest shape). **000212** teaches `_finalize_proof_if_complete` that a `set_collection` is approve-each — every layout is a required approval slot, same rule as Recipients but keyed on layouts instead of names — via one early-return branch, leaving the 000169 body byte-for-byte intact.
- **000213** — folds the dashboard's "Changes requested" stat tile into a broader **"Customer responded"** tile that also counts proofs where the customer replied on the linked Help Scout conversation (`helpscout_last_customer_reply_at`, 000208), not just the in-app sidebar change-request path. Only the `changes_requested` JSON key on `dashboard_tile_counts()` is replaced by `customer_responded` (= old test OR "replied by email and we haven't responded since"); the other five counts are byte-for-byte 000202. The per-row chips stay distinct ("Changes requested" vs "Replied by email" — see `proofBucket` / `isCustomerReplied` in `src/lib/dashboardGrouping.ts`, kept in lockstep with the tile predicate).
- **000214** — **follow-up automation Phase 1** (dry-run only). Adds `proof_nudges` (the single ledger for manual + automatic reminders, so cap/spacing maths has one source of truth), `settings.auto_nudges_enabled` (default false — keeps every `send-nudges` run in `dry_run` mode), `compute_nudge_candidates()`, a heartbeat, and an analytics view. Spec: `docs/followup-automation-spec.md`; rollout executed 2026-06-10 per `docs/followup-automation-rollout.md` (applied via MCP, all functions deployed, cron + vault secret created, first dry run verified). All objects schema-qualified for the merged `proofs` schema with explicit grants (the schema has no default privileges — forgetting `service_role` breaks the edge function).
- **000215** — `proof_versions.last_reply_sent_by` (FK `auth.users`), companion to the existing `last_reply_sent_at`, powering "Donna Lambe sent a reply for v2" on the detail-page Activity timeline. A column rather than an `audit_log` read because `audit_log` SELECT is admin-only by RLS. `send-helpscout-reply` stamps the caller's id; `send-nudges` stamps NULL explicitly (an automated reminder shouldn't inherit the last manual sender's attribution → renders as unattributed "Reply sent for vN"). Backfilled from the latest `reply_sent` audit row per version.
- **000216/000217/000219/000225–000227** — the **AI draft pipeline** going live (`docs/ai-draft-pipeline-spec.md`, `docs/ai-draft-rollout-phase2.md`). **000216** (Phase 2): `proofs.ai_drafts` ledger (service-role writes, designer reads) + `settings.ai_drafts_mode` master switch (`off` default / `shadow` = ledger only / `live` = drafts + notes + tag appear in Help Scout). **000217_ai_drafts_cache_usage** adds `usage_cache_write`/`usage_cache_read` so shadow can verify prompt-cache hits. **000219** (Phase 3 feedback loop) records the reply the team actually sent against each draft (`sent_body`, `edit_similarity`, `edit_class` ∈ sent_as_is/lightly_edited/rewritten/discarded) for an edit-distance measure. **000225** (Phase 3a) graduates the drafter's *house rules* + *exemplars* into admin-editable tables (`ai_draft_house_rules`, `ai_draft_exemplars`) — tone guide, link allow-list, and guardrails deliberately STAY in code; the drafter falls back to the compiled TS constants on any error and logs the fallback, and `pnpm check:briefing-seed` guards the seed against drift. **000226** (Phase 3b) adds the `ai_draft_proposals` human-in-the-loop queue + a SECURITY DEFINER apply RPC — the only write path from a proposal into the live briefing tables, gated on an admin clicking Approve. **000227** adds `ai_drafts.skip_kind` so the panel separates genuine spam from supplier mail / automated notifications instead of lumping them as "Spam".
- **000217_needs_attention_approved_earlier_version** — needs-attention rule: a customer who steps back in the version selector and approves a *superseded* version never finalizes the proof (the current version stays unapproved), so the sign-off was invisible on the dashboard. Detection mirrors the detail-page banner exactly (an `approved` row on a non-current version whose name isn't approved on the current version, proof not approved/abandoned). No threshold; not a customer-chase rule so the 000208 grace window doesn't apply.
- **000218** — finalize a proof when an already-approved version is *promoted*. "Set as current" and the delete auto-promote do a bare `update proof_versions set is_current = true`, which the 000126 approval-row trigger never sees, so a fully-approved-but-superseded version that becomes current left the proof stuck `in_progress`. Fix: an `AFTER UPDATE OF is_current` trigger that re-runs the same `_finalize_proof_if_complete` (SECURITY DEFINER, since 000182 revoked EXECUTE from authenticated). Same rules (slots, shared, QR gating, set-collection) — just a second moment the check runs.
- **000220/000222** — auto-wake snoozes when the customer does the thing the snooze was waiting for. **000220**: `record_proof_view()` (the single view chokepoint) expires an active `sent_never_viewed` snooze (`snoozed_until = now()`) the moment a genuine non-bot view of the current version lands. **000222**: an `AFTER INSERT` trigger on `proof_events` expires the four chase-rule snoozes + `nudges_exhausted` when a customer approve / request_changes lands (`designer_override_approve` excluded; `request_changes_no_version` / `approved_earlier_version` snoozes left alone — those track designer-side work). Both expire rather than DELETE so the row survives for audit/grace logic.
- **000221** — needs-attention: **`nudges_exhausted`** as a first-class rule ("2 reminders sent, still no response") so a capped chase doesn't die silently as a `skipped_capped` Outbox row — fires when a chase rule is still firing AND the `proof_nudges` per-version cap is spent for that rule on the current version (counting `sending`/`sent` of any source; manual reminders count). Also widens `rule_meta` to expose all firing rules, not just the top-priority one.
- **000223** — follow-up automation Phase 2: `compute_nudge_candidates()` gains a `has_followup_tag` output column (`helpscout_tags @> '{follow up}'`) so the sender's tested decision module can skip a human-flagged conversation (`skipped_followup_tag`) rather than letting priority ordering decide silently. Return-shape change → DROP + CREATE, grants re-stated (service_role only).
- **000224** — scope the nudge sender's send-evidence fallback to the version it's evidence *for*. The `coalesce(version.last_reply_sent_at, proofs.helpscout_last_reply_at)` fallback (for versions sent directly from Help Scout) could predate the current version and trigger a "doesn't look opened yet" nudge about a version the customer was never told existed. Fix: the fallback reply only counts as send evidence when it postdates the current version's creation; otherwise skip with `skipped_no_send_evidence`. From the 2026-06-12 adversarial review.
- **000243** — let designers delete a whole project without an admin. Reverts the `proofs` slice of 000074: drops the admin-only `proofs: admin delete` RLS policy and restores `proofs: authenticated delete` (`using (true)`); `companies` / `contacts` stay admin-only. Frontend drops the matching `role === 'admin'` gate on the Delete-project danger-zone button (`src/pages/ProofDetailPage.tsx`). Versions were already designer-deletable; `delete_proof_cascade()` is SECURITY INVOKER + granted to `authenticated`, and every FK to `proofs.proofs` is `ON DELETE CASCADE` (orders, proof_versions, proof_pins, snoozes, nudges), so the cascade works for designers. Applied to live via MCP `apply_migration` on 2026-06-18. ⚠️ Numbering gap: source now runs to 000243, but the **000225–000242** migrations between this and 000224 above aren't itemised in this summary — run `ls supabase/migrations/0002*` for the full list, never infer the next free number from this doc.
- **000244** — graduate the `viewed_not_actioned` chase rule to **auto-send** (the follow-up spec's deferred "generalise `compute_nudge_candidates` per-rule" step). The function is DROP+CREATEd to UNION two populations, each row tagged with `rule_code` (`sent_never_viewed` | `viewed_not_actioned`) and `anchor_at` (the per-rule threshold clock: send-evidence for snv — exactly its old value, so snv behaviour is byte-identical; the customer's last non-bot view for vna). The vna predicate mirrors `proofs_needing_attention()`'s vna branch exactly (current version viewed, no approve/request_changes/designer_override_approve since that last view); snoozes still pause proof-level (any rule). The `send-nudges` edge function now drives a rule list instead of one hard-coded `RULE_CODE`, with one `NudgeConfig` per rule and per-candidate template/subject; `_shared/nudgeDecision.ts` gained `ruleCode`/`anchorAt` and counts the per-(proof, rule, version) cap by `facts.ruleCode`. `toFacts` defaults a missing `rule_code`→`sent_never_viewed` / `anchor_at`→`send_evidence_at` so a function deploy that lands before this migration stays correct (deploy the function first — order-safe). The migration tail `jsonb_set`s `needs_attention_rules.automation.viewed_not_actioned` to `{mode: auto, max_nudges: 2, repeat_days: 3}`, so apply + deploy turns vna auto-send on; the first live run sends ~6 (the 46→13→6 funnel reconciles exactly to the dashboard's vna count). Admin → Needs-attention's "not wired yet" note now shows only for the two review-only rules (`AUTO_SEND_RULES` gates it). Grants re-stated service_role-only. **Applied + deployed 2026-06-18** — migration via MCP `apply_migration`, `send-nudges` redeployed (ref `bjvinrzbdrwebylkmbwy`); verified live (the function returns both populations, `automation.viewed_not_actioned` = `{mode: auto, max_nudges: 2, repeat_days: 3}`).

- **000245** — stop the dashboard's **Awaiting customer** tile (count + click-through) counting proofs the customer has already responded to. The `awaiting_customer` predicate in `dashboard_tile_counts()` gained a `not (<the customer_responded OR>)` guard so a viewed-then-responded proof counts only under Customer responded, matching the row-pill precedence (`proofBucket`). Client filter in `DashboardPage.tsx` mirrors it via the existing `isChangesRequested` / `isCustomerReplied` helpers. Applied to live via MCP; verified 57→48.
- **000246** — **"Needs attention" becomes human-only, plus a new "In follow-up" tile.** New helper `proofs_in_follow_up()` is the single source of truth for "the automation is actively chasing this proof" — keyed off the nudge ledger (≥1 sent reminder on the current version, cap not spent, rule in auto mode), so the "hide from attention" set and the "show in follow-up" set are provably identical (never-sent / no-send-evidence / exhausted / review-mode all stay in attention — no stranding). `proofs_needing_attention()` suppresses `sent_never_viewed` / `viewed_not_actioned` for in-flight proofs (rest of the 000221/000217 body byte-identical; exhausted chases still surface via `nudges_exhausted`). `public_dashboard_projects` gains four appended `follow_up_*` columns (CREATE OR REPLACE VIEW keeps grants + `security_invoker` + the `dashboard_list` SETOF type). `dashboard_tile_counts()` adds the `in_follow_up` count and excludes in-flight proofs from `not_viewed` / `awaiting_customer` so it's a clean bucket. Frontend: new indigo "In follow-up" tile + click-through filter + `proofBucket` bucket (precedence below the customer-response buckets, above awaiting/not-viewed) + a "Reminder N of M" row label; Admin → Needs-attention page relabelled **"Follow-ups"** (route path unchanged). Applied to live via MCP; verified Needs attention 14→4, In follow-up 28.
- **000271** — **staff feedback board** (new in-app feature, deliberately NOT tied to a proof). Adds `proofs.feedback_items` — staff post a bug/idea/improvement with a status pipeline (`new → under_review → planned → in_progress → done / wont_do`); everyone authenticated reads all + inserts their own (`created_by = auth.uid()`), only admins UPDATE the status/admin_note (gated on `proofs.is_admin()`). Submitter name/initials/colour are denormalised on the row (profiles is self-read-only under RLS) and stamped server-side by the `feedback_items_set_author` BEFORE INSERT trigger (SECURITY DEFINER), so the displayed author can't be spoofed via a raw REST insert. Also adds a private `feedback-attachments` storage bucket (10 MB, png/jpeg/webp; signed-URL reads, owner-write by `foldername[1]`, owner-or-admin delete). Frontend: `/feedback` board (`src/pages/FeedbackPage.tsx`) + a "Feedback" header nav pill, a submit modal with paste/drop/file-pick screenshots, and an in-app markup canvas (`src/components/ScreenshotAnnotator.tsx` — boxes/arrows/pen flattened to PNG). ⚠ Numbering: source jumps 000246→000271 here; **000247–000270 aren't itemised in this summary** — run `ls supabase/migrations/0002*`. Applied to live via MCP `apply_migration` on 2026-06-26.
- **000272** — priority on the feedback board (follow-up to 000271). Adds `proofs.feedback_items.priority` (`low | medium | high`, default `medium`, CHECK-constrained). Submitters suggest a priority when posting (the 000271 insert RLS already lets them set any column on their own row); only an admin can change it afterwards, via the same admin-only UPDATE policy that gates status — no new policy needed. Frontend: a priority picker in the submit modal, a colour-coded pill on each card, a priority filter, and a priority override in the admin triage box. Applied to live via MCP `apply_migration` on 2026-06-26.
- **000276** — **analytics functions** backing the new Admin → Analytics page (the first conversion-insight surface, built from the late-June-2026 conversion analysis — see memory:conversion-analytics-baseline). Adds five read-only `SECURITY INVOKER` `language sql` functions in the `proofs` schema: `analytics_funnel()` (json — enquiry→opened→decided→approved→order-sent→paid counts, the viewed-but-never-decided leak, median days-to-approve, and `settings.payment_mode`), `analytics_weekly()` (per-creation-week cohort with a `mature` flag for cohorts ≥7 days old), `analytics_by_designer()` (per-designer conversion, maturity-controlled to proofs >7 days old so a designer holding fresher work isn't penalised), `analytics_by_segment(p_dimension)` (`material | currency | shape | recipients`), and `analytics_hot_leads()` (the live "opened, no in-app decision, not snoozed" worklist, tiered hot / re-engaged / stale / warm by repeat-view buying signal). All read base tables + `public_dashboard_projects`, so figures reconcile with `dashboard_tile_counts()` by construction; EXECUTE revoked from anon/public, granted to authenticated only. Applied to live via MCP `apply_migration` on 2026-06-27 (PR #365). Frontend: `src/pages/admin/AdminAnalyticsPage.tsx`, route `/admin/analytics`, first Admin sidebar tab; hand-rolled SVG/CSS charts (no chart-library dependency). "Won" = a paid order per Rob's definition, but Approved is shown alongside as the leading signal; the true top-of-funnel (Help Scout enquiry volume, since only *qualified* enquiries become proofs) is not yet instrumented.
- **000277** — analytics follow-ups from the designer-disparity deep-dive (PR #365). Adds `designer_user_id` + `is_returning` to `analytics_hot_leads`; rebuilds `analytics_by_designer` from a raw approval league table into a FAIR scorecard (open queue, returning share, new-customer conversion on the >7-day cohort, change-request recovery, avg days); adds returning-vs-new + change-request-recovery fields to `analytics_funnel`; adds a `'returning'` dimension to `analytics_by_segment`. Tag rule baked in: only the durable `repeat customer` tag is safe for conversion analysis — lifecycle tags (priority 1/2/3, ready to order, etc.) are stripped by the approval workflow and correlate spuriously. Applied to live via MCP 2026-06-27.
- **000278** — fix the Help Scout tag sync gap (PR #365). The `convo.tags` webhook only updates an already-existing proof, but Graphics tags the conversation before the proof exists, so ~64% of tags were never mirrored. Adds `proofs.helpscout_tags_synced_at`, stamps the 2026-06-27 one-off backfill as synced, and schedules the `proofs-reconcile-helpscout-tags` cron (every 15 min) that pulls tags for never-synced proofs + refreshes active ones every 12h. See memory:helpscout-tags-sync-gap. Backed by the `reconcile-helpscout-tags` edge function. Applied to live via MCP 2026-06-27.
- **000279** — **decline-feedback + price recovery** (PR for `feat/decline-feedback`, built from the conversion analysis: price is the #1 stated loss reason and the customer almost always liked the design). Adds `proofs.proof_feedback` (reason_code in price_too_high / different_direction / timing / going_elsewhere / still_thinking + note + recovery_offer snapshot; service-role write, authenticated read, anon revoked), the `decline_recovery_discount_enabled` / `_percent` settings (OFF by default), the anon `public_get_cheaper_alternatives(p_proof_id)` SECURITY DEFINER RPC (same-category cheaper materials + the discount config — pricing-data-only, house pattern), and `analytics_loss_reasons()`. The customer page shows a "Not ready to approve?" panel (`src/components/DeclineFeedbackPanel.tsx`) with face-saving reasons + price recovery; writes via the `proof-feedback` edge function (anon, isolated from proof-action) which also posts a Help Scout note. Admin → Settings gains the discount controls; Admin → Analytics gains a "Why we lose" panel. NOT an approval state — the proof stays open. Deferred: auto-suppressing nudges on a terminal reason. Applied to live via MCP 2026-06-27.

- **000304** — **team sharing** (PR #426; designer guide `docs/team-sharing-feature.md`). Adds `proof_versions.team_sharing_enabled` (boolean, default false — every pre-existing version unchanged) and re-emits `public_get_customer_proof` with the flag in the per-version object (CREATE OR REPLACE, grants preserved). When true on a current business-card version with 2+ names, the customer page shows a collapsed-by-default "Share with your team" bar above The set (per-name links + approval status + progress meter; hidden once the proof approves), and `/p/:id?for=<name>` opens the page scrolled to that recipient's ringed, greeted card — presentation only, matched case-insensitively against the current roster, honoured regardless of the toggle so old links keep working. New/Edit version forms carry the "Let the team share this proof" checkbox (new proofs default on; new versions inherit v(N-1)'s choice, so pre-feature projects stay off until ticked once); ProofDetailPage lists per-name copy links under Public URL. Panel: `src/components/ShareWithTeamPanel.tsx`. Deferred: per-layout links on Set (collection). Applied to live via MCP 2026-07-04. ⚠ Numbering gap: **000280–000303** aren't itemised in this list (the 000298–000303 open-spec/rose-gold batch is described under Ordering & checkout) — run `ls supabase/migrations/0003*`.
- **000306** — **zero-rated VAT rates on non-UK Xero invoices** ("0% EU" / "0% ROW"). Adds `settings.xero_eu_tax_type` / `xero_row_tax_type`, seeded to the live org's codes (`TAX004` = "0% EU", `TAX003` = "0% ROW", read from its rate list 2026-07-07). A VAT-free invoice (EUR/USD always; GBP → Channel Islands) now books every line to the rate matching its delivery region — EU VAT area vs rest-of-world, classified destination-first with a currency fallback in `_shared/euVatArea.ts` (`pnpm test:eu-vat`; a EUR order to Switzerland is ROW) — via `LineAmountTypes Exclusive` + per-line TaxType, which at 0% keeps every amount and the Stripe bank-feed match identical. NULL codes keep the legacy `NoTax`/"No VAT" treatment, and the webhook/retry summary-line fallback drops the custom rate, so a rejecting org still gets an invoice. `XERO_SCOPES` gained `accounting.settings.read` so Admin → Settings can list the org's zero rates in the new pickers (and the bank-account picker can finally fill) — needs a **one-time Xero reconnect**; invoicing is unaffected until then (a plain code field shows meanwhile). Same PR fixed `retry-order-invoice` omitting `amount_us_tariff` (+ `order_kind`) from the rebuilt expected total — a retried US-tariff invoice came out short of the Stripe charge and prototypes lost their labelling. (000305 `recent_paid_orders_rpc` isn't itemised here.)
- **000307** — **hold a fully-reminded proof for one reminder-interval before it escalates to Needs attention** (instead of dropping in the instant the last auto-reminder is sent). The `nudges_exhausted` escalation was *meant* to be delayed by the 000208 Help Scout grace window, on the assumption the final reminder self-stamps `helpscout_last_reply_at` — but reminders #2+ open a FRESH Help Scout conversation (send-nudges §6, deliverability), which fires `convo.created`, an event the webhook doesn't treat as a reply, so the stamp stays frozen at reminder #1's time (~13 days stale by reminder #4 on live) and the grace window never delays anything. Fix (verified against LIVE `pg_get_functiondef`, not the migration files — live `proofs_needing_attention()` already reads `request_changes_no_version` from `proof_name_approvals`): measure the wait from the reminder ledger instead. Two `CREATE OR REPLACE` (signatures unchanged, grants/`security_invoker` preserved): (1) `proofs_in_follow_up()` gains **Branch C** — a cap-spent proof whose newest counted reminder went out `< repeat_days` working days ago stays "in follow-up" (shows "Reminder N of N" on the dashboard, no frontend change); (2) `proofs_needing_attention()`'s `exhausted` CTE only emits its row once `business_days_between(last reminder, today) >= repeat_days` (`cap_counts` gains `last_sent_at`). The two guards are exact complements (`< repeat_days` → Branch C + S3 suppresses the raw chase; `>= repeat_days` → Branch C releases, exhausted row appears, S4 suppresses the raw chase), so the "never opened"/"not actioned" reason can't leak into Needs attention during the wait. Working days match the sender's `repeat_days` cooldown convention; if `nudges_exhausted` is disabled the row never appears and the raw chase surfaces after the wait, so nothing is stranded. Read-only preview at author time: all 8 then-current `nudges_exhausted` proofs (last reminder 0–1 working days ago) move to In-follow-up until their 3rd working day. Applied to live via MCP `apply_migration` on 2026-07-07 (PR #433); verified live (exhausted-in-attention 8→0, 9 held in In-follow-up via Branch C, security advisors clean for both functions).
- **000308** — **stop a viewed proof showing as a stale `sent_never_viewed` follow-up.** `proofs_in_follow_up()`'s Branch A/C read the reminder ledger (`proof_nudges.rule_code`, frozen at send time), so a proof chased under `sent_never_viewed` and *then* opened by the customer kept reporting an snv chase in In-follow-up even though the sender had already switched populations (the snv arm of `compute_nudge_candidates()` excludes any current version with a non-bot view). Symptom found on live (proof 929092c6): listed in BOTH In-follow-up (snv, "Reminder 3 of 4") and Needs attention (`viewed_not_actioned`), because the S3 suppressor matches follow-up on (proof_id, rule_code) and the two codes no longer agreed. Fix: gate the snv classification in Branch A/C on "current version has no non-bot view" (new `viewed_current` CTE), mirroring the snv predicate in `proofs_needing_attention()` and the snv candidate arm — a viewed proof drops out of snv follow-up and lands in exactly one place (vna In-follow-up if still a live auto-candidate, else vna Needs attention). `viewed_not_actioned` follow-up needs no guard (its branches are inherently post-view). One `CREATE OR REPLACE`, signature unchanged; `proofs_needing_attention()` untouched (it reads the corrected `proofs_in_follow_up()`). Applied to live via MCP `apply_migration` on 2026-07-08; verified live (`raw_chase_leak` 1→0, proof 929092c6 now single-listed under In-follow-up as `viewed_not_actioned` instead of a stale snv chase).
- **000309** — **order groups (combined payments)**, bundle orders Slice 2 (PR #438, merged + fully live 2026-07-09; design note `docs/order-groups-slice2.md`, refund runbook `docs/combined-payment-refund-runbook.md`). Adds `proofs.order_groups` owning the MONEY only: status (sent/paid/cancelled), currency, one delivery destination, the combined-shipping billing choice, pay-link token + expiry, a **`GRP-` payment reference** (single orders use `ORD-`; the 1:1 payment ↔ invoice reconciliation spine), Xero invoice id/error, and done-once flags (invoice created / emailed / confirmation sent) so webhook retries can't double-invoice. `orders.order_group_id` is a nullable FK with a partial index — standalone orders untouched. Full grant matrix stated (the proofs schema has no defaults); anon reads via the token-gated `public_get_order_group(p_group_id, p_token)` RPC + `record_order_group_pay_link_opened`, and `public_get_order` re-emitted to append `order_group_status` so a member's own pay page shows the "paid together with others" state. Applied to live via MCP `apply_migration` (`order_groups_combined_payments`) on 2026-07-08, Rob approved. See the **Combined payments** block under Ordering & checkout for behaviour.
- **000310** — **recipient phone at checkout** (`ship_to_phone`). Additive `text` column on `proofs.orders` AND `proofs.order_groups`, plus a re-emit of `admin_search_orders` (live 000264 shape + the one column). The pay pages switch on the Stripe Address Element's built-in phone field (required — FedEx paperwork needs a recipient contact number), it rides `confirmPayment` onto `payment_intent.shipping.phone` exactly like the address, and `stripe-webhook` persists it alongside `ship_to_name`/`ship_to_email`/`ship_to_address` (single orders; group + every member for combined payments). Shown in the "Deliver to" block on `/orders` cards and the admin Order log; NOT exposed on `public_get_order`/`public_get_order_group` (customer paid screens unchanged). Stock Control reads `proofs.orders` in the merged DB, so the number reaches the shipping step with no extra plumbing. ⚠ Deploy order: apply the migration FIRST — the OrdersPage select and the webhook update both name the column, so a frontend/function deploy against an unmigrated DB breaks the Orders page and 500s the webhook.
- **000311** — **proof sets** (bundle orders Slice 3, the PROOFING layer — `docs/bundle-orders-spec.md` §14). Adds `proofs.proof_sets` (the context entered once: `contact_id` NOT NULL, HS conversation id/url, advisory `currency`, bearer `token` for the customer link, `sent_at` = the membership lock, `last_opened_at`), `proofs.proofs.proof_set_id` (nullable FK, `ON DELETE SET NULL` — deleting a set releases its cards to standalone) + partial index, and `proofs.proofs.set_discarded_at` (customer set the card aside on the review page; reason lands in `proof_feedback` via the proof-feedback edge fn's new `set_discard` mode, which also skips the CS mailbox move and refuses on approved cards). Full grant matrix mirrors `order_groups` (authenticated CRUD + RLS `proof_sets_rw`, service_role ALL, anon nothing). Anon reads flow only through `public_get_proof_set(p_set_id, p_token)` — token-gated SECURITY DEFINER, explicit payload (set + company/contact names + per-member summaries: status, names, material_display, shape, current-version fields — **no pricing, no HS ids, no internal notes**) — plus `record_proof_set_opened` (stamps only after `sent_at`). Seeds the `set_review_link` reply template (display name "Bundle review link" since the 10 Jul naming decision; id deliberately NOT `proof_`-prefixed — the admin Templates page groups `proof_*` ids as post-action confirmations; this is a designer-composed pre-send message). Distinct from and completely independent of 000309's `order_groups` — sets link PROOFS at proof time and end at approval; groups link ORDERS at payment time. ⚠ Deploy order: apply the migration FIRST — ProofDetailPage's proof select names `proof_set_id`/`set_discarded_at`, so a frontend deploy against an unmigrated DB breaks the proof page; deploy the updated `proof-feedback` edge fn alongside the frontend for the discard flow.
- **000312** — seed the `bundle_update_link` reply template ("Bundle update"), backing the bundle workspace's **Send update** step after the membership lock was relaxed (10 Jul): a card CAN now be added to an already-sent bundle — the customer keeps the one link, the new card shows as "not announced yet" (its current version has no `last_reply_sent_at`), and Send update posts the announcement + stamps its send evidence. Removal stays pre-send only (post-send drops = customer set-aside or designer abandon). Same seed pattern as 000311's; `send-nudges` was also made bundle-aware in the same change (a member of a SENT bundle is chased with the `/bundle/:id?token` link instead of `/p/:id` — and note `groupSendables`' existing same-email/same-conversation sibling suppression already prevents two bundle cards double-chasing one thread).
- **000317** — **whole-bundle auto-reminders.** Before this, when 2+ cards of one sent bundle came due on the same `send-nudges` run they shared the customer's email, so `groupSendables` suppressed ALL of them (`suppressed_sibling`) and left a human to send one combined message — the bundle was effectively never auto-chased. Now a card in a SENT bundle with **≥2 still-outstanding cards** is tagged with its set id and the bundle's due cards **collapse into ONE reminder** on the bundle's own Help Scout thread with the `/bundle/:id?token` review link, capped/spaced **per set** (3 reminders, mirroring a single card's 000313 cadence). A bundle down to its **last** outstanding card is not a bundle any more — that card chases per-card on its own `/p/` link (Rob, 2026-07-15); the old 000311/000312 per-member bundle-link rewrite is superseded by this. **Widened 2026-07-24 (no migration):** an UNSENT set also collapses when every outstanding card's current version was individually sent (`bundleChaseable` in `nudgeDecision.ts`) — before this, two individually-sent cards of an unsent set `suppressed_sibling` each other twice a day forever while In-follow-up read "Reminder 0 of N" (the Shard Global Ltd case); the sender now stamps `proof_sets.sent_at` on the first live bundle send, so the set is a normal sent bundle from then on. A card with no `last_reply_sent_at` on its current version (shell mid-build, unsent draft, or sent only via an untracked HS reply) still blocks the unsent-set path → suppression. The suppression still fires when a customer's due work spans >1 logical item (two projects, or a bundle + an unrelated card): `bundleId ?? proofId` is the item key, preserving one-touch-per-customer. Schema: `proof_nudges.proof_set_id` (nullable FK, the per-set cap counts sent/sending rows by it) + a `proof_nudges_bundle_daily` unique index + three `nudge_bundle` / `_2` / `_final` reply templates. Non-rep members of a collapsed bundle get an informational `folded_into_bundle` ledger row (skipped/dry, never counts toward a cap). The collapse + bundle cap/cooldown live in `groupSendables` / `decideForBundle` (`_shared/nudgeDecision.ts`, tested via `pnpm test:nudges`); the sender routes both per-card and bundle chases through one shared `processReminder`. Dashboard note: bundle members already show as **In-follow-up** (not Needs attention) via `proofs_in_follow_up()` Branch B, since they pass the pre-send guards — this migration makes that status *truthful* (they're now actually chased). Deferred (pre-existing gaps, not regressions): the dashboard's "Reminder N of M" count and the `nudges_exhausted` escalation are per-proof `rule_code IN (snv, vna)` reads, so they don't yet reflect bundle-`rule_code` sends. Author date 2026-07-15; ⚠ target the merged stock project, apply via MCP `apply_migration` / dashboard SQL editor (not `db push`), and redeploy `send-nudges` (function-first is order-safe: the `proof_set_id` column is additive and the templates fall back to code defaults).
- **000318–000324** — the **team-communication batch** (announcements banner + in-app team chat, then any-file attachments + direct messages; PR #472, applied to live via MCP `apply_migration` 2026-07-16/17). ⚠ Numbering gap: **000313–000316** (nudge reminder sequence, nudge-candidates currency, proof_view_source, order VAT treatment) aren't itemised above — `ls supabase/migrations/0003*`.
  - **000318** — **dashboard announcements banner** (admin-posted notices). `proofs.announcements` (tone `info`/`offer`/`warning`, `body`, optional `expires_at`, soft-remove `archived_at`, denormalised poster). RLS: everyone signed in reads; INSERT/UPDATE/DELETE admin-only via `proofs.is_admin()`. Poster name/initials/colour stamped by a SECURITY DEFINER BEFORE INSERT trigger (feedback-board idiom, 000271); full grant matrix. Self-expiring: the banner shows only rows within `[starts_at, coalesce(expires_at,'infinity'))` and not archived, filtered client-side (no realtime — dashboard reloads through the day). Frontend: `src/components/AnnouncementsBanner.tsx` + `AnnouncementModal.tsx`, mounted at the top of `DashboardPage`; admins get an inline "Post announcement" composer.
  - **000319** — **team chat** (one shared internal channel). `proofs.team_messages` (denormalised author, `body`, `created_at`). RLS: everyone reads + posts as themselves; delete-your-own or admin; no UPDATE (messages immutable). Author stamped by a SECURITY DEFINER trigger. Table joins the `supabase_realtime` publication — **schema-qualified `proofs.team_messages`** (verified live: the publication is per-table, existing proofs entry is `proofs.proof_version_views`). `profiles.team_chat_seen_at` (mirrors `feedback_seen_at`, 000281) drives the header unread badge. Frontend: the shared chat "engine" `src/lib/teamChatStore.tsx` (`TeamChatProvider`, mounted once in `App.tsx` around the routes) powers the header dropdown (`ChatMenu`), the `/chat` page, and the unread badge; `src/components/TeamChatPanel.tsx` is the shared body. Presence (online / idle-after-5min / away / busy + who's-around) rides the same channel via Supabase **presence** — live-only, no DB. Notification sounds are synthesised via Web Audio (`src/lib/chatSound.ts`, subtle blip / mention chime). Pin-to-keep-open + sound-on are localStorage prefs. See "Team chat" under Features built.
  - **000320** — **@mentions + a phone push when mentioned**. `proofs.team_messages.mentioned_user_ids uuid[]` (picked from the composer autocomplete; the push targets exactly who was picked, not fuzzy text). An AFTER INSERT trigger `notify_push_on_chat_mention` fires `send-push` (event_code `team_chat_mention`, same auth/pattern as the 000284 customer-action triggers, gated on `settings.push_enabled`) for the mentioned users minus the author. Widens the `notification_outbox` source_kind CHECK to allow `'chat'`; seeds the `team_chat_mention` copy into `settings.notification_copy`. `send-push` gained a `handleChatMention` branch (recipients EXPLICIT, not proof-ownership; respects account-wide mute + an explicit `team_chat_mention: off`; deep-links `/chat`) — redeployed via MCP (v5, `verify_jwt` stays false; the live bundle is `send-push/index.ts` + `_shared/push.ts`). Only mentions push — ordinary messages never do. Prereq: each recipient must install the PWA + allow notifications (the 000282–000285 push stack).
  - **000321** — **emoji reactions**. `proofs.team_message_reactions` (one row per message+user+emoji, unique; denormalised reactor `user_name`). RLS: read all, add/remove your own; no UPDATE. Author trigger; joins the realtime publication. Frontend: reaction pills + a hover emoji picker in `TeamChatPanel`, live via the shared engine.
  - **000322** — **chat image attachments**. `proofs.team_messages.attachment_paths text[]` + the private `chat-attachments` storage bucket (10 MB, png/jpeg/webp/gif; owner-insert on `{user_id}/…`, shared authenticated read, owner-or-admin delete — mirrors the feedback board's 000271 bucket). Frontend: paste / drop / pick images in the composer → upload → thumbnails + full-screen viewer (signed URLs); image-only messages allowed. ⚠ `TeamChatProvider.send` writes `attachment_paths` on every message, so this migration must precede the frontend deploy.
  - **000323** — **chat attachments widen to ANY file type** (quick internal file transfer: PDF / SVG / AI / fonts / zips). The `chat-attachments` bucket drops its mime allow-list (`allowed_mime_types = null`) and the cap rises 10 → 25 MB; `team_messages.attachment_files jsonb` records each attachment's original `{path,name,type,size}` so downloads carry the real filename (storage keys are random uuids; `attachment_paths` stays populated in lockstep for old rows). Frontend: paperclip button, images still preview inline, other files render as download chips (name + kind + size, `?download=` on the signed URL). Storage note: files persist indefinitely — deleting a message does NOT delete its files (Rob chose to leave cleanup for later; the Feedback board DOES clean up, so chat is the known exception). Applied to live via MCP 2026-07-17.
  - **000324** — **person-to-person direct messages**. `team_messages.recipient_id` (NULL = the shared room, so every pre-DM row is untouched; set = private to that one person; FK **ON DELETE CASCADE** — a deleted recipient's thread must die with them, since a nulled recipient would republish it to the room). RLS select rewritten to `recipient_id is null or author/recipient = auth.uid()` — **truly private incl. from admins** (Rob's explicit call, with push-on-every-DM and person-to-person-only). Reactions policies re-scoped to inherit message visibility (000321's read-all would leak private threads' existence). `team_chat_dm_reads (user_id, peer_id, seen_at)` carries per-thread read stamps (the room keeps `profiles.team_chat_seen_at`). `notify_push_on_chat_dm` AFTER INSERT trigger pushes EVERY DM via send-push event `team_chat_dm` (copy seeded; "Sent an attachment" fallback for image-only bodies); `notify_push_on_chat_mention` re-emitted to skip DM rows so a crafted mention array can't leak a private snippet to a third party. `send-push` gained the `team_chat_dm` branch (deploy function BEFORE the migration; frontend only AFTER — `send` writes `recipient_id` on every message). Frontend: thread-switcher pills in `TeamChatPanel` (Team + one per teammate, presence dot + per-thread unread), thread-aware unread/sounds/typing in the store, DM unread badging the header Chat pill coral like a mention.
- **000330** — **one colour per person, and chat bubbles tinted to match** (PR #514, applied to live via MCP `apply_migration` 2026-07-20). `profiles.designer_colour` is the identity colour behind someone's avatar, chips, feedback cards and now their chat bubbles, but nothing kept it unique: the palette had four colours for five staff, and `set_designer_presentation()` (000152) assigned new people by hashing their id across *three* of them — which is what put Alan Garland on Chris Jackson's `teal`. Three changes: (1) CHECK widened to seven (`gold`, `pink`, `cyan` added; the four existing names are untouched, since they're stored on live rows and denormalised onto thousands of message/feedback rows — note `teal` renders green and `coral` renders orange post-reskin, and the UI now labels them by what you actually see); (2) `set_designer_presentation()` assigns the first colour no *active* profile is using, falling back to the least-used rather than failing an insert if the team ever outgrows the palette — deliberately **not** a unique index, which would turn hiring the eighth person into a hard signup failure; it's now SECURITY DEFINER (it must read colleagues' rows, and profiles SELECT is self-or-admin only — same reason 000329 added `team_roster()`) with EXECUTE revoked per the 000182 pattern; (3) Alan moved to `gold`, and the **six** tables that denormalise an author's colour (`team_messages.author_colour`, `feedback_items` / `announcements` / `order_link_notes` / `watch_items` / `watch_updates`.`created_by_colour`) were realigned to their profile — colour is presentation, not attribution, so past rows follow a change; names and initials stay frozen. Frontend: the four-entry colour map that had been copy-pasted into five files collapses into `src/lib/designerColours.ts` (**add a colour there, not in five places** — a partial edit leaves someone rendering grey on one screen only, silently); chat bubbles carry a 10% `color-mix` wash of the author's colour (14% is the house strength for small chips, but a bubble is a much larger filled area) resolved from the live roster first and the frozen `author_colour` only as fallback, so future colour changes need no backfill; the Edit-profile picker offers all seven, greys out ones a teammate holds (naming who), and renders real avatar colours instead of the approximate Tailwind stand-ins it used before. ⚠ Numbering gap: **000325–000329** (`orders_seen_at`, `letterpress_tiers_to_5000`, `fix_profiles_update_policy_recursion`, `order_supplier_overs`, `team_roster_rpc`) aren't itemised in this list — `ls supabase/migrations/0003*`.
- **000335** — allow `image/png` in the `proof-images` storage bucket (PR #519, applied to live via MCP `apply_migration` 2026-07-21). The artwork QR scanner's suggestions are always PNGs (both the crop and the crisp rebuild render via `canvas.toBlob('image/png')`), but the bucket had been JPEG-only since 000032 (+ SVG since 000193), so accepting a suggestion made the version save fail with "mime type image/png is not supported" — the exact failure shape 000193 fixed for hosted-vCard SVGs. One-line allow-list addition; the artwork and manual-QR drop zones stay JPEG-only client-side, so the only PNGs reaching the bucket are the scanner's few-KB QR images. Frontend companion (same PR): the QR upload paths in New/EditVersionPage derive the storage path extension from the file's real type (`.png` for scan finds) instead of hard-coding `.jpg`. ⚠ Numbering gap: **000331–000334** (the direct order hand-off batch: Phase 0, shadow mode, `create-order-handoff`, size-aware resolver) aren't itemised in this list — `ls supabase/migrations/0003*`.
- **000336** — **artwork sanity check, Phase 1** (`docs/artwork-check-spec.md` — the build-status section there is the living record; PR #521). **Applied to live via MCP `apply_migration` 2026-07-21**, with `artwork-check` v1 deployed (verify_jwt TRUE) + `place-order` v29 redeployed the same day, both byte-verified; **`artwork_check_mode` = `shadow`** since then (`artwork_check_required` still false). Adds `orders.artwork_check` jsonb + `artwork_checked_at` + `artwork_check_verdict` (clear|flagged|error) and the rollout gates `settings.artwork_check_mode` (off|shadow|live) + `settings.artwork_check_required`. Backs the `artwork-check` edge function (advisory AI comparison of the Help Scout thread + QR contents vs the Dropbox print files, report stored on the order, card on `OrderReviewPage` when live) and the `place-order` confirm gate (409 `artwork_check_required` when required+live and no run recorded — only RUNNING is mandatory, the verdict stays advisory). First live run (order 403910, metal reorder) verified the whole pipeline in ~45s and settled the open question: **the Anthropic API reads Illustrator `.ai` files as PDF document blocks directly** — no rasteriser needed. Shared logic in `supabase/functions/_shared/artworkCheck/` (`pnpm test:artwork-check`); tune the allow-list in `prompts.ts` from shadow reports. Remaining rollout: review shadow reports → tune → mode `live` → later `artwork_check_required`.
- **000346** — **one artwork check at a time per order.** Order prep fires the check twice in parallel by design — linking the Dropbox folder trips the 000337 auto-run (`force:true`), and the review page auto-runs it seconds later — and with no report cached yet, both ran in full and wrote `orders.artwork_check` blind, so the LAST to finish won while the reviewer read the other one (live trace, order 403922, 2026-07-26: two full Opus passes over the print files, and the stored record wasn't the report the human signed off before placing the order). It surfaced as the per-flag **Investigate** button erroring "that flag is not on the current report", because the lookup resolves a flag against the STORED report by exact card label and the model rewords labels every run. Adds `orders.artwork_check_running_at`: the first caller claims it via a conditional UPDATE (Postgres serialises the row, so exactly one wins) and any second caller **waits for that run's report** rather than starting its own — including when a report is cached and even for a `force` request, since the report about to land is the fresh one. Report-store and claim-release are the same patch, so a waiter can't see one without the other; a claim older than 5 minutes counts as abandoned. Orders only (the pre-send proof check has no auto-run, so no structural race). Companion code fix, no migration: `resolveReportFlag` in `_shared/artworkCheck/investigate.ts` resolves a clicked flag onto the stored report tolerantly — exact label → normalised label (parentheticals, where the file name lands, dropped) → same recipient + same field when unambiguous, refusing rather than guessing — and a genuine miss returns the current report so the page swaps it in with a notice instead of dead-ending. ⚠ Deploy order: **migration first**, then `artwork-check` (the order select names the new column); the frontend is order-independent.
- **000356** — **Admin → Shipping stops chasing parcels that have already gone out** (PR #564, applied to live via MCP `apply_migration` 2026-07-27, verified live). The tab is a readiness worklist — post-payment orders still missing the address / destination / recipient name / phone / EU VAT id / card weight a parcel needs — but it had no idea whether the parcel had since shipped, so it kept flagging orders long after delivery: on live it listed 60, of which **49 had already gone to the customer** (17 delivered), and every one of the 49 was flagged "No phone number", the one detail that can no longer possibly matter. The real worklist was 11. Adds `proofs.admin_order_shipping_state()`, a zero-arg SECURITY DEFINER RPC returning `(order_id, shipped_at, delivered_at)` for post-payment orders whose Stock Control production is finished, joined on `proofs.orders.stock_order_number` → `public.orders.order_ref` (in-house) and `public.outsourced_orders.customer_order_ref` (outsourced, **outbound** leg). **The rule is ALL non-cancelled legs done, not any** — one order can have several legs (outsourced jobs split across supplier batches; the hybrid route puts one leg in EACH table, 7 on live), and live proves both failure modes: `403790` has 100 cards shipped with a 14-card top-up still at the supplier, and `403912` has a cancelled in-house leg beside an outsourced one still in production. A cancelled leg is excluded rather than counted as done; an order with no legs (or no `stock_order_number`) is never suppressed; `coalesce` guards keep a NULL status reading as "not done", since `bool_and` SKIPS nulls and an unknown leg would otherwise silently not count against the order. ⚠ **Never count `arrived_at` or `shipped_from_supplier_at`** — those are the INBOUND supplier→Plasma leg ("arrived" means arrived at *us*, customer still waiting; two live orders correctly stay on the worklist in exactly that state), and likewise `to_ship_at` (queued), `produced_at` (made, not packed) and the FedEx/DPD paperwork stamps (label printed, not collected). Presence of a row IS the shipped signal — `shipped_at` can legitimately be NULL on an in-house job finished by status alone (64 live rows predate Stock Control's shipping module), so never test the timestamp instead. SECURITY DEFINER because Stock Control's RLS gates `outsourced_orders` to stock support/admin/graphics: a proof-viewer admin with no Stock account resolves to `auth_role() = NULL` and would silently read "not shipped" for every outsourced order, putting all 60 back. Same shape as `admin_search_orders` (000263), which already joins these two tables — inline `proofs.profiles … role = 'admin' and deactivated_at is null` guard (not `is_admin()`, which doesn't check `deactivated_at`) and EXECUTE revoked from `anon, public`. That revoke is **load-bearing**: the `proofs` schema has no `pg_default_acl` entry for functions, so a new function defaults to EXECUTE TO PUBLIC, and `anon` holds schema USAGE with PostgREST serving it — omit it and the function is callable from the public internet. Zero-arg and joins OUTWARD from `proofs.orders`, so it can't be used to enumerate the ~57% of Stock jobs that never came from proof-viewer; returns dispatch timestamps only (no supplier, tracking number, deadline or notes). Frontend: shipped orders leave the "Needs details" worklist (a line states how many were set aside and where they went), stay visible under All paid orders with a single **Shipped** / **Delivered** pill + date, and drop their flag chips (the facts stay — "Phone: —" is still on the card); the edit modal warns that a shipped parcel can be corrected but **not redirected**; an RPC failure hides nothing and says why. Note **`delivered_at` is FedEx-only in practice** — DPD rows carry no delivery sync (all 59 live `delivered` rows are FedEx), so a UK parcel reads Shipped and stays there. ⚠ Numbering gap: **000347–000355** (`proof_annotations`, `ai_drafts_feedback_match_quality`, `ai_drafts_customer_message`, `ai_draft_proposal_kinds`, `ai_draft_briefing_version`, `public_get_prototype_prices`, `ai_draft_miner_enabled`, `ai_draft_version_stats`) aren't itemised in this list, and **000354 does not exist in source at all** — another reason never to infer the next free number from this doc; `ls supabase/migrations/0003*`.
- **000357/000358** — **artwork-check usage reporting** (PR #570, both applied to live via MCP `apply_migration` 2026-07-27, verified live; `artwork-check` redeployed to v20, byte-verified, `verify_jwt` stays true). Admin → Analytics gains an **Artwork checks** tab answering how often the check runs, by whom, and how often it finds something — none of which the stored data could answer. The check kept only its LATEST report per target (`orders.artwork_check` / `proof_versions.artwork_check`), so a re-run erased its predecessor, and `requireCaller` verified the caller's JWT then **discarded the user id**; `audit_log` held only settings changes, never runs. **000357** adds `proofs.artwork_check_runs`, an append-only ledger (one row per run, errors included — real usage and real spend, and hiding them would flatter the success rate), written by the edge function after the report is persisted and wrapped in try/catch so reporting can never cost a reviewer their report. The **cached-report and 000346 waiter paths bypass `finish()`**, so they correctly ledger nothing — only actual runs count. **`source` is the load-bearing column**, because only one of the three ways a check starts is somebody *using* the feature: a human clicking Run (`designer`), the order review page auto-running on open (`auto_page`), or the 000337 Dropbox-folder trigger (`auto_folder_link`). ⚠ The middle one is the trap — it arrives on the **designer's own JWT**, so without a marker every review-page visit would read as deliberate use; the client now sends `trigger: 'auto'` and the trigger sends `trigger: 'auto_folder_link'` (that migration also re-emits `notify_artwork_check_on_folder_link()` verbatim plus the one body field). `ran_by` is stamped for `auto_page` too, so page-opens stay *separable* rather than lost, and the per-person table counts clicks only. The 56 pre-ledger runs are backfilled as `source='unknown'` with no person (one row per target — earlier re-runs are simply gone); their flag/defect counts were verified to agree with all 56 stored verdicts. **000358** adds `analytics_artwork_check(p_days)` (one json round trip, house pattern: `language sql`, stable, SECURITY INVOKER, EXECUTE revoked from anon/public). ⚠ Pre-send uptake is measured from the **first pre-send check ever run**, not the window edge — versions created before the feature existed could never have been checked, and counting them read **19/421 (4%)** where the honest figure is **14/49 (29%)**. The order check's own coverage is deliberately NOT reported: it auto-runs and gates `place-order`, so it is ~100% by construction. Deploy order is free (the ledger insert just logs until the table exists; the tab explains itself until the function exists).

- **000362** — **annotation usage reporting** (⚠ authored but **NOT yet applied to live** — apply via MCP `apply_migration` / the dashboard SQL editor per the house workflow). The 000347 annotation feature shipped live with no usage surface, so "is anyone using this?" could only be answered by querying `proof_annotations` by hand. Adds `analytics_annotations(p_days)` (same house pattern as 000358: one json round trip, `language sql`, stable, SECURITY INVOKER, EXECUTE revoked from anon/public) and an **Annotations** tab on Admin → Analytics. **The two halves are never averaged** — a designer callout and a customer pin are different acts by different people with different denominators — so each gets its own volume, spread and uptake rate. Uptake counts from the **first observed use of that half**, not the window edge, per the 000358 lesson (work predating the feature could never have used it). ⚠ Two traps found while building it, both worth keeping straight: (1) the pin denominator counts **change-request events**, so its start point must anchor on the *event* clock — `proof-action` writes the event first and stamps each pin a moment later, so a `>= first pin` cutoff drops the very request that carried the first pin from numerator AND denominator (read 2/13 against live where the truth is 3/14); (2) the callout rate is a **cohort** figure about *new* versions and deliberately won't match the raw annotated-version count, because a designer usually annotates a version that already exists and those sit outside both sides of the fraction. Resolve stats cover customer pins only (a designer's own callout isn't a task). No fixture is hardcoded out of the counts — an exclusion list baked into a migration goes stale silently and starts hiding real customers — so distinct-proof counts are reported alongside raw totals to make early concentration visible instead. Deploy order is free (the tab explains itself until the function exists).

- **000363** — **checks-per-day on Admin → Analytics → Artwork checks** (applied to live via MCP `apply_migration` 2026-07-28, verified). The weekly bars answer "is this used at all" but not "what does a normal day look like" — four bars over a 30-day window can't tell a busy afternoon from a fortnight of steady work. Adds a `daily` key to `analytics_artwork_check`; the panel becomes a Day/Week toggle (defaulting to Day, falling back to Week with the toggle hidden if the key is absent, so a frontend deploy ahead of the migration is safe). ⚠ Re-emitted from the **live** `pg_get_functiondef`, not from 000358 — 000360 had already added `spend`, and rebuilding from the older file would have silently dropped the cost panel's data (the same trap 000347's header documents for `public_settings()`); verified after apply that `spend`/`cost`/`proof_adoption` all survived and that `daily`, `weekly` and `totals.runs` reconcile to the same 91. Two deliberate choices in the new block: **empty days are emitted as zeros** via `generate_series` (a present-days-only `group by` closes real gaps — live data has a dead 14–20 July week that would simply have vanished, with the axis itself doing the lying; the `weekly` block keeps its original shape since at 4–13 bars a missing week is obvious), and **days are London days** via `at time zone 'Europe/London'` on both the runs and the series bounds, so a 17:30 BST run doesn't land in tomorrow's bar. ⚠ `left join` means `count(r.id)`, never `count(*)` — the latter counts the generated day row and reports every empty day as 1 run.

- **000364** — **"same as last time" guidance for returning customers** (**fully live 2026-07-28**: migration applied via MCP `apply_migration` (`order_previous_spec`) and verified — column present, RPC ACL authenticated-only, smoke-tested on live data; `create-order` redeployed via MCP to v47, all seven bundle files byte-verified against source, `verify_jwt` stays false per its requireDesigner in-body auth; frontend merged to `main` in PR #589). The returning-customer conundrum — lock the pay link to their old thickness/finish and you stop them choosing differently; leave it open and they may accidentally order a different spec — is resolved as guided-open: the choosers stay forced-choice, but the option they had last time carries a **"Your last order · March 2022"** badge (brand-toned; the green catalogue "Most popular" stands down while it shows, so the page never nudges a returning customer off their own history), picking a different option shows a calm "Just so you know…" note, an option that isn't among this order's choices gets a plain "isn’t available on this order" explainer (deliberately NOT "we no longer offer" — the chooser is scoped to the proof's tabs/currency, so the old option may be absent here while still in the catalogue), and an open quantity shows "Last time you ordered 500". Two schema pieces: `orders.previous_spec` jsonb — **customer-visible by construction** (both pay-page RPCs return the whole row via `to_jsonb(o) - 'token'`, which is also why NO view/RPC re-emit is needed), so only display-safe fields live in it (`{variant_id, variant_label, option_id, option_label, quantity, label, source}`, labels frozen so a later-retired variant can still be named); and `last_paid_order_for_proof(p_proof_id, p_material_id)` — the order builder's auto-suggest, cloning 000275's company-else-contact matching, `status in (paid, fulfilled)`, **production orders only** (a prototype is a sample; a PARTIAL reprint stores only the faulty subset as its quantity and is born paid with a fresh `paid_at`, so it would shadow the genuine order — which is still in the table as the honest record), and material matched via `coalesce(o.material_id, mv.material_id)` because 37 live paid orders (2026-07-28 count) predate the 000298 `material_id` stamp (SECURITY INVOKER; the explicit `revoke from public, anon` is load-bearing per the 000356 lesson). `create-order` sanitises the payload through `_shared/previousSpec.ts` (allow-list, degrade-to-null-never-400, stored only on online production orders). Frontend: the builder's "Their last order" section (auto-suggest card with one-click apply + manual entry for history that only lives in old Help Scout threads / Xero invoices), matching rules + all customer copy in `src/lib/previousSpec.ts` (`pnpm test:previous-spec`, which also parity-tests the client parser against the server sanitiser), badges/notes wired into BOTH pay pages (the thickness card JSX is duplicated across them — patch both) and `FinishChoiceCard` gained an optional `badge` prop. Display guidance only — pricing, production, and invoicing are untouched.

- **000365** — **re-seal the pay-page RPCs + revive customer order tracking** (applied to live via MCP `apply_migration` 2026-07-28 and verified; DB-only, no frontend change). Migration 000309 had re-emitted `public_get_order` as a bare `to_jsonb(o) - 'token'` (and created `public_get_order_group` the same way), silently dropping BOTH protections from 000265/000268: the internal-field strip list (pay-link holders could read `stock_order_number`, `supplier_*`, `dropbox_folder_url`, the full `artwork_check` report, Xero bindings/errors, staff UUIDs, and every internal column added since) and the `tracking_projection` key (the pay page's tracking panel went silently blank even though `customer_tracking_enabled` is ON — the 000269 `_project_order_tracking` helper survived intact, uncalled). Fix: the strip list now lives in ONE shared helper, **`proofs._customer_order_json(proofs.orders)`** (STABLE, EXECUTE revoked from public/anon/authenticated), that BOTH RPCs serialise through — 32 stripped columns incl. everything since 000268 plus `ship_to_email`/`ship_to_phone` (rendered nowhere on the pay pages) and `card_discount_reason`; `order_spec_snapshot` gets a sub-strip of its `contact`/`company`/`captured_by` keys (the group page reads only material/variant/finish/quantity labels), gated on `jsonb_typeof = 'object'` so a poisoned snapshot degrades to null instead of erroring the RPC. `public_get_order` returns to the 000268 plpgsql shape + 000309's `order_group_status`; the group RPC keeps its member ordering + `company_name` byte-identical. Verified pre-apply by executing the exact expressions read-only against live rows (before−after key diff = exactly the strip list; after−before = exactly `tracking_projection`; real stages `paid`/`in_production`/`on_its_way` returned) and by an independent consumed-fields cross-check of the ONLY two consumers (OrderPayPage, OrderGroupPayPage — `previous_spec` and the snapshot labels explicitly preserved). ⚠ See the **Re-emitting the pay-page RPCs** rule under Claude operating discipline; it is also stamped as `comment on function` on all three live objects.

- **000366** — **optionally tell the customer when a project is closed off** (applied to live via MCP `apply_migration` 2026-07-30 and verified — the seeded body's md5 matches `DEFAULT_BODIES.proof_abandoned` byte for byte). Abandoning has been completely silent since 000022 introduced the status: `handleAbandon` wrote one row and showed the designer a toast, and the only way a customer found out was by returning to their own link and reading the "This proof is closed" card. Silence stays the **default** — most abandons are ghosted leads or a designer tidying up, where an unsolicited "we've closed your project" email does more harm than good — but the abandon dialog now carries an **unticked** "Let *name* know" box that reveals an editable message. Seeds one reply template, `proof_abandoned` ("Project closed"). ⚠ Its copy is written to sit alongside the customer-facing `AbandonedScreen`, which says only "This proof is closed": so no reason, nothing final-sounding, no "cancelled/declined/expired", no promise to follow up (the page's contract is customer-initiated), and deliberately **no `{url}`** — the link still loads and the visit is still recorded, but the page shows a closed card with no artwork on it, so pointing someone back at it is a dead end. `pnpm test:abandon-notice` pins those constraints, plus the seed ↔ `DEFAULT_BODIES` byte-identity that nothing else in the template system guards. Two structural notes: the id is `proof_`-prefixed to match the codebase's own vocabulary (`proof.abandoned` audit action, `proof_abandoned` timeline milestone), so `AdminTemplatesSection` carries an exact-id override — the same trick `order_cancelled` / `order_revision` use — routing it to a new **"Project messages"** group with a new `project_lifecycle` variable scope (`first_name`, `company` only); and `send-helpscout-reply` gained an optional **`hs_status`** field (defaults to today's `'pending'`, so every existing caller and either deploy order is a no-op) which the notice sets to `'closed'` — a closing note ends the exchange, and leaving the thread Pending would drop the project straight back into the chase queue the designer just took it out of. Help Scout reopens it if the customer replies, which is exactly what the message invites. Send order is **the status flip first, then a best-effort notice** — the same order `order-lifecycle` uses server-side for the identical decision, and deliberately NOT the reverse. Sending first reads more careful and was the first cut, but it buys two worse failures: "we told the customer we closed it and then didn't" is a lie needing a correcting email, and guarding the retry against a double-send needs a record of the send that an in-memory ref can't provide — a page refresh (the natural reaction to an error) destroys it, so the second attempt emails a duplicate closure notice. Flipping first has neither: the status is its own durable record (an abandoned proof can't reach the dialog again, since Abandon is hidden while locked), so the send happens at most once per confirm, and "closed but the note didn't go" is honest and fixable by hand. The dialog switches to a **retry-the-message-only** state, keyed off a **proof-tagged** ref — tagged because `/proofs/:id` has no `key` on its Route, so React reuses the component across the header's prev/next worklist arrows and a bare flag would leak onto the next project. Two smaller traps worth keeping straight: `thread_id: 0` means "sent but the header wasn't parseable", **not** failure (treating it as failure tells the designer to re-send a message the customer already has — the house reading everywhere else), and `supabase.functions.invoke` leaves `data` null on any non-2xx, so the function's real error is only reachable via `error.context` — `src/lib/edgeError.ts` (lifted out of `MessageSendPanel`, where it was a fourth private copy) does that, and without it every distinct failure reads as "Edge Function returned a non-2xx status code". The audit row gains `customer_notified` (+ the thread id), the part of the decision that can't be reconstructed from the row later. The notify option disables itself with a reason when it can't work — replies paused, no linked conversation, or no proof version to anchor the reply to (`send-helpscout-reply` hard-requires a `version_id`, and a lead that never got artwork is exactly the population most likely to be closed off). Dialog: `src/components/AbandonProjectDialog.tsx`, rules in `src/lib/abandonNotice.ts`, verify-harness rig at `?path=/abandon-dialog`. **Deliberately out of scope** (pre-existing, latent, zero live instances — see below): abandon still has no order guard.

- **000370/000371** — **the proof page says where the order has got to, and the tracking rail can finally finish** (both applied to live via MCP `apply_migration` 2026-07-30 and verified against live data). **000370** revives the `delivered` stage that 000269 removed as unreachable — Stock Control now syncs the OUTBOUND parcel on both routes (`public.orders.customer_delivered_at`, `public.outsourced_orders.customer_delivered_at`), so the signal 000269 lacked exists. ⚠ But delivery sync is **carrier-dependent and the split is stark**: FedEx has stamped 65 of its 70 shipped parcels, DPD **0 of 122, ever**. DPD is domestic UK and roughly two thirds of volume, so simply emitting `delivered` would have parked most customers under a fourth step that could never light up — worse than the uniform "nobody sees step 4" it replaced, because the failure would now look personal. So the projection also returns **`delivery_tracked`** (from the new `proofs._carrier_reports_delivery(text)` allow-list, EXECUTE revoked per the 000356 lesson) and the rail draws only the steps it can reach: **four for FedEx, three for DPD**, each ending somewhere the journey can actually finish. Absent before dispatch (no carrier yet) = "unknown", which shows the full journey; an already-`delivered` stage forces the long form regardless, because the stamp is evidence and the allow-list is only a rule of thumb. ⚠ **The "on its way" triggers were deliberately NOT narrowed** to the newer `customer_shipped_at`: 116 in-house orders are `completed_at` but only 52 carry a shipped stamp (the shipping module postdates most of them), so switching would drag 64 orders BACKWARDS from "On its way" to "In production" on a page their customers may already have seen. Read-only preview before apply, and re-verified after: 21 orders move to Delivered, 63 DPD orders get a completing 3-step bar, **zero regress**. **000371** then adds `stage` + `delivery_tracked` to `public_get_proof_order_state`, so `/p/:id` shows the same journey the pay page does — reversing 000367's "say nothing when paid", which held for a customer who still has their pay link but not for the one who deleted it, and `/p/:id` is the URL we put in every proof email and that customers bookmark. ⚠ **Two named keys only, never `|| proj`** — the granular **tracking number is deliberately withheld** from `/p/:id`: the pay page is reached with a bearer token posted to the project contact, while the proof link is handed around by team sharing ("a convenience, not security"), and a tracking number is the one field in the projection that lets its holder *act on* the parcel (most carriers allow a redirect) rather than merely read about it. Everything else 000367 refused stays refused. Verified pre-apply across all 172 proofs with orders: stripping the two new keys reproduced the live output exactly (0 rows differing) and 0 payloads carried a tracking number. Admin's tracking master switch gates both surfaces for free, since the projection returns `level: 'off'` and this reads the stage only when it's broad/granular. Frontend: stage logic shared in `src/lib/orderTracking.ts` (`pnpm test:order-tracking`) so the pay page's full rail and the proof page's compact strip can't disagree about the journey while differing in layout; `src/components/OrderProgressStrip.tsx` + `ApprovedOrderStatus.tsx` render the footer **on the approved card** so the page has ONE place answering "where is this", with the "Ready to order?" panel by the price grid still answering the different question "how do I buy these?" (neither repeats the other — notably the card deliberately omits the link expiry the panel already prints next to the button that acts on it). Harness rig: `?path=/approved-order-status`. Same PR moved the customer-page approved signal off its cyan — see the Design system note.

- **000372/000373/000374** — **the customer can ask for more of the cards they already bought** (all three applied to live via MCP `apply_migration` 2026-07-31 and verified; spec `docs/customer-reorder-spec.md`). A repeat order used to start with the customer remembering to email us, so the orders that never got placed were the emails that never got written — and since 000371 `/p/:id` is a page they have a real reason to return to. **000372** adds the ask: `settings.reorder_enabled` (default false) + two quiet-window settings, four `proofs.proofs` columns (`reorder_requested_at`, `reorder_request_note`, `reorder_request_quantity`, `reorder_of_proof_id`), the `_reorder_quiet_period_passed(text)` helper, the service-role-only `claim_reorder_request(...)` (cooldown in the UPDATE's own WHERE clause — the 000346 atomic-claim pattern, so Postgres serialises callers and exactly one wins), and `reorder_available` / `reorder_requested_at` on `public_get_proof_order_state`. ⚠ **The quiet window measures delivery where it can and dispatch where it can't** — a "delivered + 14 days" rule alone would never fire for a UK customer, because DPD has never reported a delivery (000370), which is two thirds of volume; the dispatch fallback is what makes the feature exist outside FedEx. It is a **request, never a purchase**: `/p/:id` is handed around by team sharing ("a convenience, not security"), so self-serve checkout would let anyone ever sent the artwork spend the company's money — the same refusal 000367 made about the pay token, applied to the more consequential action. The `request-reorder` edge function (anon, honeypot, `COOLDOWN_HOURS = 24`, rechecks eligibility server-side via the same RPC the page reads) opens a **NEW Customer Support conversation** rather than reusing the proof's thread, because Help Scout locks threads after inactivity and a reorder is its own project anyway. ⚠ **Throttled, ineligible and unknown-proof all return the same `{status:'ok'}` a winner does** — differing responses would make the endpoint an oracle for which proofs carry paid orders. **000373** makes the request visible to designers: a threshold-less `reorder_requested` needs-attention rule on the SOURCE proof (priority 1, winning the alphabetical tiebreak against `request_changes_no_version`; every other rule measures OUR silence, this one measures a customer trying to buy, so there is no honest number of days to wait) plus `reorder_of_proof_id` / `reorder_requested_at` appended to `public_dashboard_projects`. ⚠ The rule's clear condition is **the CHILD proof existing**, not anything done to the source row — so a reorder raised by plain Duplicate (which never writes `reorder_of_proof_id`) leaves the flag up forever; the designer-side action that stamps it is **"Raise the reorder"** (proof-detail overflow menu + the needs-attention popover's primary slot; `duplicateProof(..., { raiseReorder: true })`), which is also the only path that pre-approves correctly. Three things it does that a plain Duplicate cannot: it **carries the source's approved `proof_name_approvals`** onto the new v1 with `carried_from_version_id = <source current version>` — load-bearing twice over, since that records the honest provenance AND suppresses the finalise trigger (live guard `WHEN (state='approved' AND carried_from_version_id IS NULL)`); it sets `status='approved'` **in the INSERT, never by a later UPDATE**, because `proofs` has no INSERT trigger but does have `notify_push_on_proof_approved AFTER UPDATE OF status` (a flip would push "proof approved" to every phone for an approval the customer never gave); and it stamps `reorder_of_proof_id` **last**, as the commit point — the rule clears on bare child existence and can never re-fire, so an early stamp plus a crash would clear the request forever against a half-built project. ⚠ Two rules: **pre-approve only when the source IS approved and has rows to carry** — both halves matter and for different reasons. The row count catches a source that is `approved` with zero approval rows (designer override, or a variant round whose slots the finaliser bails on per 000141), which would otherwise mint a child with an empty Approved-artwork table and ZIP. The **status check protects production**: `src/lib/approvedArtwork.ts` (the order-side hand-off) takes the current version's non-QR images WHOLESALE and states the invariant it relies on — "a proof reaches 'approved' only once every required slot of the CURRENT version is signed off". Every other route to approved upholds it (the finaliser demands a complete set; `handleApprove` writes a row per missing slot), but this path could not, because carried rows deliberately suppress the finaliser: reopen a source (which deletes all approvals, 000158), re-approve only one of two recipients, and a partial carry would mint a fully-approved child whose production ZIP contains the *unapproved* person's card. Requiring the source to be approved restores the invariant, since here "approved AND ≥1 row" IS "complete slot set". Both declines leave the reorder OPEN with a toast saying so; and **Set-collection slot keys are layout ids**, remapped through the fresh layout id map by `duplicateApprovalInsert` (`pnpm test:duplicate`), with an unmappable key rolling the whole reorder back instead of shipping a key that matches no image. Note nothing ever clears `reorder_requested_at`, so the source page reads its own child (`reorder_of_proof_id = <source>`) on load: that both hides the button once its job is done and renders the "Reorder raised — open it" link, keeping the pair symmetric with the child's "Open the original project". Deliberately a chip and not a stat tile: a tile costs seven coordinated edits whose expensive one is a client-side click-through predicate mirroring the SQL, which is the entire history of 000245/000246/000279 drifting apart. **000374** adds `reorder_proof_id` so the old page points forward ("you asked for a repeat of these on 20 July — open your reorder"), stopping the bookmark going stale. ⚠ The child is named **only once approved or sent** — a designer raising a reorder may be mid-build for hours, and a proof id IS a capability (`/p/` has no token), so the two guards are what keep the grant narrow enough to be honest. Frontend: `src/components/ReorderPanel.tsx` (panel + `ReorderForwardPanel`, mutually exclusive by construction — `canRequestReorder` goes false the moment a link exists), copy + gates in `src/lib/proofOrderState.ts` (`npx tsx src/lib/proofOrderState.test.ts`, which also parity-checks the client cooldown against the edge function's constant), dashboard `ReorderLine`, harness rig `?path=/reorder-panel`. ⚠ `settings.reorder_enabled` is **ON** on live as of 2026-07-31, so merging the frontend ships the panel to real customers immediately (21 proofs currently eligible); flip the column to turn it off, no redeploy either way.

- **000375** — **a shipped parcel stops claiming it is still travelling** (applied to live via MCP `apply_migration` 2026-07-31 and verified). A DPD parcel's journey ENDS at `on_its_way` — the carrier has never once reported a delivery (000370: 0 of 122) — so the rail correctly draws three steps and completes, but the copy was written for a parcel in flight: *"Your cards are on their way."* Weeks later that is false, and the reader is usually someone who came back **because** the cards arrived and they want more (the 000372 reorder population), so present tense reads as though we have lost an order they are holding. Adds `shipped_at` to `_project_order_tracking` and as an explicit third named key on `public_get_proof_order_state` (⚠ still named keys only, never `|| proj` — 000371's rule). ⚠ **The REAL dispatch stamp only** — in-house `public.orders.customer_shipped_at`, outsourced `public.outsourced_orders.shipped_to_customer_at` — never in-house `completed_at`, which is what *moves* the stage but means MADE, not posted; printing it as "shipped on" would hand a customer a specific date for an event we never recorded. A missing stamp drops the date, not the past tense. Exposing it is consistent with 000367's "fact, not capability": a date is inert, unlike the tracking number 000371 withheld because its holder can redirect the parcel. One change serves both surfaces — the pay page reads the whole projection via `public_get_order`'s `tracking_projection`. Frontend: `stageLabel()` / `stageLine()` in `src/lib/orderTracking.ts` (`pnpm test:order-tracking`) diverge for exactly one case — `on_its_way` with `delivery_tracked = false` becomes label **"Shipped"** and *"Your cards were shipped on 24 June 2026."*, and the shortened rail's last step is relabelled to match, because "On its way" as a terminal step contradicts the sentence under it. A tracked-carrier parcel is untouched (it really is travelling, and Delivered follows), and a `delivered` stamp always wins over the allow-list. Verified pre-apply and after: stripping the new key reproduced both function outputs byte-for-byte across all 173 proofs with orders; 95 orders now carry a date and 0 of the 68 terminal-DPD ones lack one.

⚠ Two adjacent gaps found while building 000366 and **not** fixed by it, both latent today: (1) **abandon has no order guard.** Abandon is hidden while a proof is approved and an order can only exist on an approved proof, so the natural route forces you through the order-aware Reopen first — but `reopenOrder` excludes `revision` orders, so after reopening a *paid* order for revision the proof is `in_progress` with a `revision` order holding the customer's money and "Abandon project" reappears with copy that mentions neither. `place-order` then refuses ("the proof isn't approved"), stranding a paid order silently; a `sent` link stays payable and `send-order-reminders` never reads proof status, so payment chases continue. Live: all 8 orders on abandoned proofs are `cancelled`, so nothing is broken today. (2) **`helpscout_follow_up_tag` doesn't exclude abandoned proofs** — the `hs` CTE in `proofs_needing_attention()` has no status filter at all (every other rule has one). 9 abandoned proofs currently carry the tag; they're masked only because that rule is disabled. Enable it and they land in Needs attention. `reconcile-helpscout-tags`' never-synced backfill has no status filter either, so supply isn't closed. This also makes `docs/followup-automation-spec.md`'s "status `abandoned` already removes a proof from all chase rules" not quite true.

`seed.sql` (applied via 000009's pricing rebuild) contains roughly 16,000 price-tier rows sourced from the per-currency Pricing CSVs in Rob's Dropbox (`mnt/Pricing`). The generic `add_ons` / `add_on_prices` tables are still present but largely superseded by `material_options` for anything that behaves as a switchable dimension on a proof.

## Pricing schema

Core tables:

- `materials` — one row per product family. Has `code` (unique), `display_name`, `category`, per-currency split-name tooling surcharges, `featured_quantities` (which qty tiers show by default), `disclaimer`, and two behaviour flags:
  - `requires_ink_names` (000024) — if true, the version form shows N labelled ink fields where N = selected variant's `ink_count`, all required.
  - `option_label` (000025) — singular form like "Finish", "Species", "Cutting". UI pluralises heuristically (`src/lib/labels.ts`).
- `material_variants` — thickness / ink_count / finish per material. `variant_type` is a discriminator: `thickness` | `ink_count` | `finish` | `default`. Has `ink_count` (used by requires_ink_names path).
- `price_tiers` — keyed by `(material_variant_id, currency, quantity)`, with `total_price` and `unit_price`.
- `material_options` — renamed from `finishes` in 000025. Second dimension beyond variant; one row per option per material. `code`, `display_name`, `is_base`, `sort_order`.
- `material_option_surcharges` — renamed from `finish_surcharges`. `(material_option_id, currency, quantity) → surcharge`. Optional: a base option with no surcharge rows (e.g. Natural, Without cutting, Black Walnut) means "use the price grid as-is".
- `add_ons` / `add_on_prices` — legacy, kept for catalogue entries without pricing data. Not used by the form.

`proof_versions` has `material_id`, `material_options text[]` (which options this version offers), `currency`, `pricing_snapshot` (JSON of base prices at creation time), `custom_quote`, `finishes` (old column dropped — see 000025 rename).

`proof_version_images` has `material_option text | null` (which option tab this image belongs to — null = shown across all options / pre-migration data) and `original_filename text | null`.

**Key rule:** `variant_type` is the single variant-dimension discriminator. `material_options` is a separate dimension that any material can use. No material uses more than one variant_type simultaneously, so always filter variants by the material's `variant_type`.

## Public views

Customer-facing queries all go through `public_*` views so RLS can stay strict:

- `public_proofs` — proofs + contact/company joined, exposes status + approved_at + abandoned_at
- `public_proof_versions` — includes `material_options`, `option_label`, `custom_quote`, `material_disclaimer`, `featured_quantities`
- `public_proof_version_images` — includes `material_option`, `original_filename`
- `public_site_settings` — singleton row with `global_disclaimer`
- `public_material_options` / `public_material_option_surcharges` — option metadata + surcharges

Whenever a migration adds or renames a column on an underlying table, the relevant view must be dropped and recreated (PostgreSQL's `create or replace view` doesn't allow column reorder — append-only or drop+create).

## Data rules baked into seed.sql

- GBP prices are VAT-inclusive. EUR and USD prices are VAT-free.
- No interpolation between listed quantity tiers. The UI must constrain the quantity picker to values present in `price_tiers` for the chosen variant.
- Standard Paper is modelled as three finish variants (`standard`, `uv_spot`, `foiling`), not a base price plus additive add-ons, because the source CSV columns replace the base price rather than adding to it.
- USD Copper is seeded from Gun Metal USD pricing (identical thicknesses and quantity tiers).
- CMYK is included at no extra charge. The obsolete "with CMYK" CSV sheets are ignored.
- Split-name tooling surcharges live on `materials` (not on variants), per currency, per extra name beyond the first.
- Metal finish surcharges (Steel + Gold, Brushed/Mirror): identical 39-tier schedule across all three currencies. Seeded in 000020.
- Wood species are all priced identically (no surcharge rows). Choice of base is arbitrary — Black Walnut picked.
- Carbon fibre CNC cutting surcharges seeded per-currency in 000026.

## Not yet seeded

- **Letterpress edge gilding** — add-on row exists, no prices.
- **Engraving, edge colour, die-cut shape** — no CSV pricing exists at all. Schema can accept them later.

## Features built

Customer-facing (`/p/:proofId`):

- Approval banner, abandoned screen, 404 screen (quiet, not jarring)
- Version tabs with "Current" / "Approved" badge
- Option switcher (Finish / Species / Cutting — label from material.option_label)
- Image grid (single, dual, responsive) with lightbox; images filtered by active option tab
- Spec summary (material, option, inks) + dynamic option label
- Change notes
- Global + per-material disclaimers
- Pricing card: grid with featured-quantity default and "Show all quantities" expand row. Surcharges baked into cells for non-base option tabs. Custom-quote replaces grid with a quiet message. Footer prepends "Prices include VAT." for GBP.
- "+from £X" suffix on non-base option tabs (suppressed for materials without surcharges like wood).
- Thickness guide (`MetalThicknessPanel` — a text list, not the SVG diagram it once was) — shown for materials with a real thickness choice, gated via `hasThicknessGuide()` in `src/lib/metalThicknessNotes.ts` on the `material_code` field added in 000177. Standard metals (steel, gold, gun metal, copper, titanium, matte black, matte white) show 300/500/800μm; Mini Steel shows 200/300/500μm with its own copy; Full Colour Plastic shows 420/680/760μm with its own copy AND its own intro line (`full_colour_plastic` / `full_colour_plastic_intro` keys in the `settings.metal_thickness_notes` JSONB — code defaults apply until the first admin save persists them). The same sets feed the open-spec checkout thickness cards (see Ordering & checkout).
- **Pay page** (`/order/:id`, `OrderPayPage`) — embedded Stripe Elements checkout for an approved-proof order: artwork recap, itemised cost breakdown, US tariff opt-out, and a self-serve VAT-invoice link. See **Ordering & checkout**.
- **Combined pay page** (`/order/group/:id`, `OrderGroupPayPage`) — ONE checkout over every order in a payment group: a line per card, per-card guided choosers for open specs (with the customer's own artwork cross-fading to the picked finish), one combined shipping figure, one US-tariff line, one total, one payment. See **Combined payments** under Ordering & checkout.
- **Team sharing** (migration 000304, `docs/team-sharing-feature.md`) — on multi-recipient proofs with the per-version toggle on: the collapsed "Share with your team" bar (per-name copy/email/native-share links + approval progress) and the `?for=<name>` personal-link focus treatment (greeting strip, brand ring, one-time scroll, pre-filled approve name).
- **Bundle review front door** (`/bundle/:id?token=…`, `SetReviewPage`, migration 000311 — bundle orders Slice 3; `/set/:id` kept as an alias) — one link to review a whole BUNDLE of cards (typically one card per material). ⚠ Naming (10 Jul): user-facing vocabulary is **bundle** everywhere — settles the spec §8 naming question ("set" collided with the wizard's Set shape vocabulary); code/DB identifiers keep `proof_sets`. A pure review hub: each card listed with preview/recipient/status linking into its existing `/p/:id` page and a "Your progress" card (*X of N approved*, per-card checklist, warm all-approved state). Card links carry `?bundle=<setId>&bundle_token=<token>` (`customerProofPathFromBundle`) so `/p/:id` shows a "Back to your bundle" bar under the masthead plus a post-approve continue link — presentation-only, and deliberately URL-carried rather than server-derived so a lone card's `/p/` link can never reach the bundle (the params only exist for visitors who came through the token-gated front door). **Deliberately NO pricing and NO order/payment/delivery language** — pricing lives on each card's own proof page; payment is Slice 2, sent later and separately. ⚠ The self-serve per-card discard ("Decide against this card?") was **removed 10 Jul** (Rob: it read as "reject this version" and invited misuse for design feedback) — a dropped card is handled by the designer abandoning that project, which the page renders as "Set aside". The plumbing stays dormant: `set_discarded_at`, proof-feedback's `set_discard` mode, `DeclineFeedbackPanel`'s `setDiscard` prop, and the workspace's discard-reason display + Restore.

Designer-facing:

- `/login` — Supabase auth
- `/` — dashboard: company/contact-grouped list, date/name sort, show/hide dormant, status pills (in_progress/approved/dormant/abandoned), "+" shortcut buttons on company headers (always visible, hidden for "No company") and contact rows (hover/focus-visible) that pre-fill the new-proof form
- **Dashboard announcements banner** (migration 000318) — an admin-posted notice strip at the top of `DashboardPage` (`src/components/AnnouncementsBanner.tsx` + `AnnouncementModal.tsx`). Admins compose from an inline "Post announcement" button (message + style `info`/`offer`/`warning` + optional end date); everyone sees the live notices as tinted, tone-coloured cards. Notices self-expire on their end date (no cleanup) and an admin can remove one early (soft-archive). Fetch-on-mount, not realtime.
- **Team chat** (migrations 000319–000324, 000330) — the shared internal channel plus person-to-person DMs, built on a single shared "engine" (`src/lib/teamChatStore.tsx` `TeamChatProvider`, mounted once in `App.tsx`) so the header unread badge, the desktop dropdown (`src/components/ChatMenu.tsx`), the dashboard-rail dock, and the full `/chat` page all read the same live state over one realtime connection; `src/components/TeamChatPanel.tsx` is the shared body (`src/lib/teamChat.ts` / `chatSound.ts` are the pure helpers). Features: live messages, day dividers + author grouping, delete-your-own (admin any), clickable links; **presence** (online / auto-idle after 5 min / away / busy + a set-your-own-status picker + "who's around" strip) via Supabase presence — live-only, no DB; **unread badge** (`profiles.team_chat_seen_at` + per-DM `team_chat_dm_reads`); **pin-to-keep-open** across pages + **notification sounds** (Web Audio subtle blip / mention+DM chime, mutable) — both localStorage prefs; **@mentions** (autocomplete picker) → a phone push to the mentioned person only (000320); **typing indicators** (channel broadcast, thread-scoped); **message search** (full-history — a debounced server search per thread merges matches older than the loaded window into the results); **history pager** ("Show earlier messages" fetches per-thread pages of 100 with the scroll position preserved; an empty-looking DM thread auto-backfills its latest page, since old threads can sit entirely outside the initial 200-message window); **emoji reactions** (000321); **file attachments** (000322/000323 — any type up to 25 MB; images preview, other files download as chips with their real filename); **direct messages** (000324 — thread-switcher pills, truly private incl. from admins, every DM pushes + badges coral like a mention); **per-author bubble tints** (000330 — a 10% wash of the sender's own identity colour, so a glance down the thread reads as "Chris, Chris, Donna" without parsing names; palette + uniqueness rules in `src/lib/designerColours.ts`). The floating dropdown is **user-resizable** (bottom-left grip, `pv:chat-size`) and can be **docked into the dashboard right rail** (`pv:chat-placement`, sticky bounded card above the activity panels — `DockedChat` in `DashboardPage`; height defaults to 42rem so it scales with browser font size, and the grip under the card drags it taller/shorter, persisted as `pv:chat-dock-height`); the header control is a labelled "Chat" pill (the Feedback icon moved into the Account menu in the 2026-07-17 header declutter). Mobile: the bottom tab bar is **Proofs · Orders · Chat · Activity · More** (2026-07-17 restructure) — Chat is a first-class tab to the `/chat` page with a count badge (coral when personal), Activity opens the dashboard activity sheet (in place on the dashboard, else via `/?activity=1`), and Quote / Flagged / Notifications / Feedback / Admin / profile / sign-out fold into the More sheet. In the room only @mentions push; DMs always push (kept signal, not noise). All shipped on PR #472.
- `/proofs/new` — new-proof form with company+contact pickers; pre-fills from `?companyId=…` or `?contactId=…`
- `/proofs/:id` — proof detail: status pill + Custom quote pill, approve/abandon/reopen flows with confirm dialogs, Copy customer URL, per-name "Team share links" copy buttons (when the current version has team sharing on, 000304), Preview, Versions table with Current + Custom quote indicators, version modal (read-only), an Activity timeline (with per-version reply attribution from 000215), and the needs-attention resolve/snooze/nudge popover
- **Closing a project off, optionally telling the customer** (migration 000366). The Abandon dialog is now `src/components/AbandonProjectDialog.tsx` rather than the plain `ConfirmDialog`: same copy, same "Abandon project" button, plus an **unticked** "Let *name* know" box that reveals the editable `proof_abandoned` message. Silent is the default and is byte-for-byte the old behaviour. See the 000366 entry above for the send ordering, the proof-tagged idempotency ref, the `hs_status: 'closed'` thread handling, and why the message carries no proof link.
- **Adding a version to an APPROVED project** (no migration — frontend only). The "customer approved, then asked for one more thing" case, most often *a colleague's card added to a signed-off design*. Both "Add a new version" buttons used to be hidden the moment a proof locked, so the only visible action was **Reopen**, which calls `reopen_proof` and DELETES every approval row on the proof (000158) — throwing away a sign-off nobody asked to undo, and taking the pricing with it. Adding a version is the non-destructive path and was always supported underneath: `resetApprovedProofToInProgress()` in `NewVersionPage` flips the parent status back at SAVE time (not on navigate) and leaves the approval history alone, and the carry-forward block re-applies each unchanged slot's approval to the new version — so only genuinely new or changed cards need approving again. The buttons now show on approved proofs behind an explainer dialog, gated on `canAddVersionKeepingApprovals` = approved **and** no order in `sent` / `paid` / `fulfilled`. That gate keys off `reopenOrder`, NOT `hasOpenOrder`, so it also catches a **lapsed** pay link — still `'sent'` in the DB and reactivatable from the Orders page, so a customer could otherwise pay against superseded artwork. Any live order means the order-aware Reopen (which cancels the link or holds the job for revision) is the correct route and the shortcut stays hidden; abandoned proofs stay Reopen-only. The Reopen dialog's own copy now names the cost — it previously described only the status flip, which made the destructive action read like the routine one. ⚠ Pricing context: the split-name tooling surcharge is driven off the version's `names` roster (`create-order` reads `names_count`; the customer page renders `(names.length - 1) × surcharge`), so **both people must sit on the same version** for one order at combined-quantity pricing. A second project instead means two orders each priced at its own smaller quantity — Combine payments merges the payment, not the price.
- **The one-recipient → two-recipient approval trap** (no migration — frontend only; the follow-up to the entry above, found on its first live use). A card with exactly ONE recipient has no Shared slot at all — `effectiveShared = shared && names.length !== 1` in `NewVersionPage` — so the common front hangs off that person's *name*. Adding a colleague brings the Shared slot back, and `handleSharedChange(true)` discarded every per-name front as an orphan, forcing the designer to re-upload it. That re-upload is a new storage object, so the first recipient's slot was no longer byte-identical and **their approval didn't carry** — the proof landed on the dashboard flagged `approved_earlier_version` and the customer had to approve again or be overridden (real case: Renewafuel Home Innovations v5 → v6, where v5's front sat on `associated_name = 'Robert Bradley'` and v6's on `null`). Three fixes: (1) `effectiveAssocForCarry` (renamed from `effectiveAssocForVariantRoundCarry`, which only handled the unassigned→per-name direction) now also re-maps a carried per-name image into the Shared slot when the side has become shared — **gated on the SOURCE version having had exactly one name**, since with two or more there are several candidate fronts and no honest way to pick one; the image keeps its stored file, so the approval carries. (2) `handleSharedChange`'s discard confirm excludes those same images — it would otherwise warn about losing artwork that now survives, talking the designer out of the correct shape. (3) The carry rule itself moved to `src/lib/approvalCarry.ts` (`pnpm test:approval-carry`, auto-discovered by `pnpm test`) so the save path and a new **pre-save warning** ("Kelly Wiltshire will need to approve again — their artwork has changed since they approved it", by the Save button) ask the same question; the warning is deliberately one-directional and only models the ARTWORK rule, so it can under-report — an option-tab change or a shifted QR set (000169 nulls `qr_confirmed_at`) can still cost an approval it doesn't predict, and silence never means "nothing to do". Also: the "Customer approved a non-current version" banner suppresses its generic *"open that version and Set as current"* suggestion when the current version carries recipients the older one doesn't, naming them instead — following it there would silently drop the colleague you just added.
- **Duplicate project** (proof-detail overflow menu, no migration — frontend only) — the repeat-order path: a customer who received their order and wants more gets a FRESH open project rather than a from-scratch rebuild or a messy reopen. One click copies the source proof's current version into a new proof for the same contact as its v1: images (artwork + QR rows, storage objects **copied** — never path-shared across proofs, since project deletion removes storage files directly), material/variant fields, names, ink names, letterpress colours, option tabs, round variants / layouts (ids remapped), `cloned_from_version_id` lineage, and an internal-notes pointer back to the source. Deliberately NOT copied: approvals, views/events, orders, reminders, change notes, and the Help Scout link — the new proof carries a `helpscout_override_reason` (satisfying the 000067 CHECK) until the designer links the reorder conversation via the detail-page pencil. Pricing needs no snapshot work (customer page reads live tiers; the 000070 trigger re-stamps the split-name surcharge on insert), so the duplicate quotes today's price list. Engine `src/lib/duplicateProof.ts`, pure mappers `src/lib/duplicateProofMapping.ts` (`pnpm test:duplicate`); rollback on any failure deletes the new proof (cascade) + copied storage objects.
- `/proofs/:id` → **Create order** (approved proofs only) → `OrderBuilderModal`; **`/orders`** (`OrdersPage`) lists orders (Awaiting payment sorts newest-first, expired links pinned) and reactivates expired pay links, and carries the **Combine payments** flow — select mode, `GroupOrdersModal`, per-group banners (copy combined link / split back up) and per-member Release. See **Ordering & checkout**. **July 2026 navigation overhaul:** the pipeline tiles ARE the section filter (tap to focus one stage, tap again for all — the dashboard's tiles-as-filters pattern; the old duplicate chip row is gone); each card shows ONE visible primary action with the rest in a shared `CardActionsMenu` ("⋯") overflow; To-order cards are two-state (collapsed triage row with Folder/Date/Colour readiness chips + "Prepare order" → expands to the prep form; a failed invoice auto-expands with Retry in the banner); the "Needs action" panel hides when empty, renders as a compact banner on mobile, and its rows jump to (and briefly ring) the stalled order's card via `jumpToOrder`; Recently ordered is collapsed by default (auto-opens while searching); the combine-payments confirm rides a floating bottom bar; an unpaid combined payment renders as ONE tinted block with its member cards nested inside (the group header is the container, not a sibling banner — nested members drop the group pill / own-link expiry / opened + pause lines the wrapper already states); section headers stick under the top bar on mobile. Visual verification: `pnpm vite --config vite.verify.config.ts` serves `verify-harness/` (the real page against a fixture supabase client — no project/auth needed) for Playwright screenshots; `?path=/admin/…` on the harness URL mounts the admin shell instead (grouped sidebar + the consolidated Catalogue data / Content / Follow-ups pages).
- `/proofs/:id/versions/new` and `…/edit` — Pricing Display → Specification → Proof Images → Change Notes → Pricing Table, in that order
- `/bundles/:id` (`SetWorkspacePage`, migration 000311 — bundle orders Slice 3; `/sets/:id` kept as an alias; UI says **bundle** throughout, code/DB keep `proof_sets`) — the designer **bundle workspace**: shared-context header (customer/HS/currency, entered once), member-card list (thumb, status, discard reason, Continue building / Open / Remove / Restore), **[+ Add a card]** (creates the proof shell from the set's context → straight into the card builder), **Preview customer view**, and **Send bundle to customer** (compose from the `set_review_link` template (display name "Bundle review link") → `send-helpscout-reply` anchored on the first card, send-evidence stamped on every member's current version, `sent_at` = first send). Post-send additions are allowed (10 Jul): the new card shows "not announced yet" until **Send update** (template `bundle_update_link`, 000312) posts on the same link and stamps its send evidence; removal stays pre-send only. Two entry points: **Entry A** — the wizard's different-materials guard terminals offer "Build as a set of cards" (via `onStartSet` from NewVersionPage; this proof becomes card 1); **Entry B** — "Add another material" in the proof-detail header overflow. The dialog opens on an INTENT guard first ("an extra card, or a change of direction?") because a different-material *revision* request uses the same words — the *instead* answer redirects to Add a new version (or the reopen flow on a locked proof), mirroring the wizard's QS2 guard. The set path then has two routes: *Start a new card from scratch* (creates/extends the set + a fresh sibling card; a sent set spawns a fresh one) or *Bring in an existing project* (attaches one of the contact's other standalone proofs as another card — `attachProofToSet`; same picker available on the workspace as **Add existing project**). Helpers in `src/lib/proofSets.ts` (`createSetFromProof` / `addCardToSet` / `attachProofToSet` / `listAttachableProofs`); a member proof shows a "Part of a set" facts row linking back.
- **Proof-type wizard** (`src/components/ProofShapeWizard.tsx`, `LayoutsEditor.tsx`) — replaces the old "business vs membership" toggle on the new-proof / new-version path. Resolves one of four structural **shapes** (see `docs/proof-type-wizard-structure.md` for the question script): **Recipients** (`cardType=business` + names roster), **Set (single)** (`cardType=membership`, empty names, `shape=set_single`), **Set (collection)** (`shape=set_collection`, one titled `proof_layouts` row per design, each an approve-each slot), **Selection** (`isVariantRound=true`, + `isPerDirectionPricing` on mixed materials). Reserved vocabulary used verbatim throughout: **layout** = one design, **set** = the whole proof, **batch** = a quantity of identical cards, **allocated** = tied to a named person, **personalisation** = per-card unique data. Personalisation is the single material-gated Set question (shown only when `materials.supports_personalisation`). A kept-together multi-material Set (collection) forces a `custom_quote` (the single-material grid can't price across materials). **Continuation note:** a per-direction-pricing **Selection** stores no version-level material (`material_id` NULL per 000142/000144), so adding a new version used to dead-end on an empty Material field with a greyed Save. The *Continuing from which direction?* picker now maps the chosen direction's free-text label back to a catalogue material via `src/lib/matchMaterial.ts` (conservative — "exactly one candidate or null": auto-pick only when the label names exactly one catalogue material, otherwise prompt the designer rather than guess a metal they'd have to catch. So "Silver Steel"/"Brushed Steel" → Stainless Steel since silver/brushed are finishes, not materials; but "Copper Steel"/"Gold Steel" stay null and prompt, because copper and gold are real materials and the label could mean steel-in-that-finish OR the Copper/Gold Metal product) and pre-fills it, landing the continuation as a normal single-material proof. The new-version Save button also surfaces *every* disabled reason — including the proof-type-wizard gate, which `missingFieldItems` never emitted — as a `To save: …` line by the button and bullets on the summary card (shared `saveBlockers`). **July 2026:** the Selection branch gained a customer-intent guard (QS2) — choosing "Different materials" now asks *"Will they pick one, or might they order both?"*; only "they'll pick one, prices can wait" resolves to the per-direction Selection, while an explicit ask for prices or any order-both/unsure answer shows terminal split guidance (a project per material), reusing the `split-guard` kind. The intent answer (`selectionIntent`) is UI-only, never persisted; the reverse-derivation functions seed `'pick'` for existing per-direction versions so they still reconstruct as resolved. Q1's first option was relabelled ("Cards showing each person's own contact details") and the Alternatives helper gained "side by side"; split-guidance dead ends now link to `/proofs/new`. Tests: `pnpm test:wizard`. See `docs/proof-type-wizard-structure.md` §8. **Selection is destructive — guarded since July 2026** (`src/lib/selectionShapeLoss.ts`, `pnpm test:selection-loss`): the variant-round save path forces `names=[]` and writes ONLY the per-direction artwork, so switching an existing version to Selection drops the recipient roster and every carried/uploaded per-person image. Nothing warned about this, and **Recipients → Selection doesn't even trip the card-type confirm** (both derive `cardType='business'`), so the set vanished in silence. It reached a customer: The Experience Auto Group's 8-name carbon-fibre proof (455a1334) was rebuilt on 28 Jul as a 2-direction "pick one" holding one person's card — the other seven gone from the current version, the bundle page showing the card with no recipients, the proof unable to finalise (variant rounds bail out of the approval finaliser, 000141) and its paid order stranded in `revision`. `handleWizardChange` now confirms the loss by count *before* any other state is applied (running it after the card-type flip would leave the form half-switched on a decline). ⚠ Note the underlying gap remains: there is no way to offer two options for ONE recipient inside a Recipients version, which is what pushed the designer to Selection in the first place.
- `/quote` (`QuotePage`) — designer-only **Quote compiler**: builds a quote across material/variant/quantity with shipping. FedEx international rates (`fedex-rate` edge function + `fedex_rate_cache`, per-card `material_variants.weight_grams` + box tare + admin adjustment %) and flat DPD domestic-UK rates (NI detected from a `BT` postcode); EUR/USD display converts via Frankfurter/ECB. Admin-editable inputs live under Admin → Lead times / Card weights / Settings.
- Required fields: pricing display choice, currency (in standard mode), material, variant (when applicable), ink names (when material.requires_ink_names), images per option tab. Validation uses a `submitAttempted` flag + `shouldHighlight(key)` helper — rose borders + "Required" labels appear only after first save click; they clear live as fields become valid. First invalid field scrolls into view; a rose-toned toast ("Please complete all required fields to save") auto-dismisses after 5s.
- `/admin/customers` — admin customer manager. Collapsed-by-default list (one row per company: chevron + name + numeric project-count chip + Edit + optional Delete); click the chevron/name to reveal contacts. A live search across company / contact / email auto-expands every visible company so contact-only matches surface immediately; renaming a company force-opens its row while edit mode is active. Sort dropdown: Alphabetical / Newest first / Oldest first (companies + the No-company group; contacts inside a company always stay alphabetical). Inline-edit: company name (case-insensitive uniqueness guard); contact name + email + parent company (incl. move to/from "No company", surfaces the `(company_id, lower(email))` 23505 as a friendly message). The count chip on a company toggles a project list (newest activity first, each row links to `/proofs/:id`) — independent of the contacts-expansion. Contacts have their own count chip with the same projects behaviour. Audit actions: `company.updated`, `contact.updated`. Pulls project rows from `public_dashboard_projects` (limit 2000) and groups client-side. No schema work — companies/contacts RLS in 000015 already allows authenticated updates.
- `/admin/*` (admin-only, `RequireAdmin` → `AdminLayout`) — a **grouped sidebar** since the 2026-07 admin nav cleanup (21 flat tabs → 13 links under 5 headed groups; on mobile the groups flatten into one horizontal strip). **Performance**: Analytics (conversion funnel + weekly approval-rate trend, hot-leads worklist, per-designer team performance, product/segment breakdowns; reads the 000276 `analytics_*` functions). **Customers & orders**: Customers (list + `:companyId` detail, the rebuilt `admin_search_customers`-backed surface), Order log, Shipping (delivery details a paid order needs before it ships — fill the blanks on offline/free orders, correct or redirect an address; PR #460), Outsourcing. **Messaging & automation**: Follow-ups (rules engine + dormant cutoff + automation config, with the stranded-approvals report folded in as its final section), AI drafts (Drafts ledger + Briefing editor + Proposals approval panel), Content (`/admin/content/:tab` — Messages = the reply-template editor, Site copy = editable customer copy: metal thickness notes, about-proof, QR panel, login page). **Catalogue**: Materials + Create material + per-material editor, Pricing (+ import) + per-material/add-on editors, and **Catalogue data** (`/admin/catalogue/:tab` — Lead times · Card weights · Prototype prices · Xero item codes (with the invoice self-test folded in below the grid) · Material options · Paper colours; tabs are lazy-loaded so the xlsx-heavy Paper colours grid keeps its own chunk). **System**: Users, Settings (nine on-page sections with a sticky jump nav + scrollspy — the Phase 3 alternative to splitting the page), Activity (audit log). `/admin` and the header Admin pill land on the **Admin home hub** (`AdminHomePage`, the `/admin` index route) — a searchable overview listing every section as a card, rather than the old redirect straight into the Analytics data page (which read as empty above the fold). A **feature search** finds any admin page or setting without the exact name (fuzzy: partial word / subsequence / typo / synonym): the sidebar "Search admin…" box, the `/` shortcut, and the hub's search prompt all open the `AdminSearch` palette, backed by the `src/lib/adminSearch.ts` registry + fuzzy matcher (one list also drives the sidebar nav + hub cards, so they can't drift). Results deep-link into sub-tabs and — via a `#hash` the `AdminSettingsPage` scrolls to — the individual Settings sections. Every old flat URL (`/admin/lead-times`, `/admin/templates`, `/admin/site-copy`, `/admin/xero-self-test`, `/admin/stranded-approvals`, `/admin/core-colours`, …) redirects to its new home. **House rule to stop the sprawl regrowing**: a new per-material/per-variant value becomes a new Catalogue data tab, a new toggle becomes a Settings section — only a genuinely new *job* earns a sidebar entry (add it to `ADMIN_DESTINATIONS` with `sidebar: true` and it shows in the nav, the search, and the hub at once). Page files in `src/pages/admin/`.

### Design system

⚠ **The customer page's approved signal is a token, not a hex — and its value is constrained by contrast, not taste.** `--c-in-stock` / `--c-in-stock-soft` are rescoped inside `.customer-accent` (`src/styles/design-tokens.css`) and drive all twelve places approval is signalled on `/p/:id` (pill, hero card fill/border/left rule/medallion/heading, the `OutcomeCapCard` cap, the chosen-variant border, the share-panel meter). The root green `#0e9b4e` manages only **2.97:1** as pill text on its own tint — under AA at the pill's 11px — which is why the customer scope originally deepened it into a cyan (`#0a6f69`, 5.45:1). July 2026 moved it back into the green family without losing that: **`#17703c` on `#dcfce7` = 5.59:1**, marginally better than the cyan, white-on-fill for the medallion 6.02 → 6.14:1. ⚠ Hue matters as much as contrast here: at 145° it sits ~13° off the page accent (`--c-brand-500 #58c59d`, 158°), so approval reads as its own signal rather than a dark version of the page furniture — which is exactly what a legible-but-wrong candidate like `#24664f` (159°, **1°** off the accent) would have done. Approved and changes-requested share the pill and cap-bar idiom exactly, so hue is the ONLY thing separating them at a glance; keep any future value clear of the amber `#8a5a00` too. The designer surfaces still render the root `#0e9b4e` at 2.97:1 — a real pre-existing AA failure, untouched here.


`src/design/` is the shared Plasma UI kit introduced in the 2026-05-29 reskin (`Button`, `Input`, `Field`, `Pill`, `ProofStatusPill`, `PanelShell`, `DesignerChrome`/`DesignerHeader`, `PlasmaWordmark`, etc., barrelled via `src/design/index.ts`; live demo at `/__design`). Design tokens are in `src/styles/design-tokens.css`. Prefer composing these primitives over hand-rolled Tailwind on designer/admin surfaces; match the existing component for anything new.

## Ordering & checkout

Approved proofs can be turned into a paid order. Built on PR #317 (embedded Stripe Elements, merged to `main` 2026-06-22, superseding the earlier hosted Stripe Checkout). Spec: `docs/ordering-checkout-spec.md`. Gated by `settings.ordering_enabled`; **`settings.payment_mode`** selects test vs live Stripe keys — **`live` as of 2026-06-28** (real charges now process through the embedded flow). The one-off live-Xero verification noted under go-live below may still be outstanding.

Flow:

- **Designer** opens an approved `/proofs/:id` → **Create order** → `OrderBuilderModal` (material/variant/quantity, per-person split, shipping treatment, US tariff) → `create-order` edge fn writes an `orders` row (`status='sent'`) + a pay-page token, then posts the pay link on the proof's Help Scout thread via `send-helpscout-reply` (so it reaches the project contact, not the checkout email).
- **Customer** pays at **`/order/:id?token=…`** (`OrderPayPage`) — an on-page embedded Stripe Elements form (Link email + Address + Payment), no redirect. The Address element also collects a **required recipient phone number** (000310) for the courier paperwork; it persists as `orders.ship_to_phone` with the other delivery details at payment time. Open-quantity / per-person-split pricing is computed client-side to match the server. `create-checkout-session` builds the PaymentIntent and persists the price breakdown (`amount_cards/tooling/personalisation/shipping/us_tariff`).
- **`stripe-webhook`** (`payment_intent.succeeded`, signature-verified, no JWT) flips the order to `paid`, creates an **itemised Xero invoice** (product item code + tooling `020` + shipping `050` intl / `052` UK + US tariff `910`; non-UK invoices book their lines to the zero-rated "0% EU"/"0% ROW" tax rates per 000306; collapses to a single summary line — dropping the custom tax rate too — if Xero rejects any code, the classic Demo-org case), optionally records a clearing-account payment (`settings.xero_stripe_account_code`, null today), emails the VAT invoice, and posts a branded confirmation on the HS thread. `order-vat-invoice` (anon, token-scoped) backs the self-serve "Download VAT invoice" link on the paid screen — only once the invoice reads PAID.

**US tariff & customs handling** (000249): a flat $39/£39/€39 charge added by default to US-bound orders, with a pay-page opt-out (the customer then deals with US Customs and any tariffs themselves). Its own Stripe + Xero line (item `910`, export/no-VAT). Per-currency fee + item code + customer copy live in `settings`, admin-editable under Admin → Settings. Pure resolver `resolveUsTariff` in `_shared/orderPricing.ts`.

**Supplier spoilage overs** (000328, `proofs.orders.supplier_overs`): for a hybrid order — base cards made by a supplier (the outsourced route), then FOILED in-house — foiling spoils some cards, so the supplier must make more blanks than the customer bought. The place-order review screen (`OrderReviewPage`, supplier route only) has a manual **"Spoilage overs"** field (starts at 0, no auto-default, no detection — Rob's call) that pads the supplier email's `Qty:` line to `quantity + overs`. The customer's quantity is untouched everywhere else — invoice, per-person split, and the in-house finishing target all stay at what they bought — so the overs only reach the supplier. `place-order` computes `supplierQty` on the supplier route only and stamps `supplier_overs` on the order at placement (surfaced on the order log). Qty > split-sum is expected when overs are added (extra blanks are unallocated spares); Stock Control's outsourced importer already tolerates it (live e.g. order 403875: outsourced 800 vs split 750). Foiling on plastic (Swype/QX) and metal shows up in the free-text ink names, not a variant, which is why detection was deliberately left out and the number is entered by hand.

**Channel Islands & Isle of Man VAT** (no migration — pure code): the Channel Islands (Jersey `JE`, Guernsey `GG`) are outside the UK VAT area, so a **GBP order delivering there is charged ex-VAT** — every VAT-inclusive pricing INPUT (price-tier totals, split-name tooling, personalisation rates, finish surcharges) is stripped ÷1.2 and rounded to the penny *before* the interpolation maths (the round-up-to-£1 step isn't scale-invariant, so stripping a computed result would drift from the pay-page mirror), and the Xero invoice books VAT-free via `InvoiceParams.vatFree` — since 000306 that means every line carries the org's zero-rated **"0% ROW"** rate (the islands are outside the EU VAT area too), falling back to the old `NoTax`/`TaxType NONE` "No VAT" treatment while the settings codes are unset. Custom-quote figures and fixed card discounts are charged at face value. The **Isle of Man (`IM`) is deliberately NOT VAT-free** — it's inside the UK VAT area, so VAT applies exactly as on the mainland; it differs from GB only in shipping (FedEx-rated, and its invoice shipping line books to the domestic item `052` because the invoice's domestic flag now follows the UK VAT *area*, not `country === 'GB'`). A customer who picks "United Kingdom" with a JE/GY/IM postcode is normalised to the island's own code (`normaliseShipDestination`, mirroring the NI BT-postcode detection) so pricing, FedEx rating, the persisted `ship_dest_country` and the invoice all agree. Relief is derived from the RATED destination only (`ship_dest_country`), never the Stripe card-form address, so the tax treatment always matches the stamped amounts; for free/manual-shipping orders it comes from the designer's country hint, so **set the country hint on Channel Islands orders**. Twin helpers in `supabase/functions/_shared/ukVatArea.ts` + `src/lib/ukVatArea.ts`; `pnpm test:vat-area` imports both copies and fails on drift. The Quote compiler (`/quote`) deliberately doesn't know about the islands yet (its country list has no JE/GG/IM) — quote CI jobs knowing the checkout charges ex-VAT.

**Open-spec orders** (PR #420, merged 2026-07-03; spec `docs/metal-spec-at-checkout-spec.md`; **preference-only finishes** added in 000303): the designer can leave **thickness and/or finish for the customer to choose at checkout** — the builder defaults to it where the choice is real (2+ priced thickness variants = the metal family; 2+ proofed finish tabs), killing the old "awaiting confirmation of preferred thickness/finish" email round-trip that clogged Links to send. Schema (000298/000299): `orders.material_id` (stamped on every order), `thickness_open` / `finish_open` (durable flags — openness ≠ resolvedness; create-checkout-session honours the request body while a flag is set, request-wins like the rating destination, then persists the pick so webhook/Xero/place-order read it; error codes `variant_required` / `finish_required`), `help_requested_at` (the pay page's anon "Not sure? Ask us" → `order-question` edge fn → HS note + reactivate + an Orders-page "Asked for help" chip), and `material_options.photo_url` (per-finish studio photos, uploaded per material in the material editor, stored in the public `material-icons` bucket under `finish-photos/`; exposed via `public_get_customer_proof`). The pay page's **Confirm your card** step runs **quantity → thickness → finish** (quantity first so every option card prices at the customer's real quantity), thickness cards from the admin-editable `metal_thickness_notes` copy (optional `badge` key, code-default "Most popular" on standard 500µm), finish cards preferring studio photo → the proof's per-finish artwork tab → text-only, with a hover loupe + full-screen viewer (`src/components/FinishChoiceCard.tsx`) and a preloaded cross-fade on the recap preview as finishes switch. Excluded: custom quotes, prototypes, reprints, offline orders. **Rose Gold Metal** (`metal_rose_gold`, 000300/000301) exists as a first-class gold-priced clone with its own Xero items (0148/0149/01495) precisely because this feature surfaces material identity — pick it, not Gold Metal, for rose gold jobs; copper remains a separate always-brushed product. Quantity got the same durable flag in **000302** (`orders.quantity_open`, PR #421) after the persist-at-checkout write was found freezing customer-chosen quantities against revisits — openness is always the flag, never column-is-null. **Preference-only finishes** (000303): Full Colour Plastic's gloss/matte is the first finish dimension that changes neither artwork nor price — `finishIsPreferenceOnly()` in `src/lib/materialTraits.ts` gates the divergence from metal's rules: the version form treats the material as option-less (proofs never grow gloss/matte tabs), the builder defaults finish to customer-chooses from the catalogue alone (no 2-proofed-tabs requirement), and the pay page offers every catalogue option educated by `material_options.description` (new column, exposed on `public_get_customer_proof`, rendered under the name on `FinishChoiceCard`, editable per finish in the material editor's Finish photos section). Seed: Gloss (base) / Matte, no surcharge rows — both cards read "Included".

Invariants learned the hard way (all fixed, see the 2026-06-22 commits): the pay page MUST pass `confirmParams.receipt_email` — Stripe does not auto-promote the Link element's email onto the PaymentIntent, and without it the Xero contact has no address so the VAT invoice can't be emailed. The invoice's domestic-vs-international shipping line follows `order.ship_dest_country` (the destination the order was *rated* against), NOT the card-form address country, so the shipping code matches the shipping charge + tariff. The Xero invoice contact is built from the Stripe checkout name/email, not the proof company/contact — a deliberate trade-off Rob accepted (the payer gets the receipt). The VAT-invoice "ready" link only appears once Xero marks the invoice PAID (clearing account or bank feed), so in Demo it stays on the soft "available shortly" copy.

**Combined payments (order groups)** — migration 000309, PR #438, **fully live 2026-07-09** (schema + all edge functions deployed and byte-verified + frontend merged). A designer bundles **any N already-approved separate projects** for one customer into ONE payment: Orders page → Awaiting payment → **"Combine payments…"** (solid button; the list sorts newest-first, expired links pinned) → tick 2+ eligible orders → modal (confirms one payer / one delivery address, sets the combined-shipping billing) → `order-group` edge fn creates the group + its **GRP-** pay link. Eligibility: awaiting payment, online, not already grouped, one currency, proofs still approved, priceable (quantity set, or left open for the customer, or an agreed custom-quote total). **Open-spec members are first-class**: the group pay page (`/order/group/:id`, `OrderGroupPayPage`) gives each open card the same guided choosers as a single-order link — quantity → thickness → finish, forced-choice, nothing pre-selected, the customer's own artwork cross-fading to the picked finish (`src/components/ArtworkFade.tsx`, shared with `OrderPayPage`; tier maths shared via `src/lib/openSpecTiers.ts`, parity-tested against the server by `pnpm test:open-spec`) — with per-card prices and the combined total updating live and Pay gated until every choice is made. Checkout group mode (`create-checkout-session` with `group_id` + `member_choices`) prices every member server-side with the single-order maths, persists each pick onto its order row, computes ONE combined shipping figure (UK flat rate or FedEx on the members' summed weight) + ONE US tariff line, ONE PaymentIntent. `stripe-webhook` group mode flips the group + every member paid and builds ONE itemised Xero invoice (product + tooling + discount lines per card via the shared `invoiceBuild.ts` group builder, one shipping line, one tariff line), one VAT-invoice email, one HS confirmation — all idempotent per group. Guard rails: a member's own pay link **refuses payment unless its group is cancelled** (double-charge protection); `send-order-reminders` skips grouped members; cancelling a member auto-releases it and an emptied group cancels itself; **Release** on a member card returns it to a standalone order with its own live link (one invoice per payment event). Per-card production/dispatch is untouched — grouping changes the money, never the making. Deferred: group-level payment reminders; artwork thumbnails for locked cards + the paid screen; the "Ask us" box on the group page (order-token-scoped, can't authenticate with the group token). **Partial refunds are manual by design** — see `docs/combined-payment-refund-runbook.md`. Test fixtures: **Atari Corp** (contact Nolan Bushnell, non-deliverable @example.com email, no HS thread, auto-nudges disabled) is the standing test customer, kept deliberately for staff demos — don't clean it up without asking Rob. ⚠ Its state as of 2026-08-01, corrected after this doc's earlier "two approved fixture projects" was found stale and cost a build a detour: the **steel 2-name recipients** proof (`b7d0796e-c7a5-44d1-860c-e39f92730521`) is `approved` with artwork on its current version; the full-colour-plastic `set_single` one is `abandoned` with zero images and is NOT usable as a fixture. **The steel one is load-bearing** — it is the target of the Tier 3 uptime check (`e2e-prod/proof-view.spec.ts`, workflow `proof-view-check.yml`), so abandoning it, deleting its images or reopening it turns the production monitor red and pages whoever is on call. The check's failure message names that cause explicitly rather than reading as an outage, but change the fixture on purpose, never in passing.

**Unpaid-order reminders** (`send-order-reminders`, migration 000238; cadence made admin-editable in **000270**): a pg_cron job (`30 9 * * 1-5` = 09:30 UTC weekdays) sends a single repeating reminder on the HS thread. Cadence comes from two `settings` columns, editable under Admin → Settings → Ordering & checkout: `order_reminders_max` (1–5, default 3) and `order_reminder_interval_days` (1–30, default 3). Reminder *k* goes out once *k × interval* days have passed since the link was sent, one per run, up to the max, and stops the moment the order is paid / expires / is cancelled; live sends are additionally gated to the London working-day window (Mon–Fri 09:00–17:00). The old two-stage "gentle at day 7 + pre-expiry" model and the `order_reminder_2` template are retired — there is now one template, `order_reminder_1`, with an optional `{? order_expiry}` block. **Live as of 2026-06-28** (`settings.auto_order_reminders_enabled = true` and the `replies_enabled` master gate on); it ran dry-run before that. Mirrors `send-nudges`; ledger table `order_nudges`. Progress is surfaced to staff on the Orders page (per-card next-due / skip reason) and the proof-detail Activity timeline (`order_reminder_sent` entries).

**Go-live status (2026-06-28):** `settings.payment_mode` → `live` and `settings.auto_order_reminders_enabled` → true are both **done** (verified against live settings). Still Rob's call / outstanding: do one small real order + void to confirm the live Xero invoice (incl. the never-yet-exercised mixed VAT-inclusive + zero-rated-tariff line), and enable the **approved_no_order** needs-attention rule (Admin → Needs-attention — ships disabled, flags approved proofs with no order link sent ≥ threshold working days).

## Help Scout integration on the New project form

The `/proofs/new` form has two lookup mechanisms for linking a proof to a Help Scout conversation:

1. **URL-paste (primary, designer-initiated)** — designer pastes a conversation URL or numeric id into the "Start from Help Scout" field and clicks **Look up**. Hits the `lookup-helpscout-conversation` edge function with the parsed id. Populates the customer/contact/conversation fields from that specific conversation; the designer disambiguates upstream by copying the URL of the correct one.

2. **Email-driven (secondary, automatic)** — fires whenever `selectedContact.id` changes (whether the contact came from the URL-paste flow, manual contact-picker, or `?contactId=` URL param). Hits the `match-helpscout-conversation` edge function with the contact's email. Surfaces a multi-match picker when the email has multiple HS conversations; auto-applies on a single match; opens the override-reason panel on no matches. Filters out `closed`-status conversations on the HS side, so test fixtures with closed threads return zero matches even when the email has a real conversation history — easy to mistake for a missing feature during synthetic testing.

Both flows write to the same `helpscout_conversation_id` / `helpscout_conversation_url` fields on the proof. RLS keeps the columns designer-only.

## Price-list rendering script

A small vanilla-JS script that renders the pricing tables on the three Squarespace marketing-site price-list pages (GBP at `/gbp-price-list`, EUR at `/euro-price-list`, USD at `/us-price-list`). Replaces the old third-party CommonNinja embeds — all three pages cut over as of 2026-05-15. Lives in this repo so it's version-controlled, ships through the existing Netlify deploy, and reads from Supabase via `public_get_price_list` (migration 000184). Pricing data only — same anon-RPC house pattern as the customer-page and lead-times paths.

Source:

- `src/price-list/main.ts` — bootstrap. Detects currency (page URL → GBP/EUR/USD), finds every `<div data-price-table="<key>">` placeholder on the page, calls `public_get_price_list` once, renders each table. Quiet "pricing is being updated" placeholder on RPC failure with one fast retry.
- `src/price-list/fetch.ts` — anon RPC call. Uses `VITE_SUPABASE_URL` + `VITE_SUPABASE_ANON_KEY` baked in at build time.
- `src/price-list/render.ts` — table rendering + injected styles. Matches the live CommonNinja visual spec exactly (Poppins from Google Fonts, identical sizes/weights/colours/borders) so the cutover was visually a no-op. Computes the three surcharge columns: metal mirror/brushed from `material_option_surcharges`; carbon-fibre CNC from `carbon_fibre_cnc` − `carbon_fibre`; letterpress gilding from `paper_letterpress_gilded` − `paper_letterpress`.
- `src/price-list/tables-config.ts` — the 16-table config (one per CommonNinja table). Each entry pins a stable `key` (the placeholder's `data-price-table` value), the `material_code`, a `dimension` (`ink_count` | `thickness` | `finish` | `default`) describing how columns map to variants, the verbatim `column_headers`, the per-column lookup keys, the ordered `quantities` to render as rows, and an optional `surcharge_column`. Verbatim from the 2026-05-15 parity audit.
- `src/price-list/golden-master-gbp.ts` — the verified GBP cell values from the parity audit, one entry per table with `expected_gbp` and optional `expected_gbp_surcharge`. Hard gate.
- `src/price-list/golden-master.ts` — pure check logic. Calls `buildTable` on the same RPC payload the script renders from, diffs every cell + surcharge against the golden master, returns a flat result list.
- `src/price-list/harness.ts` — preview-harness wiring. Renders all 16 placeholders against the live RPC and prints a per-table pass/fail tally plus a list of every mismatch.
- `price-list-preview.html` — dev-only QA page. Loads the harness via Vite dev server. Not part of the deployed build.
- `scripts/check-price-list-golden.ts` — same golden-master check, headless. Wired as `pnpm check:price-list`.

Build:

- `vite.price-list.config.ts` is a second Vite build target, library mode in IIFE format. Emits `dist/price-list/price-list.js` with a stable unhashed filename so the Squarespace `<script src>` URL is permanent. Runs as part of `pnpm build` (after the SPA `vite build`) and ships in the same Netlify deploy under `proofs.plasmadesign.co.uk/price-list/price-list.js`. The Supabase URL and anon key are interpolated at build time via `import.meta.env`.

Squarespace side: each of the 16 tables per currency page is a `<div data-price-table="<key>">` placeholder; the page loads the script once via `<script src="https://proofs.plasmadesign.co.uk/price-list/price-list.js" defer></script>`. The script detects currency from the page URL, calls `public_get_price_list` once, and fills every placeholder it finds.

## QR decoding and artwork scanning

### The decoder (`src/lib/qrCodes.ts`)

Third decoder in this slot, each swap driven by real uploads rather than taste: **jsQR** → **`@zxing/library`** (the JS port, after a stylised dotted QR broke jsQR) → **`zxing-wasm`** (zxing-cpp compiled to WebAssembly, July 2026). Don't trust older notes saying jsQR or ZXing-JS.

The swap was measured, not assumed. A benchmark took the stored `qr_decoded_data` of **123 live QR rows** and tried to re-find each payload in that version's **artwork** images, counting exact string matches:

| Decoder | Found |
| --- | --- |
| `@zxing/library` (old) | 94 / 123 — 76% |
| `zxing-wasm` (current) | **117 / 123 — 95%** |
| Native `BarcodeDetector` | 118 / 123 — 96% |

Zero regressions (no case the old decoder won) and no extra false positives. The gain is concentrated exactly where it matters: dense vCards on ~800px proof exports went 62% → 100%. `BarcodeDetector` scored marginally higher but is absent from Firefox and Safari, so a native-first chain would make results differ per designer's browser — one code path was judged worth two percentage points.

Notes for future work:
- The `.wasm` is imported as a Vite `?url` asset so it ships **from our own origin** (`dist/assets/zxing_reader-*.wasm`). zxing-wasm otherwise resolves it against a CDN at runtime. Don't "simplify" this away.
- The decoder module is still dynamically imported, so the wasm cost is only paid when a decode actually runs.
- `decodeAllQrs(blob, max)` returns **every** QR in an image with corner `position`s; `decodeQrFromFile` is the single-result wrapper the manual upload path uses. Multi-code detection roughly doubled the hit rate on per-recipient rosters (7/14 → 12/14).
- The old canvas + 2000px-downscale path is gone — `readBarcodes` takes the Blob directly, which also removed the "browser failed to read the image pixels" failure mode on large print exports.

**Correction worth remembering:** an earlier analysis concluded the ~95%-vs-76% gap was a *source resolution* problem and recommended re-exporting proofs at higher resolution. That was wrong. The pixels were always sufficient; the JavaScript decoder was the bottleneck. Don't re-plumb the export pipeline on that basis.

### Artwork QR scanning (`src/lib/qrArtworkScan.ts`)

The designer used to upload a cropped copy of a QR that was already plainly visible in the artwork they'd just dropped. Now the QR section scans the artwork itself and offers each code it finds as a one-click suggestion — pre-cropped from the source pixels, with the recipient inherited from the image's `associated_name` and the side it was found on.

- `scanArtworkForQrs(sources)` takes `{ id, url, associatedName, side, filename }`. Both version forms can feed it the same shape because `preview` is a blob URL for a freshly-dropped file and a signed URL for a saved row. NewVersionPage also passes the v1 images being carried forward; EditVersionPage passes everything in `editImagesByOption`.
- Cropping uses the decoder's corner points, padded ~12% for a quiet zone, cropped from the **source** pixels (not a synthesised white border) so the stored image shows the code as it actually prints. Verified on real proofs: every crop re-decodes to the same payload, including photographs of physical cards shot at an angle.
- Where possible the crop is replaced by a **rebuild** — see below.

### Rebuilding the QR crisply (`src/lib/qrRebuild.ts`)

Customers scan the QR off the screen to test how it behaves on their own phone, so the image has to actually scan. Many proof images are photographs of physical cards shot at an angle, and a small skewed photo of a dense code often won't.

Three approaches were measured over real codes. **Two were rejected — don't re-attempt either:**

| Approach | Result |
| --- | --- |
| Re-encode the decoded **text** into a fresh QR | module-for-module identical only **53%** (38/72) — rejected |
| Sample the module grid off the artwork **ourselves** | **54%** of finds — rejected |
| Render **zxing-cpp's own `symbol` grid** (current) | **89%** of finds, 97% of those scan at 200px |

**Re-encoding the text** fails because encoders split text into chunks differently, so the bit layout diverges even with version + error-correction pinned and all eight mask patterns searched. And there is no "close": when it missed, the median difference was **38% of all modules** — half the time you'd show the customer a code that looks nothing like theirs.

**Hand-rolled sampling** (projective homography from the decoder's four corner points, Otsu threshold, finder gate) worked but only on ~54%: a grid stretched between four fuzzy *outer corners* drifts badly across a steeply-angled photo, and re-thresholding can't fix misaligned geometry (midpoint and wider-cluster variants were tried and recovered nothing). This was ~350 lines and is now deleted.

**What actually works** is that `ReadResult.symbol` hands back the straightened module grid zxing-cpp built internally to decode — it already locates the finder patterns precisely and uses the alignment patterns as interior anchors. Measured: present on 100% of decodes, exactly `4 × version + 17` square every time, and correct even for cards photographed at an angle in someone's hand. We just render it as clean black squares with a 4-module quiet zone. Always black-on-white even when the card prints white-on-black — scans far better off a screen and it's the same code.

⚠ `symbol` is marked **`@experimental`** by zxing-wasm, which is why `package.json` pins `zxing-wasm` to an **exact version** (no caret). Don't loosen it without re-running the benchmark. The risk is contained anyway: `rebuildQrImage` checks the grid is square and matches `4 × version + 17` before using it.

**The guarantee is the round trip.** Every rebuild is decoded again and only used if it reads back byte-for-byte identical; anything else falls back to the plain crop. A wrong rebuild — including one caused by a future change to `symbol` — can't reach a customer because it can't survive being re-read. `ArtworkQrFind.rendering` records which was used (`'rebuilt'` | `'photo'`).

Measured end to end: **~82% → 95%** of codes scannable from the screen at a conservative 200px display size.
- Never auto-attaches. Every find is a suggestion the designer confirms, because the scan is ~95% reliable, not perfect, and because only the designer knows whether a code belongs on this version. **The manual upload path is untouched** — the worst case is exactly the old workflow.
- Runs in the background, caps at 24 images, and fails silently. A failed scan means no suggestions, never an error in the designer's face.

**The mismatch check** is the reason this is worth more than the saved keystrokes. The uploaded copy and the artwork have always been two independent artefacts with nothing forcing them to agree — and the benchmark found a live proof (Robert Olguin Realty, abandoned, never printed) whose artwork QR pointed at `hovqr.me` while the customer was shown a code for `robertolguin.wardleyre.com`. The check fires only on the unambiguous shape: exactly one code read off an image, exactly one attached entry covering that image, and the two disagree. Coverage is **directional** — a shared entry (no recipient) prints on every card so it covers every image, while a named entry covers only that person's artwork. That direction matters: the one real divergence in live data is a *shared* entry contradicted by a *named* card's artwork, which a naive same-slot comparison never sees. Anything ambiguous stays silent; "we couldn't decode it" is never treated as evidence of a mismatch.

## Plasma vCard QR integration

Plasma's hosted vCard product (`https://qcrd.uk/<slug>`) prints a QR that decodes to a short URL only, not to a vCard payload. The proof viewer cross-references that with the vCard app so the customer verifies the actual contact details, not a blind URL.

Two QR types live side by side in the version form's QR section, and the designer picks which one they're adding (plus a third, zero-effort path — see **Artwork QR scanning** below):

- **Customer-supplied QR** — drop an image, the decoder reads it, the existing eight `qr_kind` values handle the rest. Unchanged from 000168 / 000169 apart from the decoder swap below.
- **Plasma vCard QR** — paste a `qcrd.uk` URL or slug, the form validates it against the vCard app via `lookup_card_by_slug`, generates the QR SVG client-side with `qrcode@1.5.4` (locked to the same options the vCard studio uses, see below), and offers a Download SVG button so the designer can drop the exact bytes into the print artwork. Stored as `qr_kind = 'hosted_vcard'`, `qr_vcard_slug = <slug>`, `qr_decoded_data = https://qcrd.uk/<slug>`, `image_path = <generated .svg>`.

Customer side, `QrCodePanel` branches on `qr_kind === 'hosted_vcard'` and calls the vCard app's three anon RPCs (`lookup_card_by_slug`, `lookup_card_links_by_card_id`, `lookup_card_contact_methods_by_card_id`) to render contact fields live. Styling fields (photo, cover image, accent colour, font) are deliberately not shown — those are the customer's to personalise after the cards arrive. If the vCard app is unreachable, the panel quietly falls back to "contact details temporarily unavailable" plus the URL; the page never breaks.

There is **no shared database** between the two apps and **no proof-viewer edge function proxying the lookups**. The vCard app is consulted purely over its existing anon REST surface. Two env vars (`VITE_VCARD_SUPABASE_URL` / `VITE_VCARD_SUPABASE_ANON_KEY`) carry the publishable key into the proof-viewer bundle; the key is already public on the vCard site so baking it in is fine.

### QR byte-identity guarantee

The customer's approval is honest only if the QR they see in the proof is the QR that prints. Two guarantees back that up:

1. **Destination identity.** A QR's destination is fully determined by the encoded string. Both apps encode `https://qcrd.uk/<slug>`; pin base and slug and the destination matches regardless of the QR library.
2. **Byte identity.** `qrcode` is deterministic; identical input + identical options = identical SVG bytes. Verified on `qrcode@1.5.4`: SVG for `https://qcrd.uk/7k2nq8x` is byte-stable at md5 `fe395e7fcf5063a2d7f82b2c9a26faab`, 1161 bytes.

The canonical options live in two places in lockstep:

- `apps/studio/src/components/QrPanel.tsx` in the vCard app
- `src/lib/vcardQr.ts` in this repo

A committed golden fixture (`src/lib/__fixtures__/vcard-qr-7k2nq8x.svg`) and the matching test (`src/lib/vcardQr.test.ts`) catch any drift in the QR library or options. If you change either side, check both — the test only flags drift on the proof-viewer side.

### Files

Library:

- `src/lib/vcardQr.ts` — QR generation, slug parsing (`parseVcardSlugInput`), URL builder.
- `src/lib/vcardClient.ts` — anon RPC wrappers + bundled `fetchVcardForSlug`. Env read lazily inside the helper so unit tests can shim via `process.env` (each ESM module gets its own `import.meta`).

UI:

- `src/components/QrCodeUploadSection.tsx` — adds the "Plasma vCard QR" add path + Download SVG button alongside the existing drop zone.
- `src/components/QrCodePanel.tsx` — adds `HostedVcardView` that fetches and renders the live contact data.

Pages:

- `src/pages/NewVersionPage.tsx` / `src/pages/EditVersionPage.tsx` — read/write `qr_vcard_slug`; hosted-vCard rows use a `.svg` path extension.

### What stayed deferred

- A scan-test on the live qcrd.uk URL during the proof (option 2b from the build brief).
- A `.vcf` download in the proof — at first-proof time the vCard card is still a draft and `.vcf` 404s.

Phase 4 — the `qr_snapshot jsonb` on `proof_name_approvals` — has since shipped (migration 000194). The proof-action edge function writes a slug-keyed snapshot of each hosted-vCard QR's contact fields at approval time, so the designer-side proof page renders exactly what the customer approved, frozen against the now-editable live vCard.

## Edge functions

Supabase edge functions live under `supabase/functions/`; shared helpers in `_shared/` (`helpscout.ts`, `audit.ts`, `replyTemplates.ts`, `nudgeDecision.ts`, `fedex.ts`, `csv.ts`, `orderPricing.ts` (`resolveUsTariff`, tested via `pnpm test:order-pricing`), `ukVatArea.ts` (Channel Islands VAT relief, twinned with `src/lib/ukVatArea.ts`, tested via `pnpm test:vat-area`), `invoiceBuild.ts` (Xero line builder), `xero.ts` (OAuth + invoice helpers), and the `aiDrafts/` package). Deploy with the Homebrew `supabase` CLI and `--project-ref bjvinrzbdrwebylkmbwy` (the merged stock-control project — see memory:feedback_proof_viewer_edge_deploy); never via the dead CLI link. Roughly grouped:

- **Help Scout** — `lookup-helpscout-conversation`, `match-helpscout-conversation`, `fetch-helpscout-conversation-context`, `helpscout-status`, `helpscout-webhook` (inbound events: reply-activity stamps for 000208, `convo.tags` → `proofs.helpscout_tags`, `convo.merged` re-point per `docs/helpscout-merge-repoint-spec.md`), `send-helpscout-reply`, `admin-set-helpscout-user-id`, `admin-test-helpscout`.
- **Customer / proof** — `proof-action` (records the customer's approve / change-request / variant-selection, layers + then defer-hides the staff confirmation reply in HS, writes the 000194 QR snapshot), `proof-feedback` (anon, 000279 — records "not ready to approve" decline reasons into `proof_feedback` and posts a Help Scout note; deliberately separate from proof-action), `acknowledge-disclaimer`, `customer-proof-images` (`select *`s `public_proof_version_images`, the path image rows flow through rather than the RPC).
- **Automation** — `send-nudges` (the follow-up reminder sender; **live** since ~2026-06-15 — `settings.auto_nudges_enabled = true` — auto-sending `sent_never_viewed` + `viewed_not_actioned` (000244) via a rule list; decision logic in `_shared/nudgeDecision.ts`, tested via `pnpm test:nudges`), `ai-draft` (the AI drafting brain; logic in `_shared/aiDrafts/`, guardrails tested via `pnpm test:ai-drafts`, backtest harness `pnpm backtest`), `reconcile-helpscout-tags` (000278 — 15-min cron that pulls current Help Scout tags for proofs the `convo.tags` webhook missed; service-role gated).
- **Ordering & checkout** (see the Ordering & checkout section) — `create-order` (designer creates the `orders` row + pay-page token), `order-group` (designer-only create/release/dissolve of combined-payment groups, 000309), `create-checkout-session` (builds the Stripe PaymentIntent + persists the price breakdown; picks test/live keys by `settings.payment_mode`; **group mode** via `group_id` + per-member `member_choices`), `stripe-webhook` (signature-verified, no JWT; flips paid, creates the itemised Xero invoice, emails it, posts the HS confirmation; **group mode** keyed off `metadata.order_group_id`), `order-vat-invoice` (anon token-scoped self-serve VAT-invoice link, `verify_jwt` left true; group mode via `group_id`), `retry-order-invoice` (manual invoice retry, falls back to the proof contact email; handles group invoices), `send-order-reminders` (unpaid-order reminder sender; **live** since `auto_order_reminders_enabled` was set true, 2026-06-28; skips grouped members), `order-lifecycle` (cancel path auto-releases a grouped member first). Stripe webhook deploys verify_jwt=false.
- **Admin / ops** — `create-user`, `deactivate-user`, `reactivate-user`, `admin-user-password`, `export-audit-log`, `export-pricing`, `import-pricing`, `fedex-rate`, `contact-form-submit`.

## Frontend patterns

- `src/lib/useImageFileDrop.ts` — hook for zone-level + page-wide drag-and-drop image upload. Ignores internal thumbnail reorder drags by checking `dataTransfer.types.includes('Files')`.
- `src/components/PageDropOverlay.tsx` — pointer-events:none overlay card.
- `src/components/PricingDisplayField.tsx` — Standard vs Custom quote radio cards (label-wrapped radios, sr-only inputs with focus-within ring).
- `src/components/CurrencyField.tsx` — GBP/EUR/USD segmented pill (same pattern). Disabled on edit (currency locked at creation).
- `src/lib/labels.ts` — `pluralLabel("Finish") → "Finishes"`, `pluralLabel("Species") → "Species"` heuristic.
- `src/components/PricingDisplay.tsx` — shared price grid, accepts optional `quantitySurcharges: Record<number, number>` that bakes into every cell; exposes "Show all / Show fewer quantities" row inside the table as a full-width accessible button.

## iOS PWA viewport / soft keyboard (`DesignerChrome.tsx`)

Staff use this app as an installed iOS home-screen PWA (standalone). Two hard-won rules live in `DesignerChrome.tsx`; the saga behind them (PRs #483–#494, plus the instrumentation PR #491) must not be re-derived — see memory:ios-keyboard-instrumentation for the full trail.

- **The mobile app frame.** Below `md` the app is a locked `h-dvh` flex column with `overflow-hidden`; an inner `#app-scroll` div owns ALL scrolling and the bottom tab bar is `absolute` INSIDE the frame (never `position:fixed`). This is the fix for the tab bar detaching: iOS pans the layout viewport (keyboard, short docs, overscroll bounce) and `fixed` elements ride the pan, but a frame with zero window-scrollability has nothing to mis-pan. Every frame class is `max-md:` so desktop is byte-for-byte unchanged.
- **The soft keyboard: hands-off while focused.** While ANY editable element is focused, the viewport effect does **nothing** — no height write, no scroll, no transform. iOS presents the keyboard and pans/scrolls to reveal the input natively (exactly like a native app); any JS that mutates layout mid-focus creates a tug-of-war that makes iOS abandon keyboard presentation entirely. Only AFTER `focusout` does it restore: snap `window.scrollY` to 0 and, if iOS stranded its pan (`visualViewport.offsetTop > 0` — an iOS-26.0-era bug leaving the bar floating, thumb-draggable because a compositor pan is unreachable from `window.scrollTo`), counter-shift the frame by that offset until the platform clears it. On iOS 27+ the pan restores itself so the counter-shift rarely fires; it's kept for staff still on iOS 26.x.

Do NOT reintroduce, however tempting: keyboard-height "glue" that resizes the frame while typing; any `scrollTo`/`scrollIntoView` during keyboard presentation; a per-page `visualViewport` snap-back (a `vv.height >= innerHeight - N` check reads as "keyboard closed" *mid-typing* in the standalone PWA, because `innerHeight` shrinks with the keyboard there — this shipped as a real bug in ChatPage and scrolled the page while the user typed); or a permanent `setInterval` re-check. `interactive-widget=resizes-content` in the viewport meta is ignored by WebKit. Separately: the "keyboard won't appear AT ALL" symptom is a known iOS OS-level bug (installed PWAs stop presenting the keyboard until reinstalled/restarted), not an app bug — the cure is delete-and-reinstall the app or restart the phone, and it is NOT caused by anything in this code (confirmed on-device: the keyboard appears even with all viewport JS disabled).

## iOS PWA data freshness — every live feature needs a foreground resync

Same root cause as the section above (iOS treats an installed PWA's page as a suspended native app), different subsystem, and worth stating separately because it silently breaks *data* rather than layout. **Backgrounding the app kills its network connections — including the Supabase realtime WebSocket — but keeps the page in memory. Returning to it does NOT remount React**, so no mount effect re-runs and nothing refetches. A "fetch once at mount, then rely on realtime INSERTs" design therefore drifts permanently the first time the phone goes in a pocket.

The symptom shape to recognise: a list that is frozen **with holes in it** (not merely old), no error, no gap marker — so the screen reads as "this is the whole conversation" when it isn't. It is invisible on desktop, so a feature can look perfect on the Mac and be quietly broken on every phone. Diagnosed 2026-07-20 when team chat on a phone was missing ~15 messages mid-morning plus the three most recent, while the database held them all (PR #513; memory:ios-pwa-realtime-resync).

`syncMessages()` in `src/lib/teamChatStore.tsx` is the reference implementation — copy its shape for anything live (realtime tables, presence, polling, unread badges):

- **Resync on mount, on `visibilitychange`/`focus`/`online` (throttled — those three overlap on a desktop window switch), and on realtime reconnect.** Handle the *failure* statuses on `.subscribe()`, not just `SUBSCRIBED`: a dropped socket sets a stale flag, and the next successful subscribe resyncs the gap. Only reacting to `SUBSCRIBED` means a drop is never noticed.
- **Reconcile, never replace.** The fetched window is authoritative for its own time range (so deletions made while away disappear), while older pages the user pulled in via a "load more" affordance are kept.
- **A failed read must leave good local state untouched.** ⚠ supabase-js returns `data: null` on error, so the idiomatic `data ?? []` silently empties the list — the same class of silent-write trap as the lazy-builder note in "Claude operating discipline". Never derive "there is nothing more to load" from a fetch that may have failed: gating the manual recovery affordance on a flag computed from that same fetch is what turned a stale chat into an unrecoverable one, because one bad read both emptied the list and hid the only control that could refill it.
- **Recompute counters from their stored stamps rather than incrementing them**, or a resync that replays known rows inflates the badges.

Deploy note: the fix cannot install itself on the devices that need it, since they are holding the old bundle in memory — after shipping, every phone needs one manual force-quit (swipe up, flick away) to pick up the new code. Not yet audited: other one-shot-fetch surfaces (dashboard, Orders) likely carry the same pattern.

## Still to build

- Letterpress edge gilding prices
- Decision on engraving / edge colour / die-cut pricing model
- **Ordering & checkout go-live** — built + merged (PR #317, 2026-06-22). As of **2026-06-28** `settings.payment_mode` is `live` and `settings.auto_order_reminders_enabled` is true (both verified against live settings). Remaining (Rob's call): enable the `approved_no_order` needs-attention rule, and before/around the first real US order live-test the mixed VAT-inclusive + zero-rated-`910` tariff invoice on the live Xero org. See the **Ordering & checkout** section.
- ~~Help Scout "follow up" tag sync (Phase 2b)~~ — **shipped 2026-06-12**: `helpscout-webhook` handles the `convo.tags` event and mirrors conversation tags into `proofs.helpscout_tags` (replace-not-merge). Automation interaction: a "follow up" tag suppresses auto-nudges with logged outcome `skipped_followup_tag` (migration 000223 + `_shared/nudgeDecision.ts`). Requires the `convo.tags` event ticked on the proofs webhook subscription in Help Scout.
- **Follow-up automation — LIVE.** `settings.auto_nudges_enabled = true` (flipped ~2026-06-15) and the `replies_enabled` master gate is on, so `send-nudges` sends for real. The cron `proofs-send-nudges` (pg_cron job 8) fires 09:00 + 15:00 UTC on weekdays. **Auto-send rules:** `sent_never_viewed` (Phase 2) and `viewed_not_actioned` (000244). **Review-queue-only:** `approaching_dormant`, `stuck_in_progress` (their Auto-send dials are stored but unconsumed — see the per-rule note in Admin → Needs-attention). Graduating either remaining rule to auto is the same per-rule generalisation 000244 did (extend `NUDGE_RULES` in `send-nudges` + the candidate UNION + `AUTO_SEND_RULES` in the admin page). See `docs/followup-automation-spec.md` / `docs/followup-automation-rollout.md`.
- **AI draft pipeline rollout** — Phases 1–3b are built and the migrations applied (000216/000217/000219/000225–000227). Code lives in `supabase/functions/_shared/aiDrafts/` (drafting logic), the `ai-draft` edge function, and the admin Drafts panel (`AdminAiDraftsPage` + Briefing + Proposals). The live gate is `settings.ai_drafts_mode` — currently `off`/`shadow`; flipping to `live` (drafts appear in Help Scout) is the operational decision still pending the shadow-mode review. Remaining build: Phase 3c edit-miner that auto-files proposals. Specs: `docs/ai-draft-pipeline-spec.md`, `docs/ai-draft-rollout-phase2.md`.

## Git workflow

Commit changes locally as you work. Rob typically pushes manually at the end of a work session, but during this project he's consistently asked for "commit and push" at the end of each task — follow whatever he says. Never skip hooks (`--no-verify`) and never `--amend` a pushed commit without asking.

Local dev: Rob runs `pnpm dev` in a separate terminal. Changes hot-reload so no deploy is needed to verify frontend work.

### DB migrations — the merged-project workflow (verified 2026-06-10)

⚠ **The CLI push path is dead.** Live proof data moved (~2026-06-08) into the
merged **stock-control** Supabase project (`bjvinrzbdrwebylkmbwy`), where
proof-viewer lives in the **`proofs` schema**. The CLI link
(`supabase/.temp/project-ref`), the local `.env`, and the `pnpm db:diff` /
`db:push:confirm` / `db:status` scripts all still point at the retired
standalone project `xpcjanqrcgzjmwketxtt` — which answers with the full
schema and **zero rows**, so anything run against it passes vacuously. The
merged project has no 000NNN migration history, so `supabase db push` at it
would try to replay all ~220 migrations into the stock app. Never repoint the
link for pushes.

The working convention:

1. **Author** new migrations in this repo as `supabase/migrations/000NNN_*.sql`
   (numbering continues for audit trail; run `ls supabase/migrations/0002*`
   first). Write them **schema-qualified** (`proofs.` prefixes); functions pin
   the live convention `set search_path = proofs, public, extensions, pg_temp`
   and SECURITY INVOKER/DEFINER per the live siblings.
2. **Verify live state read-only first** via the Supabase MCP
   (`execute_sql` on `bjvinrzbdrwebylkmbwy`) — column names, grants, existing
   objects. The repo's historical migrations pin `search_path = public` and no
   longer reproduce prod on fresh replay; the live DB is the truth.
3. **Apply** per the prod-gating rule: Rob pastes the migration into the
   stock-control dashboard SQL editor (or approves an MCP `apply_migration`
   with a descriptive timestamped name). Claude never applies to prod
   unilaterally.
4. **Grants are explicit.** The proofs schema has NO default privileges —
   a new table is born with no grants at all (service_role included). Every
   `CREATE TABLE` must state its full grant matrix or the edge functions
   silently lose access.

Edge deploys: Homebrew `supabase` CLI with `--project-ref bjvinrzbdrwebylkmbwy`
(see memory:feedback_proof_viewer_edge_deploy).

## Claude operating discipline

The patterns below come from real friction in PV-2026W20. Each one cost a round trip with Rob; they're worth investing the few extra keystrokes upfront.

**Picking a migration number.** Always run `ls supabase/migrations/0001*` (or Glob) before writing a new migration. Never pick the next number from the migration summary in this doc — it's curated and lags reality. The duplicate-prefix check in `db-push-confirm.ts` is a backstop, not a substitute. The collisions cost two extra PRs in PV-2026W20.

**Staging files for a commit.** Never use `git add -A` in the proof-viewer repo. Stray `_tmp_*` files and untracked assets sit in the working tree and a wildcard add sweeps them into otherwise-clean commits, forcing amend + force-push to clean up. Always pass explicit paths: `git add path/to/file1 path/to/file2`.

**Working around sandbox git lock files.** Cowork sandbox cannot `unlink` files in `.git/`, so the workaround is to `mv` the lock out of the way. Always `mv` to `/tmp/`, never to a sibling name in-tree. Renaming `.git/refs/heads/<branch>.lock` to `.git/refs/heads/<branch>.lock.cleanup.<pid>` leaves an artefact that git treats as a broken ref and breaks the user's next `git pull` with `fatal: bad object refs/heads/...`. Sibling-rename inside `.git/worktrees/<wt>/` is fine — git only walks `refs/`.

**Pre-handoff state check.** Before saying "ready to merge" or "push it now", run a quick state pass: `git status`, `git log -1 --oneline`, `pnpm db:status` if migrations were touched. Catches drift between what you think shipped and what actually shipped. The dashboard-artefact step in PV-2026W20 was based on stale assumptions twice; both could have been caught by this check.

**Trusting curated docs.** Treat the migration summary in this file, and Rob's global `~/.claude/CLAUDE.md`, as orientation only. Both have been the source of "trusted information that turned out to be wrong" in the recent past. Verify against source / the live system before acting on anything from either.

**Drop-and-recreate views must re-state grants.** Dropping and recreating a `public_*` view wipes its grants. Any migration that drops and recreates a `public_*` view must explicitly re-state both `REVOKE SELECT ON <view> FROM anon, public` and `GRANT SELECT ON <view> TO authenticated` after the recreate. The drop silently removes both, with no error — the only symptom is a broken customer page or a permission-denied on the next REST call. See 000168 (forgot the revoke after recreating `public_proof_version_images`) and 000174 (the fix, captured two months later after the drift was discovered in a push chain).

**Re-emitting the pay-page RPCs.** Any migration that re-emits `proofs.public_get_order` or `proofs.public_get_order_group` must keep serialising order rows through `proofs._customer_order_json()` (000365) — never a bare `to_jsonb(o) - 'token'`, the exact regression that shipped twice (000230→000265 and again 000268→000309, which leaked supplier/Xero/artwork-check internals to pay-link holders for three weeks and silently killed the customer tracking panel). Any new `proofs.orders` column a customer must not see goes on the strip list in that helper, in the same migration that adds the column. The rule is also stamped as `comment on function` on all three live objects.

**Future SELECT-only tables need an explicit REVOKE.** `ALTER DEFAULT PRIVILEGES` (000176) grants full CRUD to `authenticated` on every new table by default. If a future migration creates a table that should be SELECT-only for `authenticated` (e.g. a sensitive admin-only catalogue), it must immediately follow the `CREATE TABLE` with `REVOKE INSERT, UPDATE, DELETE ON <table> FROM authenticated`. Forgetting this is silent — RLS may block the writes in practice, but the grant is still unnecessarily permissive and a policy change could expose it.

**supabase-js queries are lazy — a bare `void` builder never runs.** A PostgREST builder only sends its HTTP request when `.then()` is invoked (i.e. when awaited). `void supabase.from(...).update(...)` with no `.then` attached is a no-op: no request, no error, nothing. This shipped as a real bug (July 2026): the chat read-stamps, `feedback_seen_at`, `orders_seen_at` and the bundle `record_proof_set_opened` call were all fired this way and silently never persisted — unread badges cleared locally then resurrected on every reload. Every fire-and-forget write must attach `.then(({ error }) => …)` (log or roll back on error); `supabase.functions.invoke(...)` and `supabase.auth.*` return real promises and are exempt. Applies identically to the Stock Control port's `proofs()` accessor.

## Working style

- Rob is a non-coder doing his first Claude Code project. Explain each step as you go, avoid jargon, don't assume knowledge of build tooling or SQL beyond the basics.
- Keep diffs focused and explainable — one feature per commit with a message that reads in plain English.
- Prefer tight, boring code over clever abstractions. When in doubt, match the existing pattern.
- Before claiming a feature is done, build (`npm run build`) to confirm types + bundle are clean. Skip `lint` unless there's a package.json script for it.
