import type { JSX } from 'react';
export declare function Toggle({ on }: {
    on: boolean;
}): JSX.Element;
/** Both controls write the same value, so both say the same thing. */
export declare const PREF_TITLE = "Keep apps visible";
export declare function prefHint(on: boolean): string;
//# sourceMappingURL=Toggle.d.ts.map